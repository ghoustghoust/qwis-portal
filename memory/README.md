# `memory/`

**放什么**：AI 会话的**按日工作记忆**，一天一份 `<YYYY-MM-DD>.md`。现役一件：

| 文件 | 记的是什么 |
|---|---|
| `2026-09-02.md` | 当天最关键的一条时间线风险：**审查与修复并行进行**，所以 AI 的结论可能基于修复前的代码快照（文里明写"关键含义：AI 的审查结论可能基于 9:40 之前的代码快照"）。这正是 AGENTS「并行修改时须交叉验证最新状态」那条规则的由来 |

**不放什么**：架构事实与验收结论 —— 那些属于 `docs/ISSUES.md`（缺陷账本）、`ARCHITECTURE.md`（坑）与 `docs/FEATURE_MATRIX.md`（SSOT）。这里的内容是**过程记录**，会过期，不许被当作当前事实引用。

**归属端**：一次性（会话过程）

**状态**：frozen（只有 2026-09-02 一份，后续轮次改记进 `docs/ISSUES.md`；不再往里追加，除非又出现"并行改动导致结论失效"这类需要留案的时间线问题）
