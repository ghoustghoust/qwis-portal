# 全网情报系统 · 已知问题清单（活文档）

> 只列**当前活跃**的问题（未修复 / 观察中 / 挂案）。
> 已核销历史：`docs/deprecated/ISSUES-resolved-2026-09-14.md`（09-13~09-15 全量，含热点榜三阶段/媒体治理/精选断更根治）
> 与 `docs/deprecated/ISSUES-resolved-2026-09-13.md`（更早）。
> 功能需求类事项见 `docs/NEXT-DEV-REQS.md`。
> 最后更新：2026-09-21（**文档整理轮**：⛔ 放行清单 15 行整块移出成归档件 · B27~B70 六域登记原文与🟡观察中整节核销出账 · 活跃表四条已核销行出账 · 新登记 **B128**（三处零引用资产，死代码候选不删）· **B105** 按新读数升级为"execFileSync + 根目录 scratch 驱动"这一族 · 产品选择已随用户整表批准转为排期输入。本档自此只留"现在什么坏着 + 下一步做哪条"。）
> 上一轮（09-20 夜）的结论：Turso 换库止血 B118 · 早报无图 B120 · BL8 判据假红更正 · **B104 / B106+B114 / B102 三条收口**；端到端验收轮 9/10，唯一产品红 = B121。
> 归档三件：`docs/archive/debugging/2026-09-21-release-approval-ledger.md`（放行清单 + 放行后落点）· `docs/archive/debugging/2026-09-21-issues-closed-rows.md`（本轮核销原文）· `docs/archive/debugging/2026-09-19-delivery-evidence-ledger.md`（B8~B26 逐条处置与证据对账）。
> 文档清洁与归档规则见 `docs/DOC_GOVERNANCE.md`。

---

## ✅ 放行清单：09-21 整表放行，已出账

> 原「⛔ 待你点头的放行清单」15 行**已物理移出**（含逐字原文、放行后落点表、三条随审计轮冻结的动作）→ `docs/archive/debugging/2026-09-21-release-approval-ledger.md`。本档不再提问：**放行 ≠ 已做完**。
> 排队待做（按"再等下去谁最疼"，逐条证据在对应 B 编号行）：
> ① **B103** 内容级转储（任何删除类验证的前置闸）→ ② **B101 + BL10** 保留策略定稿（用户已定「接回每日清理，7 天 + 豁免」）与 `'null'` 2.5 万行订正（必须排在转储之后）→ ③ **B117** 三份仍绑生产 Turso 的测试搬隔离库 + 新判据 → ④ **门禁扩面**：B114②／B115①②／B116／B124 拟号规范／B126 表格格数／B127 F1~F8 落产物／W9 收紧成"坑号须进用例名或断言消息" → ⑤ **三端小刺打包**：B107／B109／B111／B112／B113／B115／B116 → ⑥ **B102 残余**：`api/[...slug].js:2010` 的第三份删除谓词收进 `lib/retention.js` → ⑦ **B79** 悬空订阅 id（你自己在后台重新勾选）。

---

## 🧮 B1~B26 检验结论（09-21 收口判定轮）

> 判定口径三态：**归档**（已核销，原文只在归档件里）· **待开发**（还欠代码或生产数据动作）· **待复验**（代码已落地，只等一次真跑批/浏览器读数，不欠代码）。
> **本区间没有任何一条还停在"等你批准"**：需拍板的 5 条（B17/B18/B21/B23/B24）已按下方 §产品选择 的推荐默认项整表裁定（用户 09-21），落地时不再回来提问。
> 一手证据：`docs/archive/debugging/2026-09-19-delivery-evidence-ledger.md` §B8~B26 本轮处置（逐条读数与取证脚本）· `docs/archive/debugging/2026-09-21-issues-closed-rows.md` §四（B12/B13/B22/B26 出账原文）· `docs/deprecated/ISSUES-resolved-2026-09-14.md`（B1~B7 核销）。

| # | 判定 | 现在坏着什么 / 还欠什么 | 去处 |
|---|---|---|---|
| B1~B7 | **归档** | 09-12/09-14 两批修复均已核销（B1~B4 与 09-12 审计轮同号不同事，引用必须带轮次） | `docs/deprecated/ISSUES-resolved-2026-09-14.md` |
| B9 | **归档**（号位说明） | 号位被 09-14「退役源引导」占用，活跃表自 B8 起而**无 B9** | 同上 |
| B12 · B13 · B22 · B26 | **归档** | 09-21 出账四条：测试重锚后 `npm test` 全绿 / `/api/meta` == origin/main / 三个颜色类已在 `index.css` 定义 / 重统计已拆成 `GET /api/status/daily-sources` | `2026-09-21-issues-closed-rows.md` §四 |
| B8 | 待开发（已拆出 B92） | 行内标记已正常，**块级结构**（`-` 列表 / `1.` / `###` / 空行分段）`mdInlineParse` 根本不支持；另欠一条"真含 md 标记"的线上样本 | B92 归 37 域（前端渲染），B8 保持活跃 |
| B10 | 待开发（只剩生产数据一步） | 代码面 5 份栏目表已收成 `lib/daily-columns.js` + W13 全仓扫；线上 `settings.daily.columns` 仍是回声文案 → 后台点「恢复默认栏目」**或**授权我改这一条 | 属生产写，排在 B103 转储之后 |
| B11 | 待复验 | 后台 `TabLoader` 骨架屏已落地并有锁 I11；后台在登录门后，我不代你输口令 | 待你浏览器复验 |
| B14 | 待开发（被 B15 阻塞） | `qOne` 本体已修、`reading.digest` 真在生成，但 `readCount` 里 99.94% 来自 `'null'` 脏值 → 用户可见面「我读了多少」仍错 | 随 B15/BL10 一并收口 |
| B15 | 待开发（已批准执行） | 根因分支已在 `tools/migrate-to-turso.js:253`；生产 4 列脏值（`read_at` 24,855 / `tags` 24,355 / `reason` 24,799 / `videos.watched_at` 846）待订正，**写前把原值备份进 `docs/eval/`** | BL10，硬排在 B103 内容级转储之后 |
| B16 | 待开发（已批准执行） | 清洗器（第一人称一票否决）+ 放弃原因出声已就位；线上第 5 期 `theme=null` 需重跑一期周刊覆盖（花 AI 配额） | 与 B121 同批：下一次 weekly 跑批后按形态指纹复验 |
| B17 | 待开发（本轮已拍板） | 初筛窗只有 24min，全量 ≈2.2h → 周刊只策展了 `published_at DESC` 前缀 | ✅ 按推荐：**预筛降量（六维分门槛），不抬配额** |
| B18 | 待开发（本轮已拍板） | `videos` 表既无 `score` 也无 `translated_title`，`enrichBriefTitles` 结构性跳过视频 | ✅ 按推荐：**只建 `score`，放弃视频翻译**（schema + 三端采集语义同步） |
| B19 | 待复验（观察中） | "reasoning 冒充正文"已断（无正文即抛错）；剩模型/额度侧波动 | 本文件观察段（W6 族） |
| B20 | 待复验 + 长期项 | 五个写入点已同口径接进 `applyDailyQualityGate`；欠"下一批日报无 <30 分条目"的实测读数，长期欠本地 `analyzeBatch` 六维分 | 本文件观察段 ② |
| B21 | 待开发（本轮已拍板） | 每日早报无「本期索引」；切回 `/daily/` 卡顿未定位（chunk 冷加载 vs 46 卡重排） | ✅ 按推荐：期号 + 归档与 H13/40-4 一起立项 |
| B23 | 待开发（本轮已拍板） | `rate: status==='ok'?100:0` 假成功率原样在位，比没有数字更误导 | ✅ 先做 35B 滚动窗口补分母，再改端点（阻塞项 BL3） |
| B24 | 待开发（本轮已拍板） | 心跳 168 条只覆盖 25 小时、`failures[]` 截断 20 且无正样本 → 逐源算不出分母 | ✅ 35B 用每源定长滚动窗口替代（BL3） |
| B25 | 不归本会话 | 4 项里 3 项已在 `93eb32e` 提交；第 4 项 `daily-ai.js#isEnabled()` 是**判据变更**不是格式改动 | 由并行会话自己收口，不代提交、不代核销 |

---

## 📋 交付链状态（历史轮次，已归档）

> 09-19 夜轮 / 09-20 上午轮 / **09-20 夜 B104 收口轮**的十一条状态表与洁净轮记录已外迁：`docs/archive/debugging/2026-09-20-round-status-records.md`（判据不变：AGENTS §3 逐条标"跑了/没跑"，只准留一份活的）。

## 🔴 活跃 bug

> B8~B26 的逐条处置与取证在 `docs/archive/debugging/2026-09-19-delivery-evidence-ledger.md`（**归档 ≠ 验收**，仍待你终验）；本表只留"现在什么坏着"。
> 09-21 出账四条（B12 重锚后全绿 · B13 `/api/meta` 已一致 · B22 颜色类已定义 · B26 已拆独立端点），原文见 `2026-09-21-issues-closed-rows.md` §四。
> 号位说明：B9 已被 09-14 批次占用（退役源引导），故本表从 B8 起而**无 B9**；B1~B7 在 `docs/deprecated/ISSUES-resolved-2026-09-14.md`（09-12 审计轮的 B1~B4 是另一批同号不同事，引用时必须带轮次）。


| # | 问题 | 定性 | 去处 |
|---|------|------|------|
| B8 | 综述/文章详情无排版（加粗/重点标注丢失，纯文本渲染） | 前端渲染层（markdown 化） | T5-5 / spec 32 |
| B10 | 每日早报 AI 版仍显示关键词版栏目注解（"Codex、Claude、豆包…"） | AI 栏目 desc 未随 v2 更新 | T5-3 一并 |
| B11 | 后台 Tab 切换懒加载 chunk 冷启动卡顿（前台已做骨架屏，后台未覆盖） | 感知性能 | spec 33 |
| B14 | `tools/collect-turso.js` 调用**从未定义的 `qOne()`**（6 处：`:648 :655 :725 :970 :971 :1216`），每处 `ReferenceError` 被上层 `try/catch` 吞成一行 runner 日志。后果：`reading.digest` 从未生成（**阅读足迹卡自上线起就没存在过**）、`daily-ai stats.videos` 恒 0、collect「少量失败」分支下同一 try 内的 `collectStalled` 停滞检测被整体跳过 | 已修（补 `qOne` + 静态回归锁），待 runner 批次产出验证。详见坑 #33 | 本轮 |
| B15 | **【2026-09-19 推翻，原判为误诊】** 原判「`articles.read_at` 被批量标已读 24,855 行」是错的：真根因是 `tools/migrate-to-turso.js:250` 用 `if (typeof v === 'object') return JSON.stringify(v)` 序列化，而 **`typeof null === 'object'`** → NULL 列被写成**字面字符串 `'null'`**。云端实测 `read_at='null'` **24,855 行**、真 ISO 已读仅 **287 条**；同批污染 `tags`/`reason`/`videos.watched_at`（逐列行数在归档原文）。旧查询 `read_at >= datetime('now','-1 day')` 复现 24,870 行是 **`'null' > '2026-…'` 的文本序假象**，不是有人批量标已读。本地 `data/app.db` 干净，**污染只在云端** | 一行修根因（`v === null` 分支）+ 一次性数据订正（连带 `tags`/`reason`/`watched_at`）。**这批脏值同时毒害三处**：保留天数清理豁免、未读角标、阅读足迹 → 只清 `read_at` 就能让 2.5 万篇回到未读并恢复回收 | 本轮已定性，待授权执行。**09-19 夜追加（受 B101 制约）**：末句"恢复回收"目前是空的——保留清理本身从未触发，且云端此刻还有 **10,733 条**已满足删除谓词的存量待删 → 清 `read_at` 只会让这 2.5 万篇**重新变成"可被删"**。两件事必须一起决策，否则等于把一个未触发的清理器重新点亮 |
| B16 | 周刊导语污染第四次复现（坑 #26）：第 2 期 `theme` 入库为「我需要找到贯穿这些文章的核心主线。」并同步进 `weekly.archive` 标签；且 `generateWeeklyMagazine` 五条 `return null` 全不出声 → coverTheme/storylines 整体为空无从判断 | 清洗器已修（句首第一人称一票否决 + 反向保护用例）+ 放弃原因已打日志；**脏数据需重跑一期周刊覆盖** | 本轮 |
| B17 | 周刊初筛预算结构性不足：本轮 2015 条候选 × `ai.minIntervalMs=4000` 串行 ≈ 2.2 小时，而 `runWeekly` 的初筛窗只有 `BUDGET_MS*0.4`=24 分钟 → 日志必出现「初筛预算截断」，周刊实际只策展了 `published_at DESC` 前缀，**不是全周内容** | 待决策：提高初筛配额 / 改为批量初筛 / 预筛降量（六维分门槛） | 需拍板 |
| B18 | 视频/播客条目的「置信度评分」与「标题翻译」无法呈现：`videos` 表实测**既无 `score` 也无 `translated_title` 列**（列清单：id,source_id,platform,title,url,vid,cover,duration,author,intro,published_at,favorite,created_at,watched_at,play_uri）。`enrichBriefTitles` 也因此结构性跳过视频（id 是 `'v'+id`，查 `articles` 永不命中） | 需决策：给 videos 建列并把视频接进翻译/深析管线（涉 schema + 三端采集语义同步 + AI 配额），或放弃这两个诉求 | 需拍板 |
| B19 | 我的早报「今日总结」线上常空：`generateTheme` 判污染后返回 null（宁缺毋滥，方向是对的），根因是 Agnes 推理模型间歇只吐 reasoning 不吐正文——与坑 #26 同源 | 已断掉"reasoning 冒充正文"（无正文即抛错）；额度/模型侧波动归 W6 继续观察 | 观察中 |
| B20 | 每日早报**完全没有分析后质量门槛**（用户标注「一两颗星是不是含金量不高」抓出）：深析契约无 `veto`，`runDailyAi` 只在初筛用 `ignore`，「重点更新」栏判据是 `source_spotlight`（看源不看分）→ 实测 `score=10`「出售 AI 工作站」reason 自陈"不适合收录至早报"仍坐最显眼大卡。另：本地灾备 `daily-ai.js analyzeBatch` 只返回 `{summary,importance,tags}` **无六维分**，与 runner 深析契约已分叉，门槛无法同口径同步 | 门槛已加（`passesDailyQualityGate`，默认 ≥30 与 `ai.filterThreshold` 同口径，可配 `ai.dailyMinScore`）；**待下一批 daily-ai 跑批生效**。长期需给模型 `veto` 出口 + 本地补六维分 | 本轮（待验证） |
| B21 | 每日早报缺「本期索引」（周刊页 T5-6 已有右侧条目索引可复用），且从别的页签切回 `/daily/` 有卡顿；期号方面 `daily_reports` 有自增 `id`（线上 `report.id=100`）但直接当"第 N 期"无意义（含非 AI 批次插入），仍应与我的早报一起走归档设计 | 索引/卡顿待查（卡顿需先定位是 chunk 冷加载还是 46 卡 + 封面重排）；期号并入 H13 一起定 | 待办 |
| B23 | 云端 `/api/health/source-stats` 的"成功率"是**布尔伪装**：`api/[...slug].js:1693-1704` 注释自陈「云端无 job_queue 历史」→ 实现 `rate: status==='ok'?100:0`。用户要的"成功抓取概率"目前不存在，且这个假数字比没有数字更误导 | 必须先用 35B 的滚动窗口把分母补上，再改此端点；禁止继续用 100/0 冒充概率 | 待 35B |
| B24 | 采集心跳 `settings.cloud.collect.history` 撑不起任何统计：代码注释写「7 天×24」，实测 168 条只覆盖 **25 小时**；且 `failures[]` **截断 20 条、只含失败源** → 有负样本无正样本，逐源算不出分母（`tools/collect-turso.js:489-505`） | 35B 用每源定长滚动窗口替代；顺带把注释与切片按 mode 分池订正 | 待 35B |
| B25 | 上一轮会话中断留下**未提交在途改动 4 个文件**（B8 主题 markdown 化 `DailyPage.jsx:178` + `index.css` `.md-mark/.md-code`、B11 后台 TabLoader 骨架屏、本地 `daily-ai.js isEnabled()` 改判据）。已实测 `npm run build` 通过、`npm test` 297/301 绿（4 红即 B12 既有项，非本次引入）；但 `index.css` 那处编辑把注释收尾和选择器挤成一行 `*/.rail-btn {`（CSS 仍可解析，属破口非故障） | 补一个换行 + 决定是否提交；**未提交 = 未推 = 线上没有 B8/B11** | 待用户确认（页面标注要重做） |

### 09-19 深夜登记 · 采集保留与删除语义（B101~B103）——B102 已收口（含代码），B101/B103 已放行待做

> 起因：用户断言「云端采集把 24 小时以前的文章删掉，而本地采集没有删除逻辑」并要求停手取证。
> **实测前提不成立**（全仓删除只有 7 天窗口，无 24h 删除；无在途提交，线上 `961fec04` == origin/main），
> 但顺藤摸出方向相反的三个真问题。完整读数、行号与判据见 `docs/specs/43-collect-retention-safety/spec.md`。

| # | 问题 | 定性 | 去处 |
|---|------|------|------|
| B101 | **「每天 04:13 保留清理」从未触发**，而三处文档长期写死它在跑（已按实测加注"当前不生效"）。缺环：`cleanup` job 的 `if` 只认 `github.event.schedule`，主力触发器 cron-job.org 走 `workflow_dispatch` 且其 `mode` 选项里**没有 cleanup**（`collect.yml:246` + `:41-48`）→ 生产心跳近 168 轮 **cleanup=0**、216 条 scheduled run 无一落在 20:13 前后。**待删量已量化**：拿那两条 DELETE 的**原样谓词** COUNT → **10,733 条**（保留 8,616 + 热榜 2,117）【⚠️ **09-21 按现役库重算：这个数已经作废** —— 换库后新库没有 `'null'` 污染（BL10），`read_at IS NULL` 真命中 5.9 万行，于是同一条谓词的待删量变成 **50,636 条 = 现役 59,832 行的 84.6%**（热榜 26,361 / 普通 24,275，其中普通那批带 **667MB** 正文）。口径：`lib/retention.js#whereFor('runner', …)` 原样谓词 + `cutoffIso(7)`，读数与取法记在 `docs/eval/bl10-null-audit-20260921.md` §三。**这条读数是 B101 接回触发口前的必看的数**：今天接上，第一次跑就会删掉全库 84.6%（有 B103 的转储可回，但那等于用一次事故验证备份）】，另 `published_at IS NULL` 47 行永不命中谓词 → 这批永远删不掉（B93 脏值族的删除面） | 调度触发链缺失 + 文档不实（**不是**数据被删） | spec 43 · D2/D4（删不删是产品决策）。`.github/workflows/collect.yml` **未动**。心跳 mode 分布与 `cutoff` 等原始读数在本轮精炼删下的归档原文 |
| B102 | **本地那一份清理比云端狠得多，是真正的删数据弹药**：`server/services/datamgr.js:12-17` 的 `CLEAN_TABLES` 含 `articles` 与 `videos`，`:90-103` 的 `cleanup()` **无任何豁免**；`maintenance.js:9-13` 每 24h 调它（`retentionDays ?? 7`），`scheduler/index.js:138-141` 是 `setInterval` → **本地服务一起满 24 小时就开火**。对 `data/app.db` 只读实测：一次会删 **37,593 / 40,775 文章（92%）+ 1,161 / 1,161 视频播客（全部）**。直接违反 2026-09-13 用户决策「视频/播客永不清理」，也违反 AGENTS §1「采集语义三份必须同步」。此刻 3000 端口无响应（调度器没活着）所以尚未发生 | P0 风险（一旦有人起本地服务并放满 24h 即成真）| spec 43 · D1 ✅ **09-20 夜已收口**：谓词唯一实现 `lib/retention.js`（三端共用）+ 白盒 **W17**（判据 `findRetentionViolations`，与锁 R4 同源）+ 行为锁 `tests/regression-retention.test.js` R1~R4；本地作用域 articles/videos = skip。逐条读数与已知空洞（`api/[...slug].js` 的第三份谓词按整文件豁免）见放行清单归档件 §三 #3 行 |
| B103 | **回滚底牌比"过期"更糟：云端根本没有内容备份**。(a) 本地 `data/backups/` 最新一份曾停在 **2026-09-06 20:13**（397MB），当前 `app.db` 703MB —— 近 13 天无本地快照【**09-21 更正**：09-20 换库救援时其实留了 `app-rescue-20260920021435.db`（705MB / 41,254 文章），本轮又刷了 `app-20260921-014112.db`（715MB / 43,512 文章，逐表核过非空）；旧说法只数了 `app-*.db` 命名，漏了带后缀的那几份 —— 记此防再误读】；(b) **云端 `POST /api/backup` 只导出 `sources`/`groups`/`settings` 三张配置表**（`api/[...slug].js:1965-1972`），`handleBackupRestore:1984-1993` 也只 upsert 这三张表 —— **`articles` 与 `videos` 不在任何备份里**，云端不存在内容级恢复路径。结论：保留清理一旦被点亮且删错，线上文章**无处可回**（上游 RSS 早已翻页，重采不回来） | 前置安全条件（不是产品缺陷，但它是任何删除类改动的第一道闸） | ✅ **09-21 交付**：新增 `lib/content-dump.js`（纯逻辑：keyset 分页 / gzip 分片 / sha256 清单 / 校验 / 删除闸 / 回放语句）+ `tools/dump-content.cjs`（`--full`、增量、`--verify`、`--gate`、`--restore --into`、`--mktarget`），命令入口 `npm run dump:content` / `dump:gate`。**一手读数**：云端转储 59,832 文章 + 1,291 视频 = 161.6MB / 154 片，逐片校验通过；本地转储 43,467 + 1,161 = 101.4MB；**真回放演练**（`--restore --mktarget --into data/rehearse/app.db`）17.6s 放回 59,832/1,291 行，抽样 12 行 × 23 列**逐字段 0 不一致**，`--gate` 现报 `allowed:true`。锁 `tests/regression-content-dump.test.js` D1~D10（10/10）。**F2P 不适用**（坑 #64 规则②：新增门禁无对应旧形态，证据 = D2~D6 的突变对照：篡改分片/清单脱钩/id 倒退/空转储/过期都必须红）。**仍欠**：① 删除侧**接线**（runner `runCleanup` 与云端手动端点在 DELETE 前调 `deleteGate`）随 B101 那批做，此刻闸是可用工具而非强制路径；② 云端 `/api/backup` 仍只三张配置表，未改（`api/[...slug].js` 有并行会话在改） |
| B105 | **`tests/regression-my-brief.test.js:82`「2. 有订阅 + settings 报告 → 正常透传」只在 `npm test` 全量套件里红**：驱动子进程以 `status: 3221225477`（`0xC0000005` 访问违例）**在退出阶段崩溃**，而断言要看的载荷已经正确打出来（stdout = `BODY {"ok":true,"report":{"date":"2026-09-12","theme":"测试导语",…}}`）。单独连跑该文件 **3/3 全绿**（每次 `EXIT=0`）。**三轮全量复跑定性完成**：run1 红 / run2 绿 / run3 绿 ⇒ 判 **`fail_flaky`**（1/3 复现，非产品红；`npm test` 的恒红只由 G3 造成）。方向：测试自建的临时库/根目录 scratch 驱动（`.mybrief-driver-<pid>.cjs`）在全量并跑下的句柄/文件竞争，属**测试地基**问题 | 测试地基（`fail_flaky`，1/3 复现；**09-21 换文件复发，见右侧**） | 待做（已放行）：把这类用例改成"退出码与断言载荷分别判"（子进程崩溃要能打出 stderr/句柄），并排查与 `regression-20260919g`（同为 execFileSync 驱动）等文件的并发资源冲突。**09-21 文档整理轮第 4 次复现，且换了文件**：`tests/regression-cloud-sources.test.js:241`「10. groups move kind 校验 + categoryLocked」以 `status: 3221225477`（同一个 `0xC0000005`）在退出阶段崩，而 stdout 已正确打出 `OUT {"status":200,"body":{"ok":true},"dbv":{"gid":2,...,"categoryLocked":1}}` —— 驱动文件形态同为**根目录 scratch**（`.cloud-sources-driver-15724.cjs`，与 `.mybrief-driver-<pid>.cjs` 同款）。⇒ 本条从"某一个文件偶发红"升级为**"execFileSync + 根目录临时驱动"这一族测试地基的缺陷**（`npm test` 该轮 462/463，唯一红即此条，读数 `.tmpchk/test-0921-final.txt`） |
| B107 | **噪声（热榜聚合）判定在三端手写 14 处（含 aggregator 变体共 20 处），而"该用它的地方"没用**：`/api/reading`（「我的阅读」）**根本没套噪声口径** → 实测 `type=all` 的 25,142 条里 **17,860 条（71%）来自 hotlist 聚合源**，用户足迹被热搜淹没。落点逐处行号清单见归档原文（同 W10 抓过的"分类判定抄 6 份"同族） | 三端一致性缺口 + 口径未落地（AGENTS §1） | 小 spec 已出：`docs/specs/36-reading-semantics/36-7-source-spread-and-dedup.md`（拟建单实现文件 `noise.js`（落在 `lib/` 下） + 拟白盒 **W18**）。**是否排除噪声属产品语义，需拍板**；放行动作在放行清单归档件 §三 #10 |
| B108 | **`tools/gen-dir-index.cjs` 不做 AGENTS §2.8 承诺的那件事**：§2.8 写"跑它刷新各目录 `README.md` 的自动段"，实测该脚本**只有两种行为**——目录没有 README 时生成骨架（`:109` 原文「已有正文的目录一律不动」）、`--check` 报未登记/悬空（`:95/:99`，且 `process.exitCode=0` 恒按警）。**没有任何"写自动段"的代码路径**，README 里也不存在 BEGIN/END 标记（grep `lib/README.md` 0 命中）；本轮跑完 `--check` **仍报同样 7 处**未登记。**连带**：我此前几轮"已跑 gen-dir-index 刷新索引"这句话的效果被高估了 | 文档承诺 ≠ 工具行为 |  ✅ **09-21 由并行会话按 (b) 收口，且比 (b) 更彻底**：AGENTS 原 §2.8「目录索引要跟着变」整条删除、`tools/doc-lint.cjs` 第 7 条（目录索引待维护）移除、`gen-dir-index.cjs` 与 `audit-graph.cjs` 一起进 `docs/specs/42-full-audit-2026-09/quarantine/`。**代价留账**：目录 README 的"未登记条目/悬空"自此没有任何机器提示，DOC_GOVERNANCE §2.5 的"索引由机器写"退回人治（H17 那族漂移风险回到人身上）。**引用前先复核**：这批改动**尚未提交**（同批还含 AGENTS 新增「文档改动戳」= `tools/doc-stamp.cjs` → `docs/STAMPS.md`，两者也未跟踪）<!-- doc-lint:ignore --> |
| B109 | **本地 `settings.alerts.events` 残留两个"代码里已不存在"的幽灵事件键**（`wemp_down`、`wemp_cookie_expired`，对应事件 09-04 随 we-mp-rss 退役删除，`server/services/alerts.js:14` 注释自陈），配置没跟着清 → 后台渲染出两个永不触发的开关（假开关家族，同 H10/B51）。同时确认事件表是**两份代码 + 两份落库**：云端与本地各 7 键而**键集互不相同**（本地独有 `source_slow`，缺 `ai_failed`/`frozen_digest`/`mybrief`）→ 这是 B46 的精确化 | 配置残留 + 三端事件表分叉 | 小 spec `37-2`（拟建单实现文件 `alert-events.js`（落在 `lib/` 下） + 拟白盒 W19）。**清幽灵键属生产/本地配置写，需点头**；放行动作在放行清单归档件 §三 #10 |
| B110 | **我自己的操作事故（如实登记）**：为核对 37-1 的"本地真值渠道完好"，用 `node -e` 打印本地 `settings.alerts` 渠道对象时，掩码只写在**顶层键**，而真实 webhook 在**嵌套的 `config.url`** 里 → 一条含 token 的飞书 webhook 明文进了会话输出。仓库文件未被写入、云端凭据未受影响（那是**本地库里的渠道真值**），但**明文出现在外部会话记录里就等于暴露**。同一坑 09-12 已在读层修过（`api/[...slug].js:1568` 注释自陈「密钥在 `c.config` 嵌套层，只在顶层打码等于没打」），我在一次性探针里重犯 | 操作事故（不是产品缺陷；产品侧嵌套掩码已修） | **当前建议：是否轮换该 webhook 由你定**（我不擅自碰生产凭据）。待放行后 ①"探针打印 settings 前必须递归掩码"写进 `docs/DELIVERY_VERIFICATION.md` 或探针规范并配坑号（与 `tests/` 锁同批，坑 #61）②共享 `maskDeep()`，禁一次性脚本自写掩码。与 B113/B122 同族 |
| B111 | **同一份"翻译提示词"有 3~4 个事实源**：本地读 `getSetting('ai.prompt.translate')` + 兜底常量；云端走 `prompts/translate.md` **且另带一份"内嵌兜底"**（`api/_ai.js:283` 注释自陈同步义务）；runner `tools/collect-turso.js:1618` 手写常量——而 runner **本来就能 `loadPrompt`**（同文件 `:1709` 术语抽取在用）→ **不是能力缺失，是没收口**。**后果**：`ai.prompt.*` 只有本地读（全仓 2 命中均在 `server/`），后台"编辑 prompt"对云端无效 = 第二个假开关（H10/B51 同族）。⚠️ **撤销一条旧计划**：原 39-6 要清 `autoTranslate` 死键——实测它活着（`translate-skill.js:164/184` 读、`TranslateSkillTab.jsx:83-85` 写），删了就是砸功能 | 三端一致性 + 假开关预防（AGENTS §1/§2） | 小 spec `39-6`（拟建 `ai-prompts.js`（落在 `lib/` 下）+ 拟白盒 **W20**：同义 prompt 字面量 ≥2 份即红；AC1 用"改 settings 后三条链路的 system prompt 都变"的行为锁，不做文本比对）。放行清单归档件 §三 #10 的打包行里 **prompt 这条指本行**（见 B124 ③） |
| B112 | **产物域的三处"纸面数字/结论"被实读推翻**（登记以防再被引用）：①`daily_reports` 现 **98 行、近 7 天 32 行**（40 总 spec 记 93/27）；②**`settings['reading.digest']` 线上已存在**（558 字节，B14 补 `qOne` 后 runner 开始产出）→ 40 背景 §1「阅读足迹永不出现」**不再成立**；③新缺陷：`daily_reports.stats.schemaVersion` **落库类型混杂**（`json_extract` 分组实测 `(无)` 48 / 数字 `2` 19 / 字符串 `"1"` 1，近 14 天）→ "哪一版"目前只靠 `briefGuards.isAiDailyReport()` 兜，写入侧没有统一常量与类型 | 数据一致性 + 文档不实（三处均已按实测订正进 `docs/specs/40`） | 小 spec `40-1` 残余节 + `40-8` S5 定标（写入侧统一为整数常量、三处写入器共用，与 W14 同批钉）；放行动作在放行清单归档件 §三 #10。**自检教训**（留在归档原文）：探针要读对层级——本轮先按归档"期顶层"读 `coverTheme/storylines` 全读成 false，实际正文在 `report.*` 之下，差点误报成"数据缺失" |
| B113 | **一条真实飞书 webhook 明文躺在未跟踪也没被 ignore 的产物文件里**：`docs/eval/audit/schedule.json` 的 `$.dbState.settings[1].value` 存了整条 `settings['alerts']` 原始 JSON（只读比对：与本地库真值 host 相同、token 前 4 位相同、长度 36/37）。风险不是"已泄露到线上"（未跟踪、不在 HEAD），是**一次 `git add docs/` 就连凭据一起提交**，而 `git check-ignore` 实测该路径没被忽略。根因在生成侧：`tools/audit-graph.cjs` 把 `dbState.settings[*].value` 原样落盘，没过递归掩码 | 凭据卫生（B110 同族，从"会话输出"升级到"落盘产物"） | 三件待点头：①`.gitignore` 加 `docs/eval/audit/`（1 行、零代码，放行清单归档件 §三 #14 同批）②落盘改走共享 `maskDeep()` + 白盒"产物出现 `open.feishu.cn/open-apis/bot` 即红" ③该文件我**没删**（它是上一轮审计的证据）。是否轮换 webhook 与 B110 合并决策 <!-- doc-lint:ignore --> |
| B115 | **文档锚点系统性审计（第 4 轮只读复核）：254 条 `file:line` 引用里 5 条已烂，其中 3 条是我本轮自己弄坏的**（删死码后行号上移 / 引用了被自己删掉的那行 / 实现早已搬进别的组件），而 `npm run lint:docs` 对它们**全绿**——悬空判据只看文件在不在，不看行号越界。**顺带挡下一条假警报**：`tests/regression-20260913.test.js:103` 的 `DELETE FROM articles` 看着像"测试在生产删文章"，实测**不成立**（`server/db.js` 是 `better-sqlite3`、不读 `TURSO_*`，且 `require('./helpers')` 早于 require db → 落 `os.tmpdir()` 临时库）；**把读者推向错结论的是 `HANDOVER.md` 那句「⚠️ npm test 会直打生产 Turso」过期警告**。三处坏锚的逐条行号与隔离证据链在归档原文 | 文档不实 + 门禁缺口（坑 #62 的量化补强：烂锚不是自然腐烂，是被自己的修复打断的） | 已做：6 处引用改成**符号锚**、HANDOVER 的过期警告按三段证据链改写，判据与清单落 `docs/eval/citation-audit-20260919.md`。待放行（放行清单归档件 §三 #12 ①）：`doc-lint` 加"行号越界"判据 + 负向自证，**硬前置：判据必须能区分"活锚点"与"作为反例被引用的死锚点"**（本轮 5 处报红只有 1 处真漏，其余 4 处是我把坏锚点当字面串引用；不解决就会天天误报、三天后被人加 ignore 绕过）。另两条：裸文件名要求写全路径、`_cite-audit.cjs` 变常驻命令 `npm run lint:cites` |
| B116 | **总 spec 的"背景"层在按已作废的前提排期**（第 5 轮只读复核）：四域总 spec 是排期入口，但里面若干"实测事实"当夜就被我自己的后续实测推翻，而**只有子 spec 改了、父 spec 没改** → 照父 spec 动工就是按假前提开工。已逐条实抓并在文件内加"作废/收窄表"：`40` 域四条（行数、`reading.digest` 已存在、`subscription.ids` 是**悬空 id 而非空数组**、`schemaVersion` 类型混杂）；`37` 域里 **37-1 的 T4 整项作废**（其"改前红"改由 B83 的隔离断言承担）、本域真剩下的活其实只有 **BL7 恢复**，且 `audit_log` 表**存在**而报警投递实际落在 `settings.alerts.recentLog` → 37-3 的口径应是"两处并存"而不是"表不存在"（**我第一版那句"表不存在"是推断而非实测，已改正**） | 文档不实（父层与子层不同步）+ 排期风险（坑 #45/#59/#62 合流） | 已做：`40`/`37` 两份总 spec 各加**背景作废/收窄表**（AGENTS §2.4：不静默删，只标注 + 指路）。**逐条原始读数在本轮精炼删下的归档原文里**。待放行（放行清单归档件 §三 #12 ③）：`lint:docs` 加"父 spec 背景段的实测断言必须带 B 编号或指路"。**如实说明**：本轮没重读生产，`weekly.archive`、`/api/settings/daily`、早报中心体积、`54 个 ☆` 四条**仍是白天旧值**，文件内已逐条标"未复测" |
| B117 | **测试侧写入面全量清点（静态 49 份 `tests/*.test.js`，第 6 轮只读复核）查出两点：①「B83 已全部搬完」说满了；②有一份测试真的向生产发 `DELETE`**。**先给结论：生产数据从未被测试写过或删过**（写 SQL 0 处、`settings` 写回 0 处）。5 份"碰云端层却没指 `file:` 临时库"里人工排除 2 份，**剩 3 份**仍绑生产 Turso：两份只 `GET`（其中一份是 `POST /api/data/cleanup/preview`——实测体内 0 个 `DELETE FROM`/0 个 `setSetting`，但用写方法表达只读），**第三份 `tests/regression-20260918` 是 `DELETE /api/weekly/archive/999999` 且带 `.env` 里的真 Bearer token**。它没造成删除**只因为** `handleWeeklyArchiveDelete` 的顺序是"读归档 → filter → 长度没变就先 `return 404`"，`setSetting` 在其后 → **安全性押在"999999 这个期号恰好不存在"的夹具选择上，而不是押在隔离上**，而它换来的信息只有"路由可达" | 测试地基 + 潜在生产写入口（当前无实害，**但这是"下一次 BL7"的形状**） | `FEATURE_MATRIX` §1.5 的 B83 那句已按实测改"写路径类已搬完，仍有 3 份绑生产"。待放行三件（详 `docs/specs/43-collect-retention-safety/spec.md` §六、放行清单归档件 §三 #13）：①`20260918` 改隔离库正负两条断言 + 补"子进程必须被指到本地文件库"断言 ②`20260913b` 的 preview 同批搬 ③**新判据**：测试里出现 `DELETE`/`POST` 却未设 `TURSO_DATABASE_URL='file:'` 即红。另需单独拍板：preview 是否改 `GET`/显式 `?execute=0` ✅ **09-21 三件全落地**（"preview 改 GET"随 §四 冻结，未动）：①②两份写方法测试搬到本地 libsql 文件库并**升级成正负两条行为断言** → 新文件 `tests/regression-cloud-writes-isolated.test.js` W-1~W-6（归档删除：存在→200 回 `removed` 且清单真少一期 + 留 1 条审计行；不存在→404 且清单一字未动 + 0 条审计行；删最新期→`weekly.latest` 退位、删非最新期→latest 不许动；`GET /api/weekly` 读侧不破；cleanup 在隔离库里 preview 数出 1 条**且真跑 cleanup 后存活的正是那 4 条豁免/窗口内的**；最后一条自证驱动真的指在 `file:` 且 `TURSO_AUTH_TOKEN=''`），旧两份里的写方法用例已删并留指路注释；③新判据落地为白盒 **W23**（本体 `lib/test-isolation.js#findWriteWithoutIsolation`，锁 `tests/regression-test-isolation.test.js` T1~T7：坏形态必须红 + 三种合法形态必须不红 + 两份被误伤的静态形状锁必须 `no-cloud` + 注释里的 POST 不算 + 摘掉两处安全标记后本文件必须翻红）。**判据第一版就把 `regression-20260919e`/`regression-bc` 两份纯静态扫描判成红**（它们只是在字符串里提到 `api/[...slug].js` 与 `'POST'`），已按 spec43 §六人工复核的口径改成"真 require 读层或自建 `createClient`"两条形态。**已知空洞**：W23 是整文件粒度（同文件出现过一次 `file:` 就整份放过），与 W17 对 `api/[...slug].js` 的豁免同族。B83 那句"仍有 3 份绑生产"随之更新：写方法 0 份，只读那 16 份按 §六 属可接受取证方式 |
| B118 | 🔴 **09-20 Turso 整库读封锁 → 线上 `/api/*` 全 500**；按用户决定换新建库止血，读侧与写侧都已实测复验（`/api/meta` commit==origin/main、run `1073` 采集步 success、新库 articles 41,254→57,344）。**代价**：新库 `settings` 来自本地灾备副本，云端独有的 `weekly.*` 归档当时读不到（旧库不许删，配额解除后可回读） | 外部配额 + 可观测性缺口 + API 层无降级路径（P0，已止血） | 取证与操作口径各留一处单源：`docs/HANDOVER.md` §2.1「换库记录」· `docs/RUNBOOK.md` §10.9 · `docs/CLOUD_PIPELINE_GUIDE.md` 不变量 19。**待你**：轮换旧 `TURSO_AUTH_TOKEN`（与 B122/B125 一并） |
| B119 | 🟠 **换库后四个"能打开但没内容"**：①日报候选被 `settings.daily.articleSourceIds` 限到 87 源（同窗口可用 1,038 篇，只放行 63）；②`wemp` 893 篇、最新一篇停在 09-04（两份云端实现各有 `UNSUPPORTED_TYPES` 且都排除 wemp，属结构性、非本次故障）；③窗口内 `videos` 0 条；④全仓无 Turso 平台 API token → **配额不可观测**，按旧库 ≈38M 读/日折算，新库十余天内会再撞同一堵墙 | 内容面缺口 + 可观测性缺口（④是 P0 级"会不会再挂"） | **09-20 夜进度**：① **已做**（用户放行）—— 直写 `settings.daily.articleSourceIds = []`（空=不限制，语义见 `tools/collect-turso.js` 里 `if (selectedIds && selectedIds.length)` 那一支），**写前备份 / 写后回读 / 其余键逐键比对未变**，spotlight 53→53 未动。**故意不走 `PUT /api/settings/daily`**：该路径的 spotlight 全量替换没有 WHERE 作用域（B34），提交空列表会先把 53 个重点源清零。一手效果读数：**26h 窗口内候选 82 条/9 源 → 1,939 条/308 源**；可见面等今晚 21:30 的 daily-ai 出报后复验（不手动 dispatch，避免把免费 AI 池打两遍）。② **仍待你**：给一个 Turso 平台 token，否则配额不可观测 = 十余天内再撞同一堵墙（这条我这边造不出来）。③ **仍待你**：公众号要恢复得先选"本地端回灌云端"还是"云端可采"（AGENTS §1 三端语义，属方案级）；视频与播客实测正常（库里 1,290 条、近 7 天 54 条、`/api/videos` 返回 30 条全带封面），原记"窗口内 0 条"是候选池被限的连带症状，随 ① 一并解。 |
| B121 | 🟠 **周刊"深析成功、骨架全丢、还报正常"** —— 端到端 E6 判 `fail_product` 3/3。run `1075` 日志两行：`本周总结放弃：1 行清洗后仅剩 6 字（<60）`、`杂志结构放弃：主线调用失败：agnes 仅含 reasoning 无 content`。落库 `weekly.latest` 20 条深析条目齐全，但 `theme=null`、`coverTheme`/`editorNote`/`storylines` 三键不存在，而 `degraded=false` | AI 产物契约 + 健壮性 + 期号语义（P1） | ✅ **09-21 三条全落地**（判定收进 `lib/brief-guards.js` 一份，runner 只调用）：①`weeklySpine()` 判"导语 / 封面主题词 / 主线≥3 条"三件，缺任一 → `spineMissing=true` **并同时置 `degraded`**（状态灯与 E6 同源），`web/src/pages/WeeklyPage.jsx` 的降级文案区分"AI 不可用"与"骨架不全·缺哪件"；②`generateWeeklyMagazine` 失败退避 15s 重试一次（`agnes 仅含 reasoning 无 content` 是 B19/W6 已知形态）；③`resolveWeeklyIssue()` 按**内容窗口**定期号 —— 同窗口重跑原地替换、不另算新期；新窗口取最大期号+1（旧算法 `archive.length+1` 删过一期就会串号并覆盖历史期）。锁：`tests/regression-brief-guards.test.js` 三条 W-*，含"三件齐不许判缺"与"刚够下界不许判缺"的反向对照。**E6 仍会红到下一次 weekly 跑批**：线上现存那期的 report 里没有 `spineMissing` 键，属历史产物，不回灌改数据；用户 09-21 开目标手动跑批后按 B120 的形态指纹复验（认 theme/coverTheme/storylines 真出现，不看"最新一行"）。**E6 在此之前一直红，不许为绿灯放宽判据**。**09-20 夜 B104 轮再复跑：E6 仍 3/3 红，读数 `items=20 storylines=0`**，证据 `docs/eval/e2e/20260920T112415/`（该轮 `acceptance.ok=true`、其余 9 剧本全绿、真退 1） |
| B122 | 🔴 **凭据卫生：真实 `AGNES_API_KEY` 明文进过本会话输出一次**（我自己一条未掩码的 `SELECT key,value FROM settings`）。**未进任何提交**：每次提交前逐文件过明文凭据扫描（0 命中），`.tmpchk/` 已整目录 ignore | 凭据暴露（外部面） | **要你做**：在 Agnes 控制台轮换，三处同步（`.env` + Vercel env + GH Secrets，AGENTS §2.6）。我可做（等点头）：把"读 `settings`/`credentials` 的探针强制走递归掩码"做成门禁判据 —— 现在全靠我自觉，而我已经破了一次 |
| B123 | 🟠 **早报页头写着「关键词规则排序」，而线上这一期是 AI 策展版**。那串字是 `web/src/components/DailyHeader.jsx:12` 里**无条件拼进 meta 的**，组件从不读 `schemaVersion`/`degraded` | 前端文案不实（展示级 P2，与 B119 同族"页面说的与产物真实档位不一致"） | 修法一行（按 `schemaVersion===2` 出「AI 策展排序」、`degraded` 再标「降级」+ 静态锁与前端负向样本）。本轮没动 `web/`：它要过 `build:vercel`，而该命令本轮与并行会话争抢 `web/dist`。可与 B119① 同批做 |
| B124 | 🟠 **门禁编号自身不可定位**（洁净轮按 §4.1 复查抓到）：①`eval-whitebox` 实装 `W1~W4/W6/W7/W9~W16`，**`W5`、`W8` 从未实现**却写"观察 W1~W16"；②文档里的 `W17~W21` 全是**拟建号**、与实存号无排版区分；③放行清单第 10 行把 7 个 B 号打包成一行不给映射 —— 我照它猜就把 prompt 那条猜成 B107/B112（实为 **B111**） | 文档精度（排期与门禁可读性）= §4.1 说的"隐式依赖" | 已做：本行摆明事实 + `prompts/README.md` 出处按实测改回 B111。待放行（与 #12 同批）：拟号一律写 `W17(拟)`；打包行拆成一行一号；`W5/W8` 在 `docs/EVAL_GUIDE.md` §4.2 登记为"已废弃、号位不复用" |
| B125 | 🔴 **P0 凭据卫生：管理台默认口令硬编码在两份源码的兜底默认值分支**（`server/middleware/auth.js:101`、`api/[...slug].js:1390`），另有 8 处明文在跟踪文件里（`ARCHITECTURE.md:70`、`docs/deprecated/` 两处、`.cluster/test-out*.txt` 五份日志）。**关键**：`.env` 的 `ADMIN_PASSWORD` 值 == 源码里那个兜底默认常量 → 线上用的就是仓库里读得到的口令；自 `2e81cd7`（09-08）起在 `origin/main`。§5 第 6 条早登记过这个正则盲区（B80/AU-05），所以门禁一直绿 | 凭据管理（P0，鉴权面） | **要你决定，我没擅动鉴权代码**（改兜底默认值会改变登录语义）。顺序：①换口令 + 三处同步 → ②两处改成"缺 `ADMIN_PASSWORD` 即拒绝登录并发告警"，配"缺 env 必须 401"的回归锁 → ③再掩码当前 tip 的 10 处明文（历史删不掉，先掩码=假清洁）→ ④是否重写历史（force-push）你定。另记我一次流程失效：扫描报了命中我仍提交了（`scan && commit` 串着跑）——**扫描已改成闸门：命中即 exit 1** |
| B126 | 🟠 **markdown 表格被内容里的裸竖线静默破格**（本轮 §4.1 精炼时逐行数格才抓到，已就地改掉两处）：①放行清单第 14 行被写成 **5 格**而表头只有 4 列 → 渲染时多出一列，读者看到的"等什么/代价/第一步"整行错位（正是你 09-20 指出的"图3 那几行"）；②本表 B125 行早期把「逻辑或」的两个竖线直接放进单元里 → 同样破格。**正确写法是反斜杠转义**（本文件 B76 那行 `data?.empty` 的兜底表达式就是这么写的，转义后照常渲染成一个竖线）。`npm run lint:docs` 对两者**全绿**：它查路径、头注、密钥、超长，**不查每行格数** | 门禁缺口（渲染层）；定性同 §4.1 说的"隐式依赖"——错位后没人看得出某段话属于哪一列 | 已做：两行都改符合表头列数。待放行（并入放行清单归档件 §三 #12，成为第④条判据）：`doc-lint` 加"同一表格内各行格数必须等于表头"，配负向自证（多塞一格必须变红）+ 反向样本（转义竖线不得误判为多格） |
| B127 | **F1~F8 的轮内读数不落进产物，事后无法复核**（09-20 夜 B104 轮实测）：`tools/eval-e2e.cjs` 只在**判红时**打印 `✗ 过程检查 …`，全绿时既不出行也不写进 `report.json`（该文件只有 `summary/cases/commands`，无 checks 字段）→ 想事后证明"F1~F8 本轮过了"，只能翻当时的控制台输出。而我按 `--report` 的用法拿本轮 e2e 产物试复核，**造出两条假红**：`check_screenshot_taken`（e2e 的图在同级 `screens/`，report 里没有 screenshot 事件）、`check_report_generated`（它要的是 `docs/eval/runs/<ts>.json` 那套 schema）——`--report` 与 e2e 产物**不是同一套输入格式**，拿错 schema 的红灯会误导成"评测产物不诚实" | 可复核性缺口（与 B114"结论与证据脱钩"同族）+ 判据输入面未对齐 | 两条动作（同属判据变更，随放行清单归档件 §三 #12 一并批）：①e2e 把 `CHECKS` 的逐条结果写进 `report.json.checks`，让"过程层过了"成为产物里的事实；②`eval-process-checks --report` 开头先校验输入 schema（缺 `commands`/多 `screens` 一律退 2 并说"喂错了文件"，不许按 fail_env 混判）。本轮交付说明里 F1~F8 的口径如实写：**轮内执行、控制台零失败行 = 过了，但产物无落账** |
| B128 | **零引用资产三处（死代码候选，只出清单不删）**：①`lib/collectors/{fetcher,repo}.js` 161 行——自述「Serverless 兼容版本，对应 `server/services/collectors/fetcher.js`」，全仓 **require 数 0**（本轮实测：`.cluster/` 与 `server/` 引的都是 `server/services/collectors/*` 那一份），却仍被 `tools/eval-whitebox.cjs:39` 当 W1 的"三端"第四份比对 → 一份永不运行的代码能单独制造假红（= 冻结审计轮的 AU-01/AU-02，处置权已收回正常开发，见 B81）；②`web/src/snapshot.js` 117 行——除自己第 8 行的注释示例以外**无任何 import**，它是 portal 时代的迁移期残留，而 portal 项目已下线并删除；③`docs/specs/42-full-audit-2026-09/quarantine/` 内 13 份 `README.md` 与被隔离工具的引用关系（10 条悬空警即由此来，属隔离目录的正常形态，不是缺陷） | AU-4 级死代码与冗余资产；**删除候选，未删** | 随放行清单归档件 §三 #10 打包批次：①②一起裁决"复活 or 摘掉 + 删"，摘 `lib/collectors/fetcher.js` 必须与 W1 清单同批改（否则 W1 少一份比对=判据面缩小），并配 F2P 改前红；本轮**不自行删**，也不得只删文件留 W1 悬空 |
| B128 | 🟠 **`lib/db.js` 的 `SCHEMA`（全新建库路径）里 `articles` 少三列**：`translated_title` / `translated_content` / `translation_provider` 只存在于 `ALTERS` 与"活着的本地库"（实测本地 `data/app.db` 22 列且**有** `translated_title`），生产 Turso 则是 23 列。所以本轮第一次拿真转储做回放演练时，被逐列校验**正确挡下**：`目标库 articles 缺列：translated_title, translated_content, translation_provider`。后果不止回放——**任何一台新机按 `ensureSchema()` 建库，AI 译文写入就会抛 `no such column`**（B18 说的 videos 缺列是同一族的另一半） | schema 漂移（本地建库路径 vs 生产实况；AGENTS §1 三端一致性的数据面） | 已绕过而不是修：转储清单改为自带源库 `CREATE TABLE` 语句，回放走 `--mktarget`（锁 D10）。**真修法待做**：三列并进 `SCHEMA` 本体 + 一条白盒"生产列集 == `ensureSchema` 列集"判据（属门禁扩面批，与放行清单 §三 #12 同批） |
| B129 | 🟠 **第四份删除路径不在 `lib/retention.js` 的管辖内**：`tools/archive-articles.js`（`npm run archive`）把 >90 天文章 `INSERT OR IGNORE INTO articles_archive` 后 `DELETE FROM articles WHERE id IN (...) AND read_at IS NULL AND later=0`。W17 的派生判据按"时间列 `< ?`"认保留谓词，而它的时间过滤在前一条 SELECT 里、DELETE 只剩 `id IN (...)` → 被判成"不是保留策略形状"而放过（**不是判据写错，是这条路径当时没进视野**）。且它少一条豁免：`featured` 未参与判定，与 `lib/retention.js` 的 `ARTICLE_DELETE_COND` 不同口径。实测现状：云端 `articles_archive` **0 行**、本地无该表 → 该工具从未真跑过，属休眠弹药 | 三端一致性缺口（W17 覆盖面的洞）+ 豁免口径分叉 | **待做**：让它改读 `lib/retention.js` 同一份条件，并把 `articles_archive` 纳入转储面（`DUMP_TABLES` 当前只有 articles/videos，归档表同样"删了就回不来"）。登记不顺手改：本轮 B103 只交付转储与闸 |
| B130 | 🟠 **`#64-1` 那条"锁文件形态"判据自己会误伤样本**（本轮新锁第一次全量复跑当场抓到，0 红之外的唯一红）：`tests/regression-eval-substrate.test.js` 的 `#64-1` 那条判据里，"**顶层**读 `.env`"是用**行首缩进**近似的（`code.split('\n').filter(l => !/^\s/.test(l))` 之后再正则找 `readFileSync(... .env`），于是我把历史事故形态作为**反例文本**写在模板字符串里、且那一行恰好顶格，就被算成"这份锁在顶层读 .env"；同批还判我 `require('../lib/test-isolation')` 违规 —— 第二半句是对的（已照口径改惰性取），**第一半句是判据缺陷**：反例文本正是这条锁要教后来的锁写的内容，被自己的门禁当违规 → 结果只能是"以后别在锁里放坏样本"，而那恰好是坑 #45/#62 禁止的方向 | 门禁判据缺陷（假红，方向危险：会把"配负向样本"这项纪律逼退回注释里） | 待放行后做（属判据变更，与 §三 #12 门禁扩面同批）：`#64-1` 的两条子判据都改走 `lib/src-spans` 的**代码跨度**视图（注释与字符串内容都剥掉，"顶层"由 AST 式的深度判定而不是数缩进），并补两条负向样本：①样本字符串里出现 `readFileSync('.env')` 必须不红 ②顶层真写这句必须红。本轮只做绕行（把样本里那一句删掉，不影响 T6 的判定力：`CLOUD_REQUIRE` + 写方法两条都在） |
| — | （排除项，记此防复查）**云端删除端点有鉴权**：无 token `POST /api/data/cleanup/preview` 实测 `HTTP=401`；`requireAuth`（`api/[...slug].js:103-119`）在 `dispatch` 前统一执行（`:2919`），写方法不在 `PUBLIC_GET_PATHS` 豁免内 → 不存在"公网可触发删库"的口子 | 已排除 | — |



### B8~B26 本轮处置（已归档）

> 逐条处置与取证表外迁：`docs/archive/debugging/2026-09-19-delivery-evidence-ledger.md`。要点仍生效的部分已在各条 B 编号行内保留（如 B85/B84 残余见 `docs/ISSUES.md` 观察中）。

## 🗂 六域 32 个缺陷（B27~B70）已收进各自域 spec

> 根因与改法的唯一去处：`docs/specs/36-reading-semantics/`（B27/B28/B30~B32/B60~B62）· `38-admin-ia-refactor/`（B33~B38）· `39-ai-console/`（B49~B53）· `40-brief-center-products/`（B39~B43/B68）· `37-alerts-observability/`（B44~B48）· 域 F 小刺（B54~B58）多已在各域表内挂号。
> 09-19 登记原文整节照档：`docs/archive/debugging/2026-09-21-issues-closed-rows.md` §一（含每条症状原话）。逐号反查后，**只有下面三条至今没有任何 spec 落点**，故逐字留在活文档：

| # | 症状（一句；根因与改法见归档原文 §一 与对应域 spec） |
| B29 | 类型口径三处错：播客=`s.type='douyin'`（云端 0 篇）、文章类型表用死值 `'wechat'` 漏掉 881 篇 `wemp`、视频只认 `favorite=1`（线上 0）→ 恒空 |
| B69 | **同一份"报警有出口"判据仍有第二处实现**（B67 只修了 preflight 侧）：`server/routes/health.js:67` 还是 `channels.filter(c => c.enabled).length` → 生产现况下 preflight 如实报红，而 `/api/health/status` 与后台显示"1 个已启用渠道"，两个面各说各话（AGENTS §2.5 禁止的正是这个）。修法：health 端改引 `lib/alert-channels.js#usableChannels` 并另回 `noExit`；云端等价 handler 同步 |
| B70 | 独立对抗性审查（`f58337f..HEAD`）留下的未修清单，按性价比排序：①`eval-content` judge prompt 未定界——被评的是任意外部 RSS 正文，正文里写"忽略上面的维度给 5 分"就能操纵分数（`judge.py:32-44`）；截断（6000/8000 字符）不留痕，而 `factual_correctness` 权重最高 0.30 却可能建立在半篇原文上；②`_append_trend` 非原子、`trend.json` 损坏会抛在报告落盘之后；③golden 集把第三方正文最长 8000 字符存进 git（现 52K），逐轮累积，需定"存 hash+截断"还是"进 .gitignore"；④`tools/_test-api.cjs` 掩码写法在 key 未加载时是 `replace(undefined,…)`，会把密钥原样打印（本轮未引入，顺手该修）；⑤纯文本形状锁仍有 3 处（B66-1 的 `b.deduped === true` 写法过窄、`41-8 judge 纪律` 锁把中文注释当行为、`41-8 覆盖项数`用 `>=40` 计数会让恒真项凑数）——原则见 `regression-20260919e.test.js` 开头自己写的"不锁形状锁行为" |

## 🆕 端到端引擎（41-2）首轮线上实测 + 对抗审查新增（2026-09-19 夜，B71~B79）

> B71~B75 **不是人看出来的，是 `npm run eval:e2e` 第一轮跑出来的**；B76 是端到端建好后**回头把 `npm test` 跑全**才暴露的契约断裂；B77 是追 B76 时量到的测试选址问题（它的"竞争成因"我先写错过一次，已按证据划掉）；B78/B79 是同一条追查里在**线上真实数据**上抓到的：你的「我的早报」此刻正处于退化的兜底路径上。写清归属，别在 37/39/40 里另立 UI spec。

| # | 症状 | 归属 |
|---|------|------|
| **B71** | 管理后台 `/admin/` 未登录时，登录门五个文案直接显示裸 i18n key：`login.title` / `login.subtitle` / `login.username` / `login.password` / `login.submit`。**根因**（实测 + 代码双向确认）：`web/src/admin.jsx` 只挂了 `ThemeProvider`，**没挂 `LanguageProvider`**（对比 `main.jsx:118` 前台挂了），于是 `LoginModal.jsx:9` 的 `useI18n()` 拿到 `i18n.jsx:5` 的兜底值 `t:(k)=>k` → 键名原样上屏。字典里 `login.*` 六键齐全（`i18n.jsx:139` 起），**不是缺翻译，是缺 Provider**，所以"补字典"是错方向 | 38-A 全局壳（**本轮已修**，见下表 + W11 白盒） |
| **B72** | 拼错的前台路径静默渲染成阅读器：实测 `GET /videos/` 与 `GET /reader/` 渲染结果一致（页面里有「加载更多/稍后阅读」），`vercel.json:11` 的 catch-all 重写把一切非 `/api/`、非 `/data/` 路径都指向 `index.html`，而 `main.jsx:108-113` 的路由分支对未知 path 一律落到阅读器兜底 → **用户打错字看到的是"正常但不对"的页面**，无任何提示 | 38-A 全局壳（导航与路由可寻址性，与 38-3 深链同批做）。**✅ 09-19 已修**（NotFoundPage + E10 回门禁位，证据见「第三批」） |
| **B73** | `/api/reading?tab=all&type=article` 端到端实测**响应可达 26s**（同参数直连 curl 约 5s，点击后的请求被排在首屏那条 type=all 的慢请求之后）→ 表现是"点了筛选像没反应"，随后一次性跳出。属 B31/B26 慢性超时族在**交互路径**上的表现：接口预算 2s（§3.5），实测超一个数量级 | 36 域（阅读器/阅读页）+ 35 自愈观测；与 B31 同批治理 |
| **B74** | **前端缺"晚到响应"守卫（本轮端到端实测抓到并已修）**：`MyReadingPage.fetchPage` 原来只有 `if (loadingRef.current) return` 一道闸——①首屏那条 `type=all` 慢请求（实测 15~30s）晚于用户筛选的快响应到达时，会把列表与计数**整体覆盖回未筛选态**（实测证据：DOM 显示「全部 25151」而页面自己拿到的 `type=article` 响应是 `counts.all=7291`）；②加载中点的筛选会被整段吞掉（点了没反应）。修法：请求领序号 `seqRef`，成功/失败两条路径落地前都比对"我还是最新那次吗"，并且**只有翻页才防重入**（`if (!reset && loadingRef.current) return`） | 36 域（本轮已修，回归锁 G9 + 端到端 E5 行为证据） |
| **B75** | 后台登录门的裸 key 不止 `login.submit`：实测同一屏还有 `login.title` / `login.subtitle` / `login.username` / `login.password`（B71 的同一根因，一并计入 B71 的修复面，不另立条目） | 38-A（随 B71 已修） |
| **B76** | **mybrief 空态有两种等价存储形态，API 三态契约在读层断裂（本轮已修）**：runner 会把空态**当成报告写进 settings 行**——无订阅写 `{empty:'no-subscription'}`、今日无精选写 `{empty:'no-content',message}`（`tools/collect-turso.js:1308/:1372`）；而读层 `handleMyBrief` 只判 `if (!report)`，于是响应变成 `{report:{empty:'no-content'}}`。前台那行 `data?.empty` 兜底一直在**替后端补这个二义性**，只看 `data.empty` 的客户端（门户、飞书推送、脚本）会把"今日无更新"读成"有内容"。**取证**：本地文件库改前红复现（读数在归档原文）；回归锁 `tests/regression-20260919h.test.js` H1~H5，其中 H3/H4 保证不误伤真报告与"行缺失"那一支 | 40 域（`mybrief.latest` 单键覆盖写正是根因；子项表见 `docs/specs/40-brief-center-products/spec.md`）；**它不是** `regression-my-brief#3` 那两红的成因（见 B77 的"因果没查实"） |
| **B77** | `tests/regression-my-brief.test.js` 第 2/3 条**直打生产 Turso 的 `settings`**，并靠 30~50s 轮询等读层 30s 缓存过期做断言。**本轮实测**：这条连红两次（`d.empty` 为 undefined），B76 归一后又绿一次——**因果没有查实**。写在这里的是**已被证据排除的假设**：~~"runner 批次在窗口里重写了 `mybrief.latest`"~~ —— 拉了 05:45~07:30 共七批 collect 的 job 清单，`mybrief` job **七批全部 skipped**、`collect` 日志里 mybrief 相关 0 行，时间线根本不成立（我一度把这条写成结论，属于坑 #50 的"判据来自猜"复发）。剩下能确认的只有"50s 窗口内读层没给出预期形状"这一件事。**并发佐证**：审计轮 Agent 在同一时段的 handoff 里独立写到"这条在全量顺序下偶发红、单跑 4/4 绿"（`docs/specs/42-full-audit-2026-09/handoff-notice.md` §2.2），而**我这边单跑两次都是红的**——两种观察都真，说明它依赖外部时序而不是用例顺序，正是"测生产共享状态"的典型症状。改法不变：整组按 H 组做法挪到本地 libsql 文件库，零生产写入。**本轮已完成 my-brief 这一份**（`tests/regression-my-brief.test.js` 全量重写为"本地文件库 + 每条用例起子进程"，6/6 绿且不再需要 90s 轮询；文件里第 6 条是自证：不读 `.env`、不给 `createClient` 传真 authToken） | 41-3/测试基础设施；与坑 #27 同族；**同类面见 B83** |
| **B83** | **"测试直打生产 Turso"不是一个文件的问题，是 7 个**：实测除 my-brief 外，`regression-ai-infra / bilibili / cloud-alerts / cloud-settings / cloud-sources / translate / weekly` 各自都用 `process.env.TURSO_DATABASE_URL` + 真凭据建客户端，并含 **5~8 条 INSERT/DELETE/UPDATE** → 每跑一次 `npm test` 就往生产数据层写几十次（坑 #27 的"全量替换语义"事故正是这一族，B78 那个悬空 `subscription.ids` 也是它的产物）。迁移模板已就位（`regression-my-brief`、`regression-20260919h`）。**本轮已迁完 4/7**：my-brief 6/6、weekly 8/8、cloud-settings 11/11（含 spotlight 全量替换语义）、cloud-alerts 8/8（旧版因生产已污染而**整批 skip** 的第 4/6/7 条现在真跑，skip ≠ pass），三份带自证条。**剩余 3 份仍绑生产，见 B117**（本条此前"全部搬完"的说法由 B117 更正） | 41-3/测试基础设施（AU-1 同族：数据正在被测试改写） |
| **B78** | **一个悬空的订阅 id 就能把「我的早报」整页打死**（线上现况，实测）：`GET /api/mybrief` 回 `{empty:'no-subscription'}`，直连库查到 `settings.subscription.ids = [2104]` 而 `sources` 里 **0 行**——2104 是某次 `regression-my-brief` 建的测试源，源行被 `after()` 删掉，settings 里留下悬空引用。根因在 `lib/source-axes.js#resolveSubscriptionIds`：兜底 spotlight 只挂在"键缺失/空数组"两支，**"声明了 id 但全部落空"直接返回空集** → 上层判成未订阅（与 09-17 已定口径同族，本该覆盖）。**本轮已修**（改共享实现，读层/本地端/runner 三端同时生效）；锁 H6 悬空→兜底、H7 部分有效**不许**兜底（防吞掉真实选择，改前也绿属预期 P2P）、H8 读层行为，H6/H8 都做过"退回旧实现必红"的反向验证 | 40 域（我的早报的输入 = 订阅轴集合；子项表见 `docs/specs/40-brief-center-products/spec.md`）；数据面遗留见 B79 |
| **B79** | **生产数据修复等你点头（本轮没替你写）**：B78 的代码修完，页面会从"还没有订阅"变成"按 53 个星标源兜底出早报"，但 `subscription.ids=[2104]` 这条悬空值仍然错——你真正的订阅集合已不可从库里恢复（`axes.migrated` 闸一次性把 focus=1 迁进来过，现存 `focus=1` 有 8 条）。两条路：①你在后台重新勾选（安全可核对）；②我按 `SELECT id FROM sources WHERE COALESCE(focus,0)=1` 的 8 条写回，**这是生产写，按 2026-09-19「先不写生产，等评测就位」的裁决要先得到你明确同意**，且写前先把原值备份进 `docs/eval/` | 运维/数据；与 BL7/BL8 同一批"等授权"的生产写 |
| **B80** | **`.env.example` 里有一条非占位口令，且已进 git 历史**（审计轮 handoff 提出，本轮独立实测复核）：该文件共 4 个键，其中 `ADMIN_USER`/`PORT` 与本地 `.env` 逐字相同（正常），**`ADMIN_PASSWORD` 是一个 23 字符、不匹配任何占位式前缀的值，与本地真值不同**（所以它是"另一个"口令，不是抄漏的占位符），随 `6be2cf8` 提交进了 `origin/main`。`tools/doc-lint.cjs` 的明文扫描只扫文档，抓不到这种被跟踪的 example 文件。**未做**：没有拿它与 Vercel 环境变量比对（读生产 secret 本身就是越界），所以**不能断言"线上口令已泄露"**，只能说"这条值不该出现在仓库里，且一旦它曾是部署口令就必须轮换" | 安全/凭据卫生。**09-21 拆分**：**轮换仍活**（与 B122/B125 一并，你侧动作）；**"重写 git 历史"与"扩 `doc-lint` 扫 `.env.example` 非占位 secrets 键"两条随被否决的审计轮冻结，不再提问** → 见 `docs/archive/debugging/2026-09-21-release-approval-ledger.md` §四。**留一句真话**：这两条即使做，也不会让历史里的值消失——只能靠轮换失效 |
| **B82** | **端到端 E8 的"不白屏"判据漏了产物页的第二合法态**（验收轮实测 flaky：`/mybrief/` 第 3 轮只有 218 字 → 连跑 pass/pass/fail_product，退 2）。红的原因不是页面坏：那 218 字正是 B76 归一后页面正常落到「今天订阅源没有新的精选内容」的空态（runner 当批写出的就是空态）。判据写成 `>500 字` 一条腿，等于**把合法空态当缺陷报** | 41 域（评测判据）；已改为"要么 >500 字内容、要么命中显式空态文案"，白屏/半空壳两条都不满足仍然红；实测 `--only E8 --repeat 3` 三轮全过（坑 #53 第⑤条） |
| **B84** | **早报媒体栏的播客卡没有封面（用户 09-19 批注「没有图标」）**：`lib/media.js` 的口径是"cover 列命中音频特征 → `audio_url=cover`、`cover=null`，**由源头像/正文图兜底展示**"，`tools/collect-turso.js:1234/1442` 也按同一形状写。但 `MyBriefPage.jsx` 的 `MediaRow` 在 `cover` 为空时**只画了一个 56×103 的灰色块 + 🎧**（实测批注元素 bounds 正是 w-14 的兜底分支 219-221 行），注释里承诺的"源头像兜底"根本没实现。修法：无 cover 时用来源头像（页面已加载 sources，`SourceAvatar` 组件现成）铺满该槽位，播放徽章叠在上面；🎧/▶ 这类 emoji 字形在 Windows 上是单色描边，也要换成内联 SVG | 40 域（三报产物）+ 38-A 全局壳（图标体系）；与 B85 同批做。**✅ 09-19 已修**（源头像兜底 + 内联 SVG；残余：图片分支待下一批生成后复验，见「第三批」） |
| **B85** | **日报页出现一个 996×594 的空心大框，正文只压在框底（用户 09-19 批注「奇怪的大框」）**：批注路径 `main > div > div:nth-of-type(5) > section:nth-of-type(3) > div > div > div`，元素内只有一行（rank 3 + 中英标题 + 标签 + 来源），但容器高度 594px、内容贴在最下方 → **登记时写的猜测（"某个给封面/图占位的固定尺寸或 `items-stretch` 兄弟撑开，图没渲染出来"）已被实测推翻**：真实 DOM 抓到的是——这个框就是 `ColumnSection.jsx#CompactRow` 的一行，来源列无界 620px 把标题列挤到 0 宽，0 宽下 `line-clamp` 不裁剪而按字换行撑高；既没有固定高度也没有丢图的槽位 | 36/40 交界（日报卡片渲染）。**✅ 09-19 已修**：最长行 594px → 129px、标题列 0 → 160px，见「第三批」（教训入 `docs/pitfalls/frontend.md` #54：先抓 computed style 再改，本条正是照这句话做的） |
| **B86** | **本地 Express 的日报根本没有「视频与播客」栏**（09-19 对抗审查查出，本轮实测复核）：`server/services/ai/daily.js:104-115` 只构造 `kind:'video'` 条目，全 `server/` 目录**搜不到一处 `audio_url`**，也没有播客（音频封面特征）判定 → 同一份日报在云端有播客栏、在本地端结构性缺失。B84 修的是"有条目但没图标"，本条是"本地端连条目都不会有"，两件事，别混成一件 | 40 域（三报产物）+ AGENTS §1 三端口径；**本轮已补一半**：`server/services/ai/daily.js` 的视频条目现在也带 `source_avatar`（与云端三份 mediaItems 同口径，本地端不部署到 Vercel，属本地链路，无云端实测面）；**仍缺的是播客条目本身**——要引用 `lib/media.js#audioCoverSql` 才能补齐，属功能补齐不是 bug 复现，**排期待定** |
| **B87** | **B83 搬迁把全仓唯一的 B 站线上探针也一起搬走了**：`regression-bilibili` 原第 4 条打的是真实 B 站接口（可达性 + 返回形状），迁移后改成本地夹具 + `fetch` 桩。方向没错（测试不该写生产库），但代价要说清：**上游 B 站响应字段漂移，现在没有任何自动化会红**——桩是自己写的，与真实协议一起变的可能性为零。AGENTS §2 条 2「必须云端实测」在这一面上目前靠人 | 41-3/测试基础设施；候选做法：把形状探针放进 `tools/audit-cloud.js`（只读、不发库），或端到端加一条"打线上 `/api/sources` 里 B 站源的 status 与 lastError"的观测判据；**未做**，先记账 |
| **B81** | **W1 三端常量 diff 的清单里有第四份实现是"生产零调用"的**：`tools/eval-whitebox.cjs:39` 的 UA 一致性清单含 `lib/collectors/fetcher.js`，而全仓库只有 3 个测试文件 require 它，`server/ api/ tools/ lib/` 里没有任何生产调用方（审计轮可达图实测 + 本轮 grep 复核一致）。后果要判准方向：**它造成的是假红风险（一份永不运行的副本跟生产对不上就红），不是假绿**（多比一份只会更严）。真正的问题是那份副本该复活还是摘掉——属于死代码裁决 | ~~42 域 AU-4~~ → **审计轮 09-21 已冻结，处置权收回正常开发**（用户：被否决那次会话的产物冻结、索引不引用，"正常开发就行"）。本条与 **B128** 同批裁决：要么复活那份副本并给它生产调用方，要么从 `tools/eval-whitebox.cjs:39` 的清单里摘掉 + 删文件——**摘清单与删文件必须同批**，否则 W1 少一份比对 = 判据面缩小。**当前事实（本轮实测）**：`lib/collectors/{fetcher,repo}.js` 的生产 require 数仍为 0，而 W1 清单仍含它 → 一份永不运行的代码能单独把 W1 判红 |

> **一条被撤销的"发现"**：我一度写下「阅读器在移动端断点下文章列表整个不渲染（`hidden lg:block`）」，
> 那是**没实测就写的**。真去 390×844 视口跑了一遍：`div.card.card-lift.p-3.cursor-pointer.relative`
> 命中 30 行、30 行全部可见（`getBoundingClientRect().width > 0`），`ReaderPage.jsx` 里也没有 `hidden lg:block`。
> 该条已删除。留这段话是因为它正是本项目反复踩的"判据来自猜"的最新版——**写进 ISSUES 的每一条都要有取证命令**。

## 👁 观察（只留还没到点的四条）

> 🟡 观察中整节（W1~W16）已照档 `docs/archive/debugging/2026-09-21-issues-closed-rows.md` §二：W1~W5 的观察点是 09-14/09-15 当晚批次、W6/W7/W8/W11/W12 已兑现或已改判据、W13/W14/W15/W16 是那轮的**操作留痕与自我更正**。
> 从那一节抄出来、长期有效的四条规则（规则不该只活在日记里）：①整文件同时红 + 错误是 DNS/连接类 → 按 `fail_env` 重跑一次，**不许**改代码、**不许**放松断言、**不许**记成已修复（W10）；②端到端的红必须先分"判据错 / 读得太早 / 真坏了"三种，直接放宽判据是禁止动作（W12）；③偶发红必须先重定向存下整段输出再复跑，红要带两侧读数（W16 + 坑 #53）；④引用提交与门禁编号以 `tools/eval-whitebox.cjs` 里**真实存在**的号为准，拟建号一律标 `(拟)`（W14 + B124）。
> **仍在观察**：①**B121 / E6** 周刊骨架——代码面三条已落地，但线上现存那期 report 里没有 `spineMissing` 键，E6 会一直红到下一次 weekly 跑批（**不许为绿灯放宽判据**），跑批后按 B120 的形态指纹复验；②**B20** 入报门槛的下一批跑批读数（代码面实测已收口：五个写入点全走 `lib/brief-guards.js#applyDailyQualityGate` 同一份）；③**W9** `regression-ui-data` 偶发红（1/4，已改成显式校验端口）；④**W16** 未定性的 2 红 / 8 次全量，再复现且取到读数即升级为独立 bug。

## 🟢 挂案（外部依赖/低优先，保持跟踪）

| # | 问题 | 状态 |
|---|------|------|
| H1 | YouTube 对数据中心 IP 反爬假 404 → 熔断反复（139 源 41 启用；熔断源由 cleanup 批次自动恢复） | 挂案：彻底解需住宅代理；现状可接受 |
| H2 | 日报引擎双份实现（api/daily-generate.js 仅兜底 vs [...slug].js 内联） | 挂案：主链路在 runner，T3 系重构时收敛 |
| H3 | ~~P2-5 /api/img 无 SSRF 防护~~ **已闭合（2026-09-19）**：新增 `api/_safeimg.js`（与 `server/util/safeimg.js` 同语义：DNS 解析后校验、逐跳校验重定向、只允许 image/*、流式字节上限），云端 `handleImg` 改走它；白盒 W7/B18 双端同语义锁已加。余：P2-4 AUTH_SECRET 回退 'dev-secret' / P2-6 LIKE '%%' 慢查询 / P2-7 handleDaily UTC 日期比较 | 低危挂案（Vercel 网络隔离+量级小）；SSRF 面已收 |
| H6 | B站采集"更好的方案"调研（Cookie 主链 vs 现匿名降级） | 用户提出，待调研 |
| H7 | 云端 /api/articles 无 dedup=1 分支（本地有，pre-existing 漂移）：今日视图成默认落地路径后，双端「合并同事件」行为差异被放大 | 挂案：T5 剩余重构时收敛（2026-09-15 对抗审查记录） |
| H8 | spec30 C3「保存后前台关键数字实时预览」未做（对照卡为生效配置静态摘要，验收②只要求对照卡）；subscription.ids 跨 serverless 实例 30s 缓存窗口（人工点击速度下不可达） | 挂案：后续小 spec（2026-09-15 对抗审查 P3） |
| H9 | **`.gitmodules` 缺失**：`portal` 在索引里是 gitlink（`160000`，pin `1f2f253`）但仓库无 `.gitmodules` → clone 后 portal 为空目录、`git submodule update` 无法解析；Vercel 构建日志已报 `Warning: Failed to fetch one or more git submodules`。另 portal 是**另一个独立 Vercel 项目**（`prj_LTaPscWw…`，名 `portal`）长期落后根树，双站漂移 | 挂案：需决策「补 .gitmodules」还是「停掉 portal 部署/收编为普通目录」（2026-09-18 审查记录） |
| H10 | `settings.ai.features`（translate/summary/classify/analyze）是**假开关**：`AiSettingsTab` 有 4 个复选框 +「x/4 完成度」，`api/_ai.js` 与 `collect-turso.js` 零引用。线上回显 `classify:false/analyze:false` 会被误判成「AI 被关了」 | 挂案：要么接进生成链路，要么从 UI 摘除（2026-09-18 排查中踩到） |
| H11 | `weekly.archive` 每期内嵌完整 report、items 又在 `storylines[].items` 重复一份，`:878` 明确不截断；`handleWeekly` 每次请求全量解析后只投影 5 个字段，而 `[...slug]` 函数预算只有 30s | 挂案：慢性 504 面，需加投影/截断策略 |
| H12 | `settings['weekly']` 在后台可编辑（`/api/settings` merge + 周刊设置卡片），但 `runWeekly` 从不读它——窗口/条数全硬编码（`collect-turso.js:765-766`、`:844`） | 挂案：改了无效，需接配置或撤 UI |
| H13 | 我的早报**没有期号也没有归档**：`runMyBrief` 落库的 `mybrief.latest` 只有 `date/generatedAt/theme/keywords/sections/themes/degraded`，无 `issue`，且全库不存在 `mybrief.archive`（单键覆盖写，历史期直接丢失）。用户 2026-09-18 标注「我的早报也该有期号」。周刊有期号是因为 `saveWeekly` 从 `weekly.archive` 末位 +1 | 挂案：要期号必须先有归档（否则期号会随覆盖写重置）；属功能设计，需单独立项 |
| H14 | **熔断后不靠人点这件事，三端行为完全不同**（2026-09-18 应用户「你去测」做的只读实测）：本地端 `store.js:31-38` 只置 `enabled=0`，**零自动恢复路径**，实测 584 个源锁死 5~7 天（其中 458 个 `lastErrorAt` 全落在 09-13T03 同一小时、错误一律 `fetch failed`＝一次代理故障批量熔断）；云端 `collect-turso.js:697-720` 有自动恢复且真在工作（一轮 cleanup 恢复 66 个），但要等 **48h 起**（第 4 次起 7 天）而熔断只需 **45min**（3 连跪 × 15min）→ 实测 195/210 源"恢复后又坏"、间隔中位 6.2h，`frozenAt` 精确聚集在每天 cleanup 那一小时。抽样 23 个熔断源真发重试：**18 个（78%）立刻成功**（6 个 YouTube 假 404 / 3 个 xgo 桥 400 / 2 个 403 / 1 个我方解析器 bug） | 用户判断方向成立（"没坏、过一会重试就能上"＝78% 量化成立），归因需修正：云端不是"没有自愈"而是"自愈被 48h 流放 + 不看错误性质 + 本地端根本没实现"。方案见 `docs/specs/35-selfheal-admin-console/35a-selfheal-engine.md`，**待批准未动工** |
| H15 | `restore-all`（`api/[...slug].js:1606-1627`）是**全量无差别解冻**：按 `fail_count>=3 AND enabled=0` 一把梭，不试探、不排除 `mergedInto/retired`（cleanup 反而排除，`collect-turso.js:705`），`unfreezeStmt`（`:1596-1604`）也不清 `frozenAt/resumeCount` → 死源被反复放回、活源计数被污染 | 挂案：35A-F8 里改成"按类别分批试探恢复"；期间人工批量入口仍可用，但要知道它会把真死源也放回 |
| H16 | **系统性故障被折算成单源失败**：出口/代理/RSSHub 桥挂一次，本轮全部到期源 `fail_count++`，于是"我们的网络抖一下"变成"几百个源坏了"（H14 的 458 源批量熔断就是这么来的）。现有 `postRunAlerts` 已有"批量失败聚合报警"（`collect-turso.js:638-648`）但**只改报警形态，不改熔断判定** | 挂案：35A-F6 要求失败率超阈值即判基础设施故障、本轮不计入单源；这条优先级应排在 H14 其余项之前（它是唯一会一次性打瘫全库的） |
| H17 | 文档治理债（本轮实测清点）：`docs/specs/` 里 10/12/27~34 共 **11 个 spec 只有 spec.md，无 plan/task/checklist**，而 `INDEX` 长期声称"四件套"；`HANDOVER.md`/`HANDOFF_PROMPT.md` 曾各自内嵌进度快照（已清，见 `DOC_GOVERNANCE` §4.5 与本次改动）；`portal/` 仍带一份过时 `docs/` 副本（关联 H9） | 已立规则未补历史：`docs/DOC_GOVERNANCE.md`（清洁 SOP + 六类归档 + 门禁）＋ `tools/doc-lint.cjs` 自检（**该脚本本轮未实现，列在阻塞项 BL6**）。历史四件套不补，只在 INDEX 如实标注"只有 spec.md" |

## ⛔ 阻塞项与优化方向（2026-09-18 文档清洁轮）

### 阻塞项（不先清掉，后面任何改动都无法判断"是不是我改坏的"）

| # | 阻塞 | 为什么阻塞 | 清法 |
|---|---|---|---|
| BL2 | B14 `qOne()` 修复只提交了 `1d135f4`，**没有一个真实 runner 批次产出证据** | 35A 的恢复判定要读源状态行，同族静默 ReferenceError 未闭环 | 等 W7 观察期，看下一批 collect/daily-ai 日志 |
| BL3 | 采集尝试流水不存在（B23 假成功率 + B24 心跳只 25h 且无正样本） | 「每源成功抓取概率」现在**物理上算不出来**，35C 只能继续显示假数字 | 35B（滚动窗口，不建新表） |
| BL4 | H9 portal gitlink 无 `.gitmodules` | 三端语义收敛时极易误改 `portal/` 副本；Vercel 构建已报 submodule 警告 | 拍板：补 `.gitmodules` or 停 portal 部署/收编为普通目录 |
| **BL7** | **P0 云端报警链路无出口**（B44：渠道被回归测试写坏成 `url:undefined` 的 TEST，09-17 起 `sent:0`） | 系统正在"哑火"：熔断/停滞/AI 失败全都不再通知，与自愈改造互为前提（没有可观测就没有可信自愈） | **2026-09-19 用户决定：先不写生产，等 spec 41 评测就位后再做** → 恢复动作与"改前必红"证据一起出；期间任何故障无人通知属**已知风险**。**2026-09-19 实测量化**：`alerts.recentLog` 近 7 天 **33 条事件全部未送达**（`results[].ok:false, error:"fetch failed"`，最后一条 09-17T08:34），生产库唯一渠道 `test-ch` 的回调是 `http://127.0.0.1:1`；而 `eval:preflight` 因判据过宽（见 B67）此前把它显示成「1 个可用渠道 ✓」——本条在门禁里长期是绿灯 |
| **BL7-更正** | ✅ **09-20 复跑 `eval:preflight`：本条两条判据转绿 → 报警出口已恢复**（渠道有真实出口 + 最新一条 `2026-09-20T03:39` 已送达、近 4 条 4 条有成功投递，另有 `mybrief` 一条真实投递文案）。**但恢复是换库副产物**（新库 `settings.alerts` 来自本地灾备副本，带的是真渠道），**不是**走放行清单 #6 那次授权写入 → 本条不许当"已修完"关闭：**旧库那份 `settings.alerts` 仍是哨兵值**，将来任何"从旧库迁 settings"的动作都会把哨兵带回来（同坑 #67 那族"别把绕过当成修好"） | 状态更正（可观测性恢复，残余在旧库） | 关闭条件补一条：**`settings['alerts'].channels` 形状要进回归锁**——现在没有任何测试钉住"渠道 URL 不是 `127.0.0.1:1` 哨兵"，下次同样的写坏仍只能靠 preflight 手工发现。属白盒新增（W 系），按 §3.6 需你点头才动工具面 |
| **BL8** | **P0 `ai.minIntervalMs=0`**（B49）。**09-19 夜精确化（只读实测）**：它是**独立的 settings 键** `ai.minIntervalMs`（值字面量 `0`，长度 1），不在 `settings.ai` 对象里；**唯一读点** `api/_ai.js:141 getSetting('ai.minIntervalMs', 4000)` → 默认 4000 被库里存的 0 显式覆盖，且本地/runner 都经 `api/_ai.js` 走这一条路（所以是"一处读、值被写坏"而不是"多处各写"） | 无间隔硬打 Agnes 免费池 → 45-60 分钟耗尽配额（坑 #A1），早报/周刊/翻译互相抢额度。今晚实测佐证：近 7 天 500 次调用里 131 次失败（B46） | 同上：延后到 41 就位；**但"防再犯"的代码侧（测试隔离 + 写路径守卫 + `<1000 视为误配` 的下限保护）不写生产，可先行**（待批准）。落点见 `docs/specs/39-ai-console/39-5-real-control-points.md` C1 |
| **BL8-更正** | ✅ **09-20 下午实测把本条前提推翻了一半**（AGENTS §2.4：原文不删，只标注 + 给读数）：直读线上 `settings` → **字面键 `ai.minIntervalMs` 根本不存在**（表里只有 `ai` 一个对象键，而 `getSetting(key,def)` 查字面键不查路径），产品侧 `getSetting('ai.minIntervalMs', 4000)` 一直取到 **4000ms**，"无间隔硬打 15RPM 免费池"**从未发生**。真正错的是 `eval:preflight` 自己：它读**原始值**、把"缺键 = null"算成 `0` → 这条判据从 09-19 起就在**报假红**，还顺着把一条不存在的 P0 写进了待办清单（#7）。教训与坑 #45/#59 同族：**门禁的取值口径与被测代码的取值口径不是同一份实现，绿灯红灯都不可信** | 判据不实（假红）+ 缺下限保护（防御性） | ✅ 已做（提交 `fc3ae51`）：①新建 `lib/ai-throttle.js` 的 `gapMs()` 作为间隔算术**唯一出处**（缺键/0/空串/NaN/负数/低于 1s 一律回落 4000），`api/_ai.js:aiChat` 改走它 → "将来真有人写成 0"也挡得住，下限是真的 ②preflight 改调同一个 `gapMs` 并**同时印"原始值 → 生效值"** ③锁 `tests/regression-ai-throttle.test.js` 四条含负向自证。复跑：preflight 9/9 真退 0、whitebox 仍 15/16（W9 缺口没变多）、F2P `docs/eval/f2p/2026-09-20055237.json`（base 改前红 1/1 且点到用例名）。**同轮我查这个键时未掩码吐出 `settings.ai` 整行 → 登记 B122** |
| **BL9** | **P0 `settings.ai` 仍可被后台写回**（B50），与旧 env-only 声明冲突 | 09-11 全链路 401 停摆 2 天的合法复发通道 | **已裁决（2026-09-19）：保留可写 + 强制审计 + 变更告警 + 写后连通探测失败即回滚**；不变量表述已在 `CLOUD_PIPELINE_GUIDE.md` §6.1 作废落档。实施在 39-1/39-3 |
| BL10 | `'null'` 字符串污染 2.5 万行（B15 订正） | 毒害保留清理豁免、未读角标、阅读足迹三处口径；不先清则任何"未读/已读"统计与自动回收都不可信 | ✅ **09-21 判为"无需动作"**：按 `typeof` 分组数**现役**云端库 → `read_at` 59,618 真 NULL / 214 有值 / **0 个字面 `'null'`**，`tags`/`reason`/`videos.watched_at` 同样 0 命中。那批污染留在 09-20 被换掉的旧库里（新库是用修好 `v === null` 之后的迁移脚本重建的），**不需要任何生产写**。读数、我自己造的两处假读数、以及对 B101 的连带影响（`read_at IS NULL` 现在真命中 5.9 万行 ⇒ 接回清理就是真删，待删量必须按现役库重算，不许沿用 10,733）全在 `docs/eval/bl10-null-audit-20260921.md`；`lib/retention.js` 文件头同一句已按实测标注作废 |
| BL11 | 端到端/白盒评测流程尚未建立 | 用户新增验收要求；没有它，32 个新缺陷的修复无法自证"真的修好了" | 见 `docs/specs/41-e2e-whitebox-eval/spec.md` |
| **BL12** | **Vercel 构建滞后于 HEAD，「推了就等于上线」不成立**（09-19 实测，含对本条第一版判定的订正）：连推 5 次后 production 仍是 04:25 的 deployment。**我第一版判成"Git 触发器停了"是错的**——每次 push 都新建了 deployment，只是新构建长时间卡在 `● Initializing`（更早的构建 16~18 秒就完），期间 production 一直服务着旧的一份 | 后果不是"没部署"而是**线上何时变不可预测** → "云端实测"只有在线上 commit == origin/main 时才算数，否则测的是旧代码 | 判据已建：`/api/meta` 回传 `VERCEL_GIT_COMMIT_SHA` + `eval:preflight` 新增「线上 commit == origin/main」，不一致即红，不许再靠"我推过了"这种代理信号。**09-19 23:30 复测**：push 后 10 分钟内 `commit` 已等于 `origin/main`，硬前置恢复可用；但**退化成分钟级排队的根因仍未查**（需人看 Vercel Deployments 日志/配额），本条不销。**禁止手动 `vercel --prod` 绕过**，那只会把根因盖住。当轮 B58/B62 的验证读数已并到 W8 一行 |

### ✅ 本轮已修（已归档）

> 证据对账表（逐份 F2P/评测读数）外迁：`docs/archive/debugging/2026-09-19-delivery-evidence-ledger.md`。**活文档里只留结论与"条数一律以证据 JSON 为准"这条判据。**

### ✅ 产品选择：09-21 整表批准（用户原话「如果要批准我直接全部批准」）

> 这张表原本是"要选什么 + 我的推荐"。09-21 用户按推荐默认项**整表批准**，所以第三列改为**裁定与落点**，推荐原文逐字留在第四列（§2.4：作废/定稿要落档，不静默删）。
> **批准后本表不再是提问，而是排期输入**；`等什么决定` 这一栏在本活文档里自此为空。生产写仍按"写前备份 + 写后回读"的既有口径执行。

| # | 要选什么 | 裁定（09-21 按推荐全批） | 原推荐（默认项） |
|---|---|---|---|
| B17 | 周刊初筛：24 分钟窗 vs 全量 ≈2.2 小时，三个方案都改行为或花配额 | 立做：改 `runWeekly` 初筛为预筛降量，不抬 AI 配额 | 预筛降量（六维分门槛），不抬配额 |
| B18 | `videos` 建 `score`/`translated_title` 列（schema + 三端采集语义 + AI 配额），还是放弃视频评分/翻译 | 立做：只加 `score` 列并接进评分管线；视频翻译明确放弃（不在三端补） | 只建 `score`，放弃视频翻译 |
| B107 | 「我的阅读」是否排除热榜聚合噪声（现 `type=all` 的 71% 是热搜） | 立做：`/api/reading` 默认排除噪声 + 另给"含热榜"开关，噪声判定收进拟建单实现文件 `noise.js`（落在 `lib/` 下）一份（拟 W18；**文件尚不存在**，建成前引用即假悬空） | 排除，另给一个"含热榜"开关 |
| B95 | 四个无入口的后台组件（B站/抖音/微信/队列面板）：删，还是挂回本机端入口 | 挂回本机端入口（云端仍不暴露），随 38 域信息架构一起落 | 挂回本机端入口（删了就一起丢排障能力） |
| B98 | 云端日报补生成门槛：注释写北京 09:00、代码是 01:00 | 改成北京 09:00（runner 00:32 已先写这一期），注释与条件同批改 | 改成 09:00（runner 00:32 已先写这一期） |
| H12 | `settings['weekly']` 后台改了不生效（窗口/条数全硬编码） | 接进配置并加"改了真生效"断言，归 39/40 域 | 接进配置并加生效断言 |
| H13 | 我的早报期号 + 归档（→ 40-4，用户 09-18 已标注要） | 立项做：先建 `mybrief.archive` 再谈期号（否则期号随覆盖写重置）；B21 并进来一起做 | 立项做 |
| B34 | spotlight 全量替换无 WHERE 作用域（修法属 38-C C2 + 38-D D3） | 候选集收进 `lib/` 一份，替换作用域 == 界面呈现集合 | 候选集收进 `lib/` 一份，替换作用域 == 界面呈现集合 |
| H1 | 是否给 YouTube 上住宅代理 | **不做**，维持现状可接受（挂案不销） | 不上，现状可接受 |
| 38-D | 后台边界 D1~D5 | 沿用 `35d` 清单，排在最后 | 沿用 `35d` 清单，放最后 |
| B122 B125 | 凭据轮换（`AGNES_API_KEY` 已进过一次会话输出；`ADMIN_PASSWORD` == 源码兜底默认值且在 `origin/main`） | 代码侧立做：两处兜底默认值改成"缺 `ADMIN_PASSWORD` 即 401 并发告警" + 回归锁 + 掩码当前 tip 10 处明文；**历史重写不做**（随 §四 冻结项）。**轮换动作本身在你侧**（Agnes 控制台 / Vercel env / GH Secrets 三处同步） | 轮换 + 改成"缺 env 即 401 并发告警"配回归锁；**历史重写不做**（随冻结项） |

已裁决、不再待批：H10 `settings.ai.features` 假开关 → 摘除（B51/39-2）· BL9 AI 配置写入口 → 保留可写 + 审计 + 告警（`CLOUD_PIPELINE_GUIDE.md` §6.1）· B15 → 改判 `'null'` 污染（BL10）· B25 在途改动 → 由并行会话持有，不代提交。


### 优化方向（已迁出）

> 未来计划归 `docs/NEXT-DEV-REQS.md` 一处（§4.1：两个事实源必分叉），本轮已整段并入其末尾。
