# 取证 · 源侧细查（后台重构的证据底稿，2026-09-24）

> 性质：一次性只读探针产物（AGENTS §3 第 7 条）。**零生产写、零 AI 消耗**。
> 取法：`sources` 全表 + `articles` 两次 `GROUP BY source_id` 聚合（**不用相关子查询**——
> 按源扫 1,129 次会把 521M 字符那列重复扫上千遍，探针自己就成了读放大）。总取数 90s。
> 本篇只描述现状与可判定信号，**不含任何已执行改动**；源侧处置属后续开发计划（用户 09-24）。

## 一、盘子（启用源 1,129）

| 口径 | 数 | 说明 |
|---|---|---|
| 按类型 | rss 972 / x 113 / hotlist 28 / youtube 14 / bilibili 1 / douyin 1 | 本地专属类型（bili/抖音/youtube）合计 **16 个** |
| 近 14 天在候选池口径下有产出 | **793** | 日均 **1,875 篇** 进库（近 14 天 26,252 篇） |
| 14 天 0 产出 | **336** | 拆开看见第二节，"僵尸"不是一类东西 |
| `last_fetched_at` 为空 | **0**（最晚 = 2026-09-24T13:16Z，就是刚刚） | 所有启用源都还在被排队抓取 |
| `next_fetch_at` 已到期 | 250 | 每轮 collect 的 `LIMIT` 决定谁能跑 |

## 二、"336 个不产出的源"分桶（关键：它们是四种不同的病）

| 桶 | 数 | 样本 | 是什么病 |
|---|---|---|---|
| A 从未采到过任何文章 | 201 | 影视飓风[bilibili]、Bob同学[douyin]、Futurepedia[youtube]、Qwen[rss]、dbaplus社群[rss] | 其中 16 个是**本地专属类型**（云端本来就不采，属正常）；剩下 **≈185 个云端 rss/x 从未落一文** → 要区分"URL 死了"与"从没轮到过" |
| B 有行但无 `published_at` | 2 | Google Developers Blog、美团技术团队 | feed 缺日期 → 时间窗判据看不见它，**等于永久出局**（采集侧数据质量问题，不是源的问题） |
| C 近 14 天有产出 | 793 | Hugging Face、xAI、AIHOT 热榜(1,123 篇) | 正常 |
| D 停更 14~30 天 | 49 | Google DeepMind(最后 09-08)、Anthropic(09-01)、DeepSeek(09-10)、李继刚(09-06) | 有真实内容后停：可能是上游慢，也可能是抓取失败被吞 |
| E 停更 30~90 天 | 50 | 阮一峰的网络日志(08-13，仅 3 篇)、Meta AI(07-27)、通义实验室(07-20) | 同上，但更像上游/URL 变更 |
| F 停更 >90 天 | 34 | **老钱说钱：最后 2022-07-01，仍 enabled、仍在排队**、有赞coder(04-07)、风叔云(04-26) | 明确的死源，每轮白白占一次抓取 |

**最刺眼的单一信号**：`共 9~11 篇封顶`的源有 **266 个**。形状像"导入时抓了一页 RSS（默认 10 条），此后再无新增"——
配合 `last_fetched_at` 全部有值且最新，说明**它们在反复被抓、但拿不到新条目**（feed 死、站点只保留最新若干条、或去重把同日期条目挡掉）。
这 266 个是后台重构里最有价值的一类：判据明确（产出封顶 + 仍在全额排队），动作可回退（停用而非删除）。

## 三、形态与体积（顺带修正我上一条的错误建议）

- **形态就不是文章的流：12 个仍在候选池**。`Recent Commits to openclaw:main` 是池内第一大户 **40.4 篇/日**；
  另 11 个是 `Release notes from *`（biome/bun/claude-code/codex/gemini-cli/langchain/openclaw…，日均 0~3 篇）。
  用户 09-24 明确过："我们的提交 openclaw commits 不该放进翻译里面，不仅浪费次数而且肯定有问题"。
  ⚠️ 注意区分：`Release notes` 是**版本发布**，与 commits 流不同类，别一起砍（09-24 已核过）。
- **体积大户不是腹泻源，是"低产长文"中文源**：14 天正文 >3M 字符的 27 个源里，前几名是
  Wallpaper中文版 8.7M(0.7 篇/日)、南方周末 6.3M(2.8)、果壳 6.1M(3.3)、一席 5.3M(0.5)——**一天不到 3 篇却每篇几万字**。
  平均正文 **15.1K 字符/篇**。
- **修正：90 天归档腾不出多少**（推翻我上一条"归档排期=腾体积主路"的说法）。实测：
  90 天前的文章只有 **3,029 篇 / 40.7M 字符 = 全库正文 521.6M 的 7.8%**；30 天前也只有 5,398 篇 / 78.0M（15%）。
  真正的原因是**增长速率**：日均 1,875 篇 × 15.1K 字符 ≈ **28M 字符/天**，一个月新增正文 ≈ 850M 字符。
  所以可选的是：① 归档窗口从 90 天压到 30 天（收益仍只有 15%）；② **根本不落 `content_html`**（改为按需补抓 / 只存摘要，
  深析阶段已实现"按 id 单取正文"，说明主链路不强依赖常驻全文）；③ 存前压缩或截断长度上限。
  这一条要单独拍板，已写进 ISSUES **H26** 的处置项（原文订正，不改口）。

## 四、给后台重构的可判定动作（**均未执行**）

1. **死源自动停用**：连续 N 天 0 新增 + 仍 enabled → 转 `enabled=0`（可回退），先拿 F 桶 34 个 + "封顶 266 个" 试点。
2. **形态出池**：`/commits/`、`github.com/*/commits/*` 这类流从候选池出（不进翻译、不进初筛），保留在源列表里可见。
3. **按端分面**：16 个本地专属类型源在云端列表里应标"本地专属"，不计入"云端僵尸"（否则治理指标是假的）。
4. **`published_at` 缺失的源**（B 桶 2 个）：采集侧补时间口径，否则它们永远进不了任何窗口查询。
