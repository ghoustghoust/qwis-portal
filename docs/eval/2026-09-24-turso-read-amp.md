# 什么在大量读 Turso · 以及 170MB 到底是哪个体积（2026-09-24 只读探针）

> 生成时间：2026-09-24T15:18Z。用户点名题：「探索一下我们的什么内容在大量读取 Turso 的读取，这是老库达到上限的主要原因。新库现在已经用了 170m 了」。
> **全程只读**：六个探针脚本只跑 `SELECT / PRAGMA / EXPLAIN`，每条执行前有只读断言（写语句直接抛）。零生产写、零模型调用。
> 探针（`tools/_*.cjs` 按仓库约定 gitignore，一次性工具不落库）：
> `_probe-turso-read-amp.cjs`（体积归因 + 代表查询计划）· `_probe-turso-read-amp2.cjs`（页/空闲页/列字节/score 类型）· `_probe-turso-read-amp3.cjs`（videos·archive·pending + 日增）· `_probe-turso-read-amp4.cjs`（按源归因，用 `lib/noise` 同一实现切轴）· `_probe-daily-read-cost.cjs`（早报读取对照）· `_probe-retention-gate.cjs`（删除闸当前状态）· `_probe-reclaim-size.cjs`（放行一次能回收多少，谓词取 `lib/retention` 唯一实现）· `_probe-recurring-scans.cjs`（cron 驱动查询的计划）
> ⚠️ **探针自己的读放大也要记账**，见 §七。

## 一、先把"体积"这个词拆成四个不同的数（它们互不矛盾）

| 量 | 实测 | 谁在意这个数 |
|---|---|---|
| **逻辑库文件** = `page_count × page_size` = 281,918 × 4,096 | **1,101.2 MB** | 读路径按页取数，扫描型查询的成本单位是它 |
| 其中**空闲页** `freelist_count` | **453.1 MB** | 09-20 那次批量 DELETE 留下的洞；**不 VACUUM 就永远占着文件** |
| 已用页（1,101.2 − 453.1） | 648.1 MB | 真实存量 |
| **Turso 控制台**显示 | 用户报的 **170 MB** | 计费/配额展示位（推断：对象存储里的压缩帧，不是逻辑页 —— 本文不去证这个等式，因为配额维度另有其账，见 §四） |
| 全部文本列字节合计（`LENGTH(CAST(x AS BLOB))`） | ≈605 MB，其中 **`content_html` 587.0 MB = 97.0%** | —— |

> 为什么强调字节口径：`LENGTH(TEXT)` 返回**字符数**，中文正文按字符报会把体积低估约 3 倍。本项目所有正文测量一律用 `LENGTH(CAST(col AS BLOB))`。

现役 `articles` = **35,446 行**（两次探针之间涨 41 行），列字节：content_html 587.0 / summary 6.1 / url 2.6 / cover 2.3 / title 2.2 / translated_content 0.4 / reason 0.3 / tags+word_count ≈0.1 MB。`articles_archive` **0 行**（归档从未跑过，与 H26 一致）、`videos` 2,878 行、`daily_reports` 57 行 / sections 合计 1.5 MB。

## 二、谁产生体积：**推翻"腹泻源撑爆库"这个直觉**

近 24h 落库 **10,916 篇 / 正文 99.6 MB**（url 去重后仍是 10,916 —— 跨源重复确实为 0，与既有读数一致）。按**阅读器降噪轴**（`lib/noise.js` 同一实现，不是另写一份 WHERE）切：

| 轴 | 行数 | 正文字节 | 单行均值 |
|---|---|---|---|
| reader-visible（进阅读器、进早报池） | 6,194 | **98.2 MB（98.6%）** | **15.9 KB** |
| reader-excluded（热榜/聚合/AIHOT 这类） | 4,722 | 1.4 MB（1.4%） | **0.3 KB** |

正文体积 top 20 源（= 近 24h 正文的 34.1%）全是 RSS 正规源：中国新闻周刊 50 行 3.7 MB（**76 KB/行**）、每日经济新闻 71 行 2.7 MB、虎嗅 167 行 2.5 MB、券商中国 29 行 2.2 MB、果壳 13 行 2.2 MB……
而全表存量行数 top 15 里前排是 微博热搜 1,249 / 金十数据 1,223 / AIHOT 热榜 1,160 —— **它们多，但它们是"薄的"**。

⇒ **结论 1（可判定，口径已按 09-24 夜对抗审查收紧）**：砍热榜/聚合这类腹泻源省的是**行**（早报池覆盖、候选削减、扫描行数），**省不到体积**；体积由"我们真正想读的全文源"产生。**增量与存量两侧都测了**：近 24h 是 98.6% vs 1.4%，全表存量是 **98.3% vs 1.7%**（§九 ①，行数前 12 的源占 22.9% 的行、只占 1.6% 的正文）。`type=rss` 的聚合源（AIHOT 三条）在阅读器降噪轴里抓不住，归类按实测标：热榜/精选全文=排除、日报=可见。

## 三、读放大台账：每调一次付多少

代价信号有三种，先分清：`SEARCH ... USING INDEX`（点查，最便宜）／`SCAN ... USING INDEX`（**有序索引扫**，带 `LIMIT` 能提前停）／`USE TEMP B-TREE FOR ORDER BY`（**排序器物化所有命中行**，`LIMIT` 救不了）。

| 调用点 | 计划（实测 EXPLAIN） | 每次代价 | 频率锚点 |
|---|---|---|---|
| 阅读器·实时流（默认首屏）`api/[...slug].js:482` | `SCAN a USING INDEX idx_articles_published` | ≈31 行读，**便宜** | 每次开阅读器 |
| **阅读器·精选栏**（线上真语句逐字重建：`api/[...slug].js:408,436,472`）| 真语句实测：`SCAN a`（**裸全表**）+ `USE TEMP B-TREE FOR ORDER BY`；把 CAST 去掉后计划变成 `SCAN a USING INDEX idx_articles_score` + `TEMP B-TREE FOR LAST 2 TERMS` | **表访问＝全表 35,446 行/次**（每调一次就是这个数），但**排序器只装满足 WHERE 的 712 行**（不是全表，"1.3 MB 正文"那条是替身查询算的，作废） | 每次开精选 |
| **阅读器·搜索 `q=`** `:142 (title LIKE ? OR content_html LIKE ?)` | `SCAN a USING INDEX idx_articles_pubco` | 命中稀的词→**扫到全表**，且每行都要读 `content_html` → **最坏 587 MB/次** | 每次搜索 |
| **早报详情 `/api/daily`** `:828 SELECT * FROM daily_reports ORDER BY generated_at DESC LIMIT 20` | `SCAN daily_reports`（**这张表一个索引都没有**）＋ TEMP B-TREE | 57 行（小）但**传输 573 KB/次，实际只用 21 KB → 27× 放大** | 每次开早报 |
| 阅读足迹·近 7 天 `:995` | `SCAN daily_reports` | 同上量级（0.6→0.2 MB） | 每次 |
| 日报宽池读（级3，6000 行，不带正文） | `SCAN ... USING INDEX idx_articles_published` | 6,000 行 / 1.2 MB | 每日批 + 内联兜底 |
| 保留读数·两条 COUNT（collect 尾部） | `SEARCH USING INDEX idx_articles_later_sk` | 索引，**便宜** | **96 批/日** |
| 我的早报·已读/稍后读 COUNT、按源近 14 天 COUNT、按 id 取正文 | 全部 `SEARCH ... USING INDEX` | 便宜 | —— |

⇒ **结论 2（可判定）**：**自动化链路（cron 驱动的采集/清理/读数）不是读放大的来源** —— 逐条 EXPLAIN 全走索引。读放大集中在**三个人手点的 UI 入口**：精选栏、正文搜索、早报详情。它们的共同点不是"读得多"，而是**没有索引支撑它们想要的那个序**（`CAST(score)` 废掉 `idx_articles_score`；`daily_reports` 整表零索引；`content_html LIKE` 天生无索引可走）。

## 四、配额维度对上了：老库的死法是「行读」，而它的单价随表大小线性上涨

Turso 官方口径（[Usage & Billing](https://docs.turso.tech/help/usage-and-billing)、[Pricing](https://turso.tech/pricing)，2026-09-24 取）：计量维度是 **rows read / rows written / storage**；免费档 = **5 亿行读/月、1,000 万行写/月、5 GB 存储**（Developer 档 25 亿/2,500 万/9 GB）；超限的行为是 **`BLOCKED`，查询直接失败**。
⚠️ **本仓实际在哪一档无法程序化确认**（没有 Turso 平台 API token，B119 ④ 那条限制没变）——下面的"能撑多少次"按**免费档**算，是保守下界；若实际是 Developer 档，同样这些次数要 ×5。
本项目 09-20 那次事故文案正是 `BLOCKED: SQL read operations are forbidden`（B118 / RUNBOOK §10.9）。
⚠️ **但"被禁的是读"推不出"先撞的是行读配额"** —— 官方口径是**任一维度**越限都以 `BLOCKED` 形式失败，被禁的动词标识不了越限的维度；
本仓又没有平台 token，**哪一维先超限这件事不可观测**（B119 ④）。能确定的只有方向：**行读与存储都随"只涨不删"同向上涨**，
而这两条的共同上游就是 §五 那道删除闸。下面按免费档算的"能撑多少次"因此只是**一条上界演算**，不是对事故成因的判定。

把 §三 的单价乘起来就能算出"多久撞一次墙"：

- 全表 35,446 行 → 一次精选栏/一次稀词搜索 = **35,446 行读** → 5 亿 ÷ 35,446 ≈ **14,100 次/月（≈470 次/日）**就打完配额。
- 而**表在按 ~100 MB / ~11k 行/日 增长**（§二），所以这个"每次的单价"每天涨 30%：同样 470 次/日，一个月前是省余额，一个月后是超配额。
- 增速为什么压不住 → §五。

## 五、真正的闸门：**跑到的那一次被挡下、一条没删；而闸的 48h TTL 与"满 7 天才可删"结构性错配**

`settings['retention.pending']`（runner 每批刷新的当前值，只读回读）：

```
读数时间=2026-09-24T15:02:56Z  retentionDays=7  待删合计=9,462  分类={hotlist:0, retention:9,462}
删除闸 allowed=false  via=credential  原因：转储凭证已过期：70.2h > 上限 48h
历史：09-21 闸=true(total 0) / 09-22 true(6) / 09-23 false(8,562) / 09-24 false(9,462)
```

机制（不是猜测，逐字读代码得出）：`lib/content-dump.js#deleteGateAny` 两条腿 —— ①runner 本地盘的转储目录（GH runner 是临时盘，**这条腿结构性不可能有**）②库内凭证 `settings['retention.dumpCredential']`，由本地 `npm run dump:content -- --scope cloud` 写入，**TTL 48h**（`GATE_MAX_AGE_H`）。

**执行证据（对抗审查指出"每天照跑一条不删"当时是无证据的断言，于是去读了唯一能证它的那把键 `settings['cloud.collect'].history`）**：
心跳窗口 `2026-09-23T16:52Z → 09-24T15:32Z`（168 条，模式分布 collect×98 / translate×65 / daily-ai×3 / **cleanup×1** / daily×1），
其中 cleanup 那唯一一次的完整 stats 是：

```
2026-09-23T22:57:14Z  {"deleted":0,"retentionDeleted":0,"blocked":"delete-gate","pendingDeleted":8525,
                       "gateReason":"转储凭证已过期：54.1h > 上限 48h"}
```

⇒ 三条结论，替换掉本轮此前的两个说法：
1. **"每天照跑、每天不删"作废** —— 23 小时窗口里它只跑了 **1 次**；那一次确实**删了 0 条、被闸挡下**（`blocked:"delete-gate"`，8,525 条待删），所以"闸挡导致不删"是**实测**而非推断，而"跑得够不够勤"要挂回 H27（定时投递率≈12%）这条已有的账。
2. 凭证签发时刻 = 22:57:14Z − 54.1h ≈ **09-21T16:50Z** → 闸在 **09-23T17:00Z 前后**才关上（本轮此前写"09-22 起恒挡"是错的，按逐日历史只能说到 09-22 23:00 还是开的）。
3. **真正的结构缺陷是两条时钟对不上**：一行文章要**满 7 天**才变得可删，而转储凭证只在**人工跑一次 dump 之后的 48 小时**内有效 ——
   实测正好演示了这一点：闸**开着**的那 48h（09-21T17:00Z→09-23T17:00Z）待删只有 **6 行**；等 09-23 待删跳到 8,525 行时，凭证已经过期。
   人工节奏**不可能**稳定覆盖"每一行的第 7 天生日"，所以这条闸只要还是"人工续期 + 短 TTL"，迟早必然长期挡住。

⇒ 这是**设计正确的安全阀**（宁可挡住也不裸删），但它的**续期是人工本地动作，且没有任何报警** —— 于是 48h 之后系统必然进入"只涨不删"状态。这正是 AGENTS §2「配额是墙钟事件」的另一面：**这次不是 provider 挡我们，是我们自己的安全阀把自己锁死，且无人出声。**

放行一次能回收多少（谓词逐字用 `lib/retention` 的唯一实现算的，不是另写一份）：

| 项 | 值 |
|---|---|
| 待删（retention 轴，7 天前未读未标记非热榜） | **9,472 行 / 正文 235.2 MB** |
| 待删（hotlist 轴） | 0 行（7 天内的热榜行都还新） |
| 7 天前存量合计 | 10,304 行 / 244.2 MB |
| 其中被豁免（已读 213 / 稍后读 3 / 精选 629） | 仅 8.9 MB |
| **回收比例** | **占全表正文 41.6%** |
| 空闲页（另需 VACUUM 才真还给文件） | 453.1 MB |

## 六、可判定动作（本轮**只登记，不做**；删除与 VACUUM 都是破坏性/长事务，须用户点头）

1. **让"删除闸挡住 > 24h"出声**（接现成的云端报警引擎，15-cloud-alerts 那条链已在跑）——这条是纯增益、无副作用，且直接对上 B118 的教训"挂了几小时没人知"。
2. **续期路径二选一**（要用户选）：(a) 把 `dump:content --scope cloud` 变成本机定时任务（本机不总开 → 仍会漏）；(b) 承认"转储腿"在 runner 上永不成立，改成**runner 自己按天写一份可核验的小清单凭证**（manifest 摘要+行数+id 区间进 `settings`，正文留在原地），把 TTL 绑到"最近一次成功采集"而不是人工动作。
3. **削掉三个 UI 读放大**（各自独立、都可回退、都不动数据）——**本条按 09-24 夜"用线上真语句重测"的结果改写，原来那版有两处过头**：
   - 精选栏：`typeof(score)` 实测只有 `null=24,371 / integer=11,034`，**CAST 确实没必要**；把 CAST 去掉后计划**实测**从
     `SCAN a（裸全表）+ TEMP B-TREE FOR ORDER BY` 变成 `SCAN a USING INDEX idx_articles_score + TEMP B-TREE FOR LAST 2 TERMS` ——
     但**这不等价于"修好了"**：三个排序键 vs 单列索引，排序器只是变小没消失；而且**能不能靠 LIMIT 提前停、真省多少行没测**。
     ⇒ 要真省得配 `(score, published_at, id)` 复合索引，或接受排序器。**不许再写成"去掉 CAST 就吃到索引"**。
   - 正文搜索：要么去掉 `content_html LIKE` 那一半（只搜 title+summary，摘要只有 6.1 MB），要么真做 FTS5 —— 但 FTS5 要先测 libsql/Turso 是否支持，属独立议题。**这条是三者里唯一"方向明确、不必先测"的**。
   - `/api/daily`：`daily_reports` **整表零索引** + `SELECT *` 一次搬 20 份（实测 573 KB，返回给浏览器的是其中 1 份 21 KB）。
     ⚠️ **但不许说成"只为用 21 KB"**：`pickDailyReport` 要**看遍 30h 窗口内的每一期**才能挑出档位（同档内还有多份），`LIMIT 20` 是这条逻辑的载体。
     实测余量：30h 内 7 行、单日最多 9 次生成（09-20）→ 20 行 ≈ 2.2 天 > 30h 门槛，**目前够用，但只有约 2.4× 余量**；
     哪一天生成次数冲过 20（历史上有过"单日 31 次"的记录，见 `lib/brief-guards.js` 的 `HISTORY_MAX_ROWS` 注释），
     AI 版就会**掉出窗口、被裸版顶掉** —— 那正是 B112 那族 bug 复活。⇒ 正确修法是**按 30h 时间窗 + 窄列取**（把"看得见"与"搬得动"解耦），顺带补 `generated_at` 索引。
4. **VACUUM 单独评估**（453 MB 空闲页 = 文件的 41%；能不能在 Turso 上跑、跑多久、要不要停机，都没实测过 → 不许当"顺手就能省"报给用户）。
5. **不动刀**：`articles_archive` 0 行不等于归档功能坏了要马上修（H26 已记：归档窗口只覆盖 7.8% 正文，它不是主因）；**也不许**为了省空间去砍全文源——§二 已证那批源才是"我们想要的东西"。

## 七、探针自身的读放大（诚实记账）

- #1/#2/#4/#_reclaim 各含**至少一趟 articles 全扫**（含 `content_html` 列）→ 第一轮诊断自身读走约 **4 × 587 MB ≈ 2.3 GB 逻辑正文**，另加 #5/#7 的早报表 20 行×多次（约 1.7 MB/次）。**补测轮（§九）又扫两趟**（#13 按源归因、#14 分桶）⇒ 本文件全部读数累计自耗 **≈ 3.5 GB 正文读取**。
  ⇒ **停手判据（写下来，免得下轮又"再多量一个维度"）**：任何新增测量先回答"这一趟值不值 587 MB"；#15（压缩比）就是按这条挑的抽样形态（400 行 / 15.9 MB）。这类探针一旦变成天天跑，它自己就是 §四 的那个配额杀手。
- **两处判据/口径自我修正**（都改在本文，脚本已就地修）：① #1 第③节第一版把 `SCAN ... USING INDEX` 也标成"⚠️ 全扫" —— 有序索引扫被误判成全表；现按 `TEMP B-TREE` / 裸 `SCAN` 两级信号判定。② #5 本机跑 `/api/daily` 那条查询要 9~12 s，**但那是本机的链路吞吐**（573 KB ÷ ≈62 KB/s，代理在墙外），换成对照列/对照索引一条都没变快；同一查询在云端 Vercel 侧的端到端是 **2.36/2.75/2.99 s**，而 `/api/meta`（187 B）在同一链路上就是 2.26 s —— 所以**早报详情的真实网络增量约 0.1~0.7 s**。别拿本机秒数当线上秒数（同 §三 的"传输字节"结论互不矛盾：贵的是字节数，不是我的链路）。
- 未做：部署面查询未逐条 EXPLAIN（本轮 14 条），其余要全量核需专门一轮审计；配额维度只引用官方文档口径，**本仓仍无 Turso 平台 token，配额本身不可程序化观测**（B119 ④ 不变）。

## 八、同一轮内的对抗审查：六条判定与逐条处置（09-24 15:48Z）

独立 reviewer（`general-purpose`，只读，不看本会话上下文）拿着"证伪"任务复核本文六条主结论，`npm test` 它自己跑过 = 628/628。**结果：1 条成立、2 条口径过头、3 条判错**，逐条处置如下（正文对应小节已就地改写，旧说法保留在此可反查）：

| # | 原说法 | 判定 | 处置 |
|---|---|---|---|
| 1 | 「被禁的是读 ⇒ 先撞的是**行读**配额」 | **判错**（非序贯：任一维度越限都是 `BLOCKED`，被禁的动词标识不了越限的维度） | §四 已改成"哪一维不可观测；行读与存储同向涨，共同上游是删除闸" |
| 2 | 删除闸机制（两条腿、48h TTL、无自动续期、无报警） | **成立**，逐条对到 `lib/content-dump.js:188,220`、`tools/dump-content.cjs:159`、两个 workflow、`lib/alert-events.js` | 保留；但下面 2b 被推翻 |
| 2b | 「`cleanup` 每天照跑、一条都不删」 | **无证据的断言**（且方向不完全对） | 读 `settings['cloud.collect'].history` → 23h 里只跑 **1 次**，那次 `deleted:0 / blocked:"delete-gate" / 待删 8,525` ⇒ 改成"跑到的一次确实被挡（实测）"+"跑得勤不勤挂回 H27"；并补出真正的结构缺陷：**48h TTL 与"满 7 天才可删"两条时钟错配**（§五） |
| 3 | 「体积不是腹泻源撑的」 | **算术成立、归因过头**（只测了增量；且 AIHOT 这类 `type=rss` 聚合源在降噪轴里抓不住） | §二 结论 1 收紧为"只覆盖增量"，去掉点名 |
| 4 | 「精选栏去掉 `CAST` 就能吃到 `idx_articles_score`」 | **过头**（reviewer 本地复现 + 本轮线上真语句复测：计划**确实**变了，但排序器只是变小没消失；且此前那句的读数来自**替身查询**） | §三/§六 改用逐字重建的线上语句 + `712 行`实测；结论降为"要去掉 CAST 也得配复合索引才真省，未测部分不许当收益" |
| 5 | 「`/api/daily` 拿 20 份只为用 1 份（27× 浪费）」 | **框错**：`pickDailyReport` 必须看遍 30h 窗口内每一期 ⇒ `LIMIT 20` 是逻辑载体；**反过来**它可能不够（历史有"单日 31 次生成"） | §六 改成"目前够用但只有 2.4× 余量，真修法是按时间窗+窄列取"，并把它记成 B112 那族 bug 的**复活条件** |
| 6 | 契约 `daily-report.json` + 锁 P11 | **两处事实错位**（`videos` 被标"仅本地"而云端 `runDailyAi` 正在发它；关键词档位说"只有 4 个键"而 runner 那份带 `prescreen`）+ **判据形式错**（P11 是手抄清单，新键漂移永远不红） | 契约三处已改（含 `candidates` 跨档口径其实**不同**：AI 档＝配额后送模型行数，关键词三份＝进门槛前行数 —— 顺手证伪了不变量 20 里"五份同口径"那半句）；新增锁 **P12** 从四份 `const stats = {…}` 字面量**派生**键集合，配自证坏样本 |

**P12 的 F2P 证据**（改判据必须证明它会红）：派生出四份键集合 = AI 档 15 键、runner 关键词 6 键、`api/daily-generate` 8 键、读层内联 8 键；把契约里的 `timeSplit` 改名为 `timeSplit_REMOVED` 后 **P11 与 P12 同时红**（`契约 stats 缺 timeSplit`），还原后 **13/13 绿**。提取器自带合成样本自证（注释里的 `b:`、三元里的 `yes/no`、字符串里的 `{ d: 7 }` 都不许进结果，而简写属性 `gateDropped,` 必须进）。

## 九、补测：存量的轴归因 + 单篇体积分布 + 压缩比（把 §二 留的"存量未测"关掉）

探针 #13/#14/#15（只读；#13/#14 各一趟全扫，#15 只抽 400 行）。

**① 存量按轴切（35,845 行 / 正文 586.7 MB）—— §二 的增量结论在存量上同样成立**：

| 轴 | 源数 | 行数 | 正文字节 |
|---|---|---|---|
| 阅读器可见 | 925 | 25,874（72.2%） | **576.5 MB（98.3%）** |
| 被降噪轴排除 | 36 | 9,971（27.8%） | **10.1 MB（1.7%）** |

**行数前 12 的源合计占全表 22.9% 的行，却只占 1.6% 的正文（9.4 MB）**，逐个点名：微博热搜 1,292 行/0.3 MB、金十数据 1,274/0.5、**AIHOT 热榜 1,190/3.7**（这条上一轮只敢猜，本轮实测：`type=rss`、reader 轴=排除；同族的「AIHOT 精选全文」354 行/0.8 MB 也是排除，而「AIHOT 日报」33 行是**可见**——同一品牌三条源分属两侧，所以上一轮"不点名"是对的谨慎，这一轮是点名了才准确）、Recent Commits 605/0.4、Top stories 565/1.2。
⇒ **取舍腹泻源在"行"上是真问题（早报覆盖、扫描行数、队列排队），在"体积"上仍然几乎不是问题。**

**② 单篇体积分布（不是长尾病，是正常长文）**：

| 桶 | 行数（占比） | 正文（占比） |
|---|---|---|
| <20 KB | 29,422（82.1%） | 69.7 MB（11.8%） |
| 20~100 KB | 5,122（14.3%） | 237.2 MB（40.3%） |
| 100~300 KB | 1,132（3.2%） | 173.1 MB（29.4%） |
| >300 KB | **193（0.5%）** | 109.0 MB（18.5%） |

⇒ **给正文设 200 KB/篇上限只省 13.6%（79.8 MB）** —— 体积不是几篇巨型文章撑的，是"大量正常长度的全文"撑的。这一条把"截断止血"的期望值钉死了，别当大药用。

**③ 压缩才是同一量级里最大的那一刀（实测 gzip-9，400 行抽样）**：
抽样 15.9 MB → 2.7 MB ⇒ **压缩比 5.90×**；按同比率外推，587 MB 正文入库前压缩 ⇒ **约 100 MB（省 83%）**，**不动任何一个源、不删任何一行**。
⚠️ 三条不许忽略的边界：
- 这是**列内自压**的实测比率；Turso 控制台那个 170 MB 是不是同一口径**仍未证**（本仓无平台 token），别拿两个数互相"验证"。
- 压缩会让 `content_html LIKE '%q%'` **彻底不能工作**（要解压整列才能搜）—— 这其实顺手逼我们改掉 §三 里最贵的那条读放大，但它是**改造**不是开关。
- 写路径有三份实现（AGENTS §1）+ 现有读点若干（`:477` 的 `substr` 兜底、日报 fmt、周刊）⇒ 压缩要**三端同做且每处读都解压**，是一次跨端改动，按 spec 走，不是顺手改。

### 体积杠杆排序（同一把尺：省多少 MB / 动多少东西）

| 杠杆 | 省 | 代价 |
|---|---|---|
| 放行一次保留清理（H29 修好闸） | **235 MB（41.6%）** | 删数据，必须先转储（安全阀本来就是为这个） |
| 正文入库前压缩 | **≈490 MB（83%）** | 跨端改造 + 搜索必须换实现 + 存量要迁移 |
| 正文 200 KB 上限 | 80 MB（13.6%） | 一处写路径改动，最小 |
| VACUUM 回收空闲页 | 文件级 453 MB（不动数据） | 能不能在 Turso 跑、多久，**未实测** |
| 砍腹泻型源 | 体积几乎为 0（1.6%） | 但会动早报覆盖与"行"侧的收益 |
