# 38-D · 早报中心（每日早报 / 周刊 / 归档）—— 板块小 Spec

> 总框架：`spec.md`（38）。状态：**待批准，未动工**（本板块有两处需用户拍板，见"未决"）。
> 板块定位：**产物策展**——管"已经生成的早报/周刊长什么样、进不进归档"，不管怎么生成（那是 40 域与 runner）。
> 关联现状代码：`web/src/components/BriefCenterTab.jsx`（265 行；`api/brief/history`、`api/settings`、`api/weekly/archive/`）+ `DailySettingsTab.jsx`（298 行；`api/settings/daily`、`api/sources`），合 552 行 / 9 个 section / 16.5KB
> 单一实现：`lib/brief-guards.js`（`HISTORY_WINDOW_DAYS=7`、`HISTORY_MAX_ROWS=200`、`DAILY_HISTORY_SQL`、档位判定）、`lib/hot-categories.js`
> 叶子缺陷：B39 ✅ 本轮已闭合（生成历史假 7 天窗）· B51 ✅ 本轮已闭合（假 AI 开关）· B40 周刊归档投影只 6 字段 · B42 域名配额绕过探索位 · B43 飞书推送"读得到开关推不出去"
> 最后更新：2026-09-19

## 背景：本轮已经在这里做过的事（作为基线）

- **B39**：标题承诺"近 7 天"，实现是 `LIMIT 7`（前 7 条）。改为 `DAILY_HISTORY_SQL` + `historySinceIso()` 的真窗口，并把档位（AI 增强 / 裸关键词）由 `brief-guards` 单一实现投影到界面；F2P 证据基线 `b766bf6`。
- **B51**：AI 设置页 4 个复选框 + "x/4 已启用功能" + 一段操作流程说明全是假的（「摘要/分类/事件关联」本就是规则实现），已连后端读写面一起摘掉（`ai.features` 全库无人消费）。
- 仍然真实的两处：
  - **B40**：周刊归档 107,918B / 5 期，界面只投影 6 个字段，`coverTheme`/`storylines`/`editorNote` 全丢 → 最新最好看的两期反而显示「—」。**数据在库里，界面看不到**（坑 #38 的反向形态：有能力没显示）。
  - **B42/B43**：域名篇数配额只裁订阅池、探索位绕过（`collect-turso.js:1377`），且键是主标签不是域名（命名误导）；飞书链路"开关读得到、消息推不出去"。

## 该有什么功能（清单）

| # | 功能 | 判据 | 归属 |
|---|---|---|---|
| D1 | 产物历史（真 7 天窗 + 分页） | 已具备（B39）；分页窗口不重叠、档位徽章与 `brief-guards` 判定一致 | 已闭合 |
| D2 | 周刊归档**全字段**呈现 | `coverTheme`/`storylines`/`editorNote` 可见；缺字段时显示"该期未含"而不是「—」 | 38-4 |
| D3 | 三报设置分离（日报/周刊/热榜） | 每个设置区说明"影响哪一份产物"，且互不覆盖（当前 `PUT /api/settings/daily` 全量替换是反面教材，见 38-C B34） | 38-4 |
| D4 | 配额与探索位的可解释性 | 显示"本期探索位 N 条未受域名配额约束"这类事实（而不是把绕过实现悄悄做掉）；键名从主标签改域名或界面明示语义（B42） | 40 域 |
| D5 | 推送链路的送达态 | 早报生成后的推送必须显示 **delivered**（不是 dispatched）；判据复用 `lib/alert-channels.js`（B67 已把这条变成机器判据） | 37 域 |
| D6 | 归档期号与生命周期 | 期号连续、可删可重生成；保留策略与 38-F 的保留天数是**同一份配置**（不许两处各写一个天数） | 40 域 |
| D7 | 功能区说明降级为 ⓘ | 用户批注⑧：解释性文字不占功能区（38-6 规范；B43/B53 同族的"手动生成命令"文案已在本轮清掉一部分） | 38-6 |

## 边界

- 不改生成链路（40 域与 runner 的事）；本板块只改"看产物、配产物展示"。
- 早报中心的 spotlight 写入口**必须移走**（→ 38-C），这是 B34 的根治，属本板块的删除项。
- 首屏预算：AC4 要求 ≤2 请求、≤60KB（现状 4 请求 ≈134KB）。

## 验收标准

- AC1：D2 用线上真实归档实测——最新一期的 `storylines`/`editorNote` 在界面可见（截图为证）。
- AC2：D3 拆开后，保存任一区不影响其他区的键（回归锁：`PUT /api/settings/daily` 之后 `weekly.*` 与 `hot.*` 指纹不变）。
- AC3：D5 在未恢复报警渠道前必须显示红色"无出口"（当前生产实测：近 7 天 33 条事件全部未送达，见 `docs/ISSUES.md` BL7/B67）——**不许显示"已推送"**。
- AC4：F2P——B40/B42 各配锁：改前红（字段丢失 / 配额绕过）→ 改后绿。

## 未决（需用户拍板，不拍板无法动工）

1. 期号与归档保留策略（H13）：滚动 7 天还是按期号永久归档？
2. `subscription.ids` 是否保留兜底（与 38-C C5 同源）：早报的"订阅"语义一旦改变，本板块的 D3 设置项文案与判据都要跟着改。
