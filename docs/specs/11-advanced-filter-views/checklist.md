# 阅读器高级筛选 + 保存视图 Checklist

> 依据：spec.md AC1-AC6 + plan.md 决策 1-8。每项均可运行/观察验证。

## 实现完整性

- [ ] smart 排序公式生效（验证：回归测试——同时间窗内 focus 源文章排在非 focus 之前；3 天前的 focus 源文章排在刚发布的非 focus 源之前）
- [ ] score_min 过滤生效（验证：回归测试——score_min=80 时无评分条目与 score<80 条目均不出现）
- [ ] lang 启发式过滤生效（验证：回归测试——中文标题在 lang=zh 下可见、英文标题不可见；lang=en 反之）
- [ ] views 分区读写（验证：回归测试——GET /api/settings 公开响应含 views；PUT views 无 token 401；非数组/超 20/空名 400 且不落库）
- [ ] 新参数缺省零回归（验证：回归测试——不带新参数时响应契约与现状一致；`npm test` 既有全部用例不回归）

## 集成

- [ ] 新参数与现有参数正交（验证：sort=smart + group_id + q 组合查询结果正确；cursor 翻页在 smart 模式下不丢不重）
- [ ] dedup 与 lang 互斥（验证：dedup=1&lang=zh 同传时按 dedup 语义返回，面板语言档禁用）
- [ ] 时间预设映射正确（验证：面板选「今天」→ 请求带 from=本地今日；文章列表只剩今日条目）

## 编译与测试

- [ ] `npm test` 全绿（含新增 regression-views-filter.test.js)
- [ ] `npm run build` 通过
- [ ] `node smoke-test.js` 通过

## 端到端场景

- [ ] **场景 1(AC1)**：阅读器切「智能排序」→ focus 源文章靠前 → 刷新页面后选择保持（localStorage)
- [ ] **场景 2(AC2)**：打开筛选面板 → 选「本周 + ≥80 分」→ 列表只剩本周内带评分达标条目，面板显示范围提示；「清除全部」后恢复
- [ ] **场景 3(AC3)**：面板配好「智能排序+人工智能分组+关键词」→ 保存为视图「AI 精选」→ Sidebar「我的视图」出现该项 → 点击后整套筛选恢复 → 删除后消失
- [ ] **场景 4(AC4)**：语言切「英文」→ 列表只剩英文标题条目（中英混合标题按含 CJK 归中文）
- [ ] **场景 5(AC5)**：未登录读者可见已存视图（读公开），保存/删除视图触发登录框（写保护）
- [ ] **场景 6(兼容回归）**：合并同事件、稍后读、全部已读、日期筛选、游标翻页、60s 静默轮询——与改版前一致
