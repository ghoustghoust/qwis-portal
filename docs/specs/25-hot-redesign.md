# 设计：热点榜重设计（2026-09-14，样图定稿）

> 样图：`archive/样图/热点榜/`（3 张 PNG）。
> 用户定性问题：现状热点榜 = 纯热搜聚合流（知乎/微博娱乐向占满、覆盖不全一天），
> 分类（模型/产品/行业/论文/教程/观点）无作用，「全部动态」不是全部动态——
> **需要贴合当前系统架构重设计：读我们自己的源（六维评分后的内容）呈现流式时间线**。

## 样图解读（BestBlogs 热点页形态）

1. **当前热点 Top5 卡**（页首）：排名 + 标题 + 热度值，右上「完整榜单 →」
2. **按日期分组的时间流**：日期分组（9月14日 星期一 · N 条 ▼ 可折叠）→ 每条 =
   时间点（左侧竖排）+ 来源（含「精选」徽章）+ 标题 + 摘要 + **推荐理由** + 右上 AI 评分/100
3. **分类 tabs**：全部 / 模型 / 产品 / 行业 / 论文 / 教程 / 观点（真实过滤）
4. **搜索框**：搜标题、摘要
5. 左侧栏保留系统导航（精选/全部动态/主题/收藏……）

## 与现状的差距（差距 → 改造点）

| # | 现状 | 目标 |
|---|---|---|
| G1 | 数据源 = 热榜聚合源（newsnow 热搜）为主 | **主数据源 = 我们自己源的高分内容**（score ≥ 阈值的六维评分文章 + 热榜聚合为辅）；时间线读 articles（评分/推荐理由字段已有） |
| G2 | 分类无作用（category 从未真实执行，P3-3） | 分类 = 真实过滤：复用六维评分的 tags / AI 分类字段（模型/产品/行业/论文/教程/观点 → 映射 tags 关键词组），服务端 WHERE 过滤 |
| G3 | 时间流只有热榜条目、无推荐理由/评分 | 时间线条目卡：来源+精选徽章+摘要+**推荐理由（reason 字段已有）**+AI 评分（score 已有） |
| G4 | 「精选」tab 与「全部动态」差异模糊（P0-3 修过 tab 参数但语义仍弱） | 精选 = score ≥ 70（六维高分）；全部动态 = 全部源全部内容（含热榜），按时间 |
| G5 | 覆盖不全一天 | 时间线回溯 7 天（数据在库），日期分组折叠 |
| G6 | 无搜索 | 标题/摘要 LIKE 搜索（服务端） |

## 技术方案

- **后端**：`handleHot` 重写为统一查询——
  `GET /api/hot?view=timeline|featured|all&category=&q=&cursor=`
  - timeline/featured/all 三视图共享一条 SQL 骨架（where 差异），游标分页沿用（坑 #25 规则：游标=原生列值）
  - 分类过滤：`a.tags LIKE '%"模型"%'` 类 JSON 串匹配（tags 已是 JSON 数组文本），或建 tags 展开表（量大后优化）
  - 排序：featured 按 score DESC；timeline/all 按 published_at DESC
  - 「当前热点 Top5」：复用 handleHotEvents（事件聚合）取 top5
- **前端**：HotPage 重排——Top5 卡 + 分类 tabs + 搜索框 + 日期分组时间线（竖线+时间点+条目卡，复用现有时间线样式）；骨架屏
- **降级**：热榜聚合源保留为「热搜」子视图（现有时间线数据不丢）

## 影响面

- `api/[...slug].js` handleHot/handleHotEvents（重写/微调）
- `web/src/pages/HotPage.jsx`（重排）
- 无 schema 变更；分类过滤如需展开表另立小 spec

---

## 二阶段（2026-09-14 用户验收新增，未实施——待确认后一个 mew-spec 完成）

| # | 需求 | 方案 |
|---|---|---|
| 二-1 板块更名 | 现名「精选/全部动态/热点榜」易误解 | 更名：**「AI 精选」/「实时流」/「热搜事件」**（Tab 标签 + i18n）；语义：AI 精选=六维评分内容流，实时流=全部源时间序，热搜事件=跨源聚类聚合榜 |
| 二-2 热点从 1500 源挑 | 「热搜事件」聚合目前被热榜同题簇（综合热搜 domain）刷屏，自有源事件被压制 | 聚类事件排序加权：`rank = 事件内 max(score) 权重 + 自有源参与数权重`，自有源高分事件置顶；聚合输入已含全部源（无需改查询） |
| 二-3 分类对齐阅读器 | 现分类=六个固定关键词组 | 分类 pills 改读**阅读器分组**（GET /api/groups，31 组），选中过滤 `s.group_id=?`——与阅读器同一套分类体系（用户要求"以阅读器的那些分类为基准"） |
| 二-4 UI 对齐样图 | 整体布局未完全按样图重排 | 按样图精修：Top5 卡（排名+热度+完整榜单→）、时间线卡（精选徽章/评分右置/推荐理由块）、搜索框样式、日期分组折叠样式——全部对齐 `archive/样图/热点榜/` 三图 |

> 一阶段已落地：自有源+热榜混合时间流（7 天）/ 分类关键词过滤 / 搜索 / 精选=AI 评分内容流（score≥60）/ Top5 卡雏形 / CAST 防御（score 文本 'null' 数据清洗 19780 行）。

## 三阶段（2026-09-14 用户验收新增，当日已落地）

| # | 需求 | 落地 |
|---|---|---|
| 三-1 实时流更名+AI 过滤 | 「实时流」→「**AI 信息实时流**」（i18n `hot.all`）；tab=all 只出 AI 相关内容：AI 主题分组源全收（分组名含 AI/人工智能）+ 其余源标题命中 AI 词表（`api/[...slug].js` `AI_KW_LIKE/GLOB/AI_BARE_GLOB`；短缩写走 GLOB 防 RAG→garage 误伤；裸 AI 走词边界 GLOB 防 SAID/CHAIR 误伤；**OR 链必须平衡二叉树拼接——SQLite 表达式树深度上限 100，实测超长 OR 链报 Expression tree is too large**） |
| 三-2 事件信源完整名 | 事件卡/详情信源展示「分组·名称」（如 公众号·数字生命卡兹克 / 新闻媒体·澎湃新闻）：handleHotEvents 新增 `sourceList`（按 source_id 去重，hotlist 无分组兜底「热榜」）+ 条目级 `source_group` |
| 三-3 趋势折线 | 事件卡右侧热度条 → 真·折线 sparkline：24 桶报道密度（firstAt→latestAt，跨度不足 6h 按 6h），单篇/单时刻事件 trend=null 前端显示「暂无可比趋势」（对齐样图） |
| 三-4 排版精修 | 页头/Top5/分类行/内容统一 860px 居中栏；Top5 名次 Top3 主题色 + 右侧热度值；推荐理由改为左侧色条引用块；事件卡三栏（排名号/标题摘要信源/大热度+折线） |
| 三-5 分类行接线修复 | T5-14 H-D 的 allGroups 此前从未拉取（分类行只剩「全部」）——已接 GET /api/groups |
| 三-6 翻译板块优先级 | runner 自动翻译出队顺序：热点榜精选（同 /api/hot featured 口径 SQL）> 我的早报（mybrief.latest 报告 id）> 周报（weekly.latest 报告 id）> 每日早报（daily_reports 最新一期 id）> 阅读器兜底（P7，同优先级内新到旧）；手动入队仍最优先 |
