# 37-7 · 监控面板纠偏打包（含两项待拍板）—— 小 Spec

> 总框架：`spec.md`（37，G2/G5）。状态：**待批准，未动工**（其中 B47/B48 两小刺本轮复核为已完成）。
> 最后更新：2026-09-19（复核轮）

## 逐条现状（实测，不照抄旧结论）

| # | 事项 | 复核结果 |
|---|---|---|
| ①a | **B47 任务队列面板数字恒 0** | ✅ **已修**：`web/src/components/MonitorTab.jsx:51` 现读 `queueStats?.overall \|\| queueStats`，与 09-19 记的修法一致（线上真值 pending=177 此前显示 0）。**但"修好后保留还是下线"仍是待拍板项**，见下 D-A |
| ①b | 面板显示的是否真是"队列"语义 | ⚠️ 待决：本地 SQLite `job_queue` 与 runner 的 `settings.cloud.collect` 是两套东西。后台现在混着看容易误读（本地队列恒 0 因为本地服务没跑：3000 端口实测无响应） |
| ② | **健康概览做成后台首页可点击跳转**（用户批注⑯⑰） | ⛔ **前置未解**：B26——`/api/status` 冷启动 30.7s→504、热态 0.66s 双峰。今晚复跑 `eval:preflight` 与 `audit-cloud` 时 `/api/status` 与 `/api/meta` 都正常返回，但**没有覆盖冷启动路径**（B26 的定性来自三连跑：30.7s/14.4s/0.66s）。跳过去大概率白屏，不许当"能做" |
| ③ | **B48 `AlertsTab.jsx` JSX 转义字面串** | ✅ 已完成（09-19 记）；本轮不再重复断言，若回归由 e2e 的"无裸转义串"文案检查兜住 |
| ④ | 新增：源数量口径 | ⚠️ 本轮实测两处不一致：`/api/meta` 的 `sources=1197`（`enabled=1`）与 preflight 的 `Turso sources=1437`（全量）。**不是缺陷**（口径不同），但监控页必须显式标"启用/全量"，否则用户读到的数会随端点变 |

## 待拍板

| # | 决策 | 我的建议 |
|---|---|---|
| D-A | 任务队列面板：保留（显示 runner 真实积压）还是按原计划下线换成 `settings.cloud.collect` 心跳视图 | **保留但改名**：叫"采集/队列实时积压"，并同时显示数据来源（本地 `job_queue` vs runner 心跳），避免又造一个"看起来是队列其实不是队列"的面板 |
| D-B | 健康概览跳转是否等 B26 拆完冷路径再做 | **等**。否则交付的是一个 504 按钮，比不做更伤信任 |
| D-C | 源数口径是否统一到"启用数" | 统一显示为"启用 / 全量"两个数，一处实现（`lib/`），端点不再各写各的 COUNT |

## 判据与验收

- AC1 面板改名与双数显示后，`npm run eval:e2e` 后台剧本断言"面板显示的数 == 页面自己拿到的那次响应里的同名字段"（防再次出现恒 0 伪装）。
- AC2 D-C 落地时配白盒判据：源数 COUNT 的条件表达式只许一份（与 B107/W18 同批评估）。
- AC3 B26 未修前，健康概览入口不许上线（若已上线要显式降级为"数据可能延迟"提示，不许静默白屏）。

## 边界

- 本包不实现 B26 的冷路径拆分（那是性能项，独立成包）。
- 不动 `settings.cloud.collect` 的写入结构（spec 41/35B 已登记它的瘦身历史，再改要一并评估体积）。
