# 坑 · 测试（testing）

### #13 smoke-test 跑的是生产库副本
- 规则：smoke-test.js 启动时 backup 到临时目录 + APP_DATA_DIR 注入，结束清理；任何测试文件 require server/* 前必须先 require tests/helpers（APP_DATA_DIR 隔离），**严禁直写 data/app.db**。

### #18 回归测试必须驱动真实路由
- 案例：2026-09-05 验收发现 3 例"手写与实现相同 SQL 再断言自己"的实现式测试——实现改了测试照样绿（空转）。
- 规则：路由行为测试一律起 express 实例打真实 HTTP（tests/regression-sourcelib.test.js 是样板：app.listen(0) + generateToken + fetch）；云端 handler 用 mock req/res 直打 `api/[...slug].js`（regression-cloud-settings 模式）。

### #27 云端回归测试写真实 Turso：全量替换语义必须快照全部行（2026-09-13 事故）
- 案例：regression-cloud-settings 测 9（focusSourceIds 全量替换）的旧"恢复"只复位 2 个测试 id，而该语义 `UPDATE sources SET focus=CASE...ELSE 0 END` 写**整表** → 跑一次 npm test 把线上 8 个 focus 订阅源全部清零（audit_log 04:42 实锤）。
- 规则：①凡是"全量替换/整表 UPDATE"语义的测试，before 里快照该语义会触碰的**所有行**，finally 里用同一条语句精确还原；②用"测试前后关键集合一致"断言自证；③知悉：npm test 会直打生产 Turso（只读断言+带还原的写），跑全量前想一下此刻适不适合。

### #T1 测试并发下的偶发失败
- 症状：全量跑偶发 1-2 项失败（regression-ui-data 曾现），单跑全绿。
- 根因：node:test 并发下的端口/资源竞争。
- 规则：npm test 用 `--test-concurrency=1`（package.json 已配）；偶发失败先单跑复现再定性，别当实现 bug 修。

### #T2 云端测试覆盖写生产 `settings` 且 `after()` 静默失败：报警链路被打死 2 天（2026-09-19）
- 症状：后台报警日志显示"已触发"，但 `audit_log` 最近三条真实事件全是 `sent:0/total:1`；线上 `settings.alerts.channels` 只剩 `{id:'test-ch',name:'TEST',url:undefined,enabled:true}`，silence 里留着 `sourceId:777777`。
- 根因：`tests/regression-cloud-alerts.test.js` 直连生产 Turso 并 `saveConfig()`，测试数据字字对应（777777 只存在于该文件），`after()` 恢复未生效且无人断言 → 生产配置被测试写坏。这是 #13「云端测试直打生产库」的复发，这次打死的正是"告诉我们出事了"的那条链路。
- 规则：①写生产配置的测试必须**快照 → 写入 → 还原 → 断言还原成功**，断言失败要让测试变红（不能只 catch）；②生产写路径要有"测试指纹守卫"（识别 `test-` 前缀 id、`127.0.0.1` 回调地址、777777 之类哨兵值即拒绝写入并告警）；③报警/凭据这类"自证链路"的键，任何改动后必须跑一次真实投递验证（`POST /api/alerts/test` 收到消息才算完）；④**监控显示"已触发"不等于"已送达"**，UI 必须区分 `dispatched` 与 `delivered`。
- 案例：`docs/ISSUES.md` B44/BL7；恢复入口 `tools/sync-alerts-config.js --force`（本地 `settings.alerts.channels[].config.url` 是真值来源）。

### #T3 检测器只覆盖一种形态 = 假门禁；"确实被读"不等于"被消费"（2026-09-19 实测）
- 症状：`settings.ai.features` 挂着 4 个复选框和「x/4 已启用功能」统计，白盒 W3 却报绿。用户据此以为 AI 功能可开关，实际「摘要/分类/事件关联」全是规则实现。
- 根因：W3 的判据是**后端从不 getSetting**。而 `ai.features` 确实被 getSetting 读了——只是读它的那一行只是把它塞进同一个 GET 响应的属性里，供界面回显。写回 → 显示 → 没有任何行为分支消费，闭环在设置页内部。
- 规则：①写"假开关检测"时必须同时覆盖两种形态——**无人读**与**只被自己回显**（判据：某键的每一处 `getSetting` 都长得像响应对象属性，见 `tools/eval-whitebox.cjs` W3b）；②任何新检查器上线前做**负向验证**：造一个该缺陷的最小样本（本轮造 `ai.fakeDemo`：GET 回显 + PUT 写回 + 界面含该键），确认它会红，再删掉确认变绿；没做负向验证的检查器视同不存在；③近似重复的判定要按**特征集合重叠度**判，不按字面量全等（见坑 #37 与 W10）。
- 案例：B51；同一形态在断言层也发生过一次（B53 第一版正则永不命中 → 坏代码在场仍显示绿），规则见 `docs/EVAL_GUIDE.md` §4.1。

### #40 Windows 上 execSync 走 cmd.exe：`^ | & % < >` 会吃掉命令，取证方向能被静默反转（2026-09-19 实测）
- 症状：`tools/eval-f2p.cjs` 第一版用 `--base ca42cd5^` 取"改动前"基线，结果 base 侧 3/3 全绿、判成"这条锁是假的"。实际上它根本没回到父提交。
- 根因：Node 的 `execSync` 在 Windows 上经 **cmd.exe** 解释整条字符串，而 `^` 是 cmd 的转义符 → `ca42cd5^` 被剥成 `ca42cd5`。git 收到的是"改动本身"，于是"改前红"永远不成立。同一类静默篡改还发生过两次：commit message 里的反引号路径被命令替换掉（内容凭空消失）、`npm test | tail` 把退出码换成 `tail` 的 0。
- 规则：①凡是**带 ref、路径、外部输入**的子进程一律 `execFile`/`execFileSync`（数组参数，不经 shell），`execSync` 只用于完全静态的命令串；②git ref 显式写 `^{commit}` 而不是裸 `^`，并**先 `rev-parse` 成 sha 再用**，比较 `baseSha === headSha` 时直接拒绝取证；③任何"取证据"的工具都要有正向探针：本例是自检里那条「base 侧必须真的看见红」——如果 base 与 head 结果一样，判假而不是判过；④交付信息（commit message）里的路径/命令不许走 shell 插值，统一 `git commit -F <文件>`。
- 案例：`tools/eval-f2p.cjs` 的 `gitRaw/git` 改造；修好后同一命令正确报出 `base 2e2c757 红 3/3 → head 0/3`。

### #41 取证基线选错，会被判成"锁是假的"——而那份判据的下一步动作是删用例（2026-09-19 实测）
- 症状：`npm run eval:f2p -- --base 64124b7 --tests tests/regression-20260919c.test.js` 报「改前也全绿 —— 这条锁抓不到 bug，按 §6 应删掉或重写断言」，指向 8 条刚写完、明知能抓 bug 的锁。
- 根因：基线 sha 是我凭记忆挑的，`64124b7` 其实是修复提交 `5aa8118` 的**子孙**——那棵树里修复早就在，"改前红"在物理上不可能发生。工具没错，错在输入；但工具把"输入错了"和"锁假了"报成同一句话，而后者附带的动作是**删除测试**，等于让一次记错 sha 变成永久削弱回归网。
- 规则：①取证基线**不靠人记**：由"这条锁在哪个提交引入"（`git log -S<用例名> -- <文件>` 取最老一条）反查，工具提供 `--auto-base`；②显式给 `--base` 时先做**基线守卫**——base 若已包含目标锁的引入提交，退出码 2 并印出建议基线与"这是取证输入错了，不是锁假了，不要按 §6 删用例"；③"改前也全绿"只允许在守卫通过之后出现，此时才是真假锁；④守卫自身要有正向探针（同一批锁在 `intro^` 上必须放行），否则一个方向写反的守卫会把所有取证都拒死——本轮 `suggestBase` 的祖先判断就反过一次，症状正是"建议基线 = 最新提交"。
- 案例：`tools/eval-f2p.cjs` 的 `lockIntroCommit/baseIsStale/suggestBase` + 自检「base 已含该修复 → 判'基线选错'而不是'锁假了'」「自动基线取最老且与传入顺序无关」；同一轮另有一条相关判据：`Cannot find module` 要按**裸包名 vs 相对路径**分成 env 红与产品红（收敛型修复只有产品红这一种取证形态）。
- 另见：坑 #40（同一工具的 shell 元字符坑）、`docs/EVAL_GUIDE.md` §6。

### #42 云端探针 URL 凭印象写 → 把"我打错域名"报成"线上故障"（2026-09-19 实测）
- 症状：推送后查线上指纹，`curl https://qwis-portal.vercel.app/api/meta` 返回 `DEPLOYMENT_NOT_FOUND`，第一反应是 Vercel 又把部署弄没了（当天正盯着 BL12）。
- 根因：`qwis-portal` 是 **GitHub 仓库名**，Vercel 应用是 `qwis-intel.vercel.app`。两个名字同源不同物，凭印象必错；而 `DEPLOYMENT_NOT_FOUND` 这个响应长得极像"部署不存在"的真实故障。
- 规则：①任何外部探针（URL、参数名、字段名）必须**从仓库里抄**——`grep` 权威文档或代码，不许凭记忆；②探针失败先自查请求本身（换一个已知肯定存在的端点、看响应体是不是自家服务的错误页格式），再下"线上坏了"的结论；③这条是 `docs/EVAL_GUIDE.md` §3.3「探针参数不许猜」的域名版，判据同源，只是本轮换个维度又踩了一次。
- 案例：本轮实测；`docs/EVAL_GUIDE.md` §3.1「远端已部署」以 `/api/meta` 的 `commit` 指纹为准，权威域名见 `docs/FEATURE_MATRIX.md`。

### #43 检查器把"失败理由字符串"当成通过：`pass: !!verdict` 让云端巡检永远满分（2026-09-19 实测）
- 症状：`node tools/audit-cloud.js` 输出「通过 19/19」，但同一行里写着 `✅ 采集端点鉴权 405 应401实际405`、`✅ 源列表异常`、`✅ 文章列表去重 deduped 标记缺失`——**报告里带着失败理由，却按通过计数**。
- 根因：判据函数约定"返回 `true` 通过、返回字符串给出失败理由"，而汇总处写的是 `pass: !!verdict`。非空字符串是 truthy，于是每条失败理由都被判成通过，理由反倒降级成一条附注。叠加第二个坑：该脚本基址写死在 2026-09-11 就已下架的 `qwis-portal.vercel.app`（坑 #42 同源，全库第 5 份手写副本），所以它 8 天来打的都是 404。
- 规则：①判据函数的返回类型必须**单一**：要么返回布尔，要么返回对象 `{pass, note}`，禁止"字符串=失败但用 truthy 判过"这种双关；②检查器一律要求 `pass === true` 才算通过（`verdictToResult` 即此判据），任何 `!!x` 式的宽判都要在锁里写明为什么宽；③检查器自身必须有**退出码**（有失败即非 0），否则人眼读完就散了，进不了任何门禁；④判据里的字段名、状态码、参数名一律**实测得来并写明出处**（本轮三条原判据分别错在：`b.items` 应为 `b.sources`、GET 断 401 应为 POST 断 403、`deduped` 字段云端压根不存在）；⑤契约不存在的检查**改成显式"未验收"第三态**（`SKIP:理由`），既不假装通过也不静默删除——删除会让缺口消失，明说会让缺口进清单。
- 案例：`tools/audit-cloud.js` 的 `verdictToResult` + `if (require.main === module) main()` 守卫；回归锁 `tests/regression-20260919e.test.js`（B64/B65/B66 共 8 条，含"正向探针"锁与"判据必须对得上实测契约"锁）。同一形态在 #T3（检测器只覆盖一种形态）与 #38（能力只用文案表达）已各发生过一次，本条是第三次。

### #44 分类器的正则跨行匹配会"造出"证据——一次合法取证被判成环境失败（2026-09-19 实测）
- 症状：给 41-8 的新文件出 F2P 证据时，工具拒绝：`base 侧有环境类失败（元凶：, ⏎ syscall: ）`。所谓"缺失路径"是两个换行之间的碎片，根本不存在这个文件。
- 根因：`/ENOENT[^\n]*?'([^']+)'/` 想抓 `ENOENT ... '路径'`，但 Node 抛出的对象 dump 长这样：`code: 'ENOENT',` 换行 `syscall: 'spawn python3',`。前缀 `[^\n]*?` 停在本行的引号上，捕获组 `[^']+` **却没禁换行**，于是跨过行首空白抓到 `,` + 换行 + `    syscall: ` 当作"路径"。分类器过宽时，它不是"漏判"而是**凭空造证据**。
- 规则：①抓路径/标识符的正则必须显式禁掉换行（`[^'\n]` 而不是 `[^']`），并用"必须同行出现"的结构锚定；②任何被分类器判成"环境红"的结果都要**点出原路径**（`envPaths`），报"有环境问题"而不给名字，等于让人瞎猜——这也是本轮加的要求；③分类器自身要同时有"树内=产品红 / 树外=环境红"两个方向的探针，以及一条"畸形输入不许产出结果"的探针（对象 dump 就是这类输入）；④同一套"树内/树外"判据要覆盖所有失败形态（`Cannot find module` 与 `ENOENT` 是同一件事的两种写法，只覆盖一种就是 #T3 的老坑）。
- 案例：`tools/eval-f2p.cjs` 的 `parseSummary(text, cwd)`/`repoRel()`；回归锁「41-3 ENOENT 按树内/树外分类，且正则不许跨行造出假路径」+ 取证器自检里「Node 的对象 dump 不许被当成缺失路径」。

### #45 "文件里有红"不等于"这条锁能抓 bug"：取证判据的循环论证 + 退出码混档（2026-09-19 独立对抗性审查查出）
- 症状：取证器对 5 个锁文件报 `✓ F2P 成立`，看起来每轮修复都有双证据。实际旧判据是 `if (!base.fail) → 假锁；否则成立` —— **改前只要有任意一条红就算成立**，红的是不是本轮那条锁根本不看；`--cases` 缺省时拿 `base.failedNames` 自己当目标集，等于"用结果证明结果"。另一条：head 侧解析不到汇总行时兜底成 `{fail:0}`，于是"没跑到"被打印成"改后 0 条全绿"。
- 根因：判据写成"有没有红"这种存在性检查，而不是"我点名的每条锁是否都改前红"；而用法/环境错误与"锁假了"共用退出码 1，1 的处置动作是**删用例**（与坑 #41 同一危险方向）。独立审查还并行查出同类 3 条：巡检全 SKIP 时退 0、评测器空 golden 退 0、"缺产物就 return"的单测（没断言也算通过）。
- 规则：①取证必须**逐条点名**：目标集内每条都要在改前红，缺一即 `state=product` 并列明"哪几条不红"；不给目标一律 `state=env` 拒判；②三态三码：`ok=0 / 锁抓不到 bug=1（允许删或重写）/ 未评测=2（禁止删用例）`，任何"没跑到/没产物/全 SKIP"都不许落到 0 或 1；③"没解析到结果"必须显式失败，**绝不允许兜底成 0**（兜底=无中生有）；④正向探针类断言（验证"检测器看得见坏形态"的）本来就不该在改前红 —— 它们属 P2P 守卫，取证时要显式排除，不要用整文件模式掩盖这个区别；⑤自我审查有天花板：这 5 条都是**独立 reviewer** 找出来的，我自己跑了 20 项自检仍没发现，所以"工具要有自检"不能替代"对抗性审查"这一步。
- 案例：`tools/eval-f2p.cjs` 的 `verdict()`（三态 + per-target）与 `scopeFiles()`；`tools/audit-cloud.js` 的 `tally/exitCodeOf`；`tools/eval-content/run.py` 的 `exit_code_of`；回归锁「41-3 判据收紧」「B65-3 计数与退出码是行为」「41-8 stub 轮次缺产物必须 skip」。

### #46 端到端"对账"对错了对象：拿剧本自己发的请求当页面的请求（2026-09-19 首轮实测）
- 症状：E5 报「筛选没退化成 all（article=0 all=0）」、E1 报「接口 200 但 items 为 0」——看着像一批前端缺陷，实际上页面好好的。
- 根因（三层，全是评测器自己的错）：①剧本先 `goto` 再自己 `fetch` 同参数接口，测的是"接口自己"而不是"这个页面"，页面用的参数、分页、缓存一概没参与；②新开的 page 停在 `about:blank` 时 `fetch('/api/...')` 没有同源 base，1ms 内 `status 0`，而 `status 0` 长得和"接口挂了"一模一样；③共用一个 page 时，前端 `api.js` 的 5s 内存缓存会让后一条剧本"页面根本没发请求"，于是对账拿不到数据。
- 规则：①**对账数据只能来自 `page.on('response')` 拦到的、页面自己发出的那次请求**，剧本另发的探针只用于"接口口径"这一类断言，并且必须在已有同源文档之后发（现在无同源直接抛 `TOOL:` 判 fail_env）；②按路径**拦截记录合并**（`collected()`）再比，列表页自己会追加请求（阅读器轮询、筛选重发），只拿某一条响应必然出现"DOM 比它多"的假红；③每轮剧本开新 page，别共用。
- **补刀（同日独立对抗审查查出）**：①里的"页面自己发出的"当时只是**口头**成立——响应事件与页面请求进的是同一个 store，而 `net.length = 0` 挡不住探针那条**晚到**的 push（记录要等 body 读完才进 store）。于是剧本发的请求会被当成"页面发过了"，`waitQuiet` 也会被它伪造成"页面已经静下来"。**方向要认清：这一类是假绿，比假红危险。** 现按 URL（`pathname + search` 全串，只比路径会把页面的同路径请求一起屏蔽）在事件回调里**同步**打 `by:'script'`，`collected/waitForApi/waitForApiWhere/waitQuiet` 一律只认 `byPage()`；探针在飞期间 body 未读完的记录也算"还在飞"，不许谎报安静。
- 案例：`tools/eval-e2e.cjs` 的 `attachNet/collected/api/scriptInflight/byPage` 四件套；回归锁 G5（每个 `file:line` 出处必须真指到含该 token 的那一行）、G6（剧本覆盖面不许缩水）。

### #47 选择器数错元素：一次"容差"其实是指错了节点（2026-09-19 实测）
- 症状：`/reader/` 文章行数量 34，接口给 30，差 4 → 我先加了 ±4 容差让它变绿。
- 根因：`div.cursor-pointer.flex.gap-2` 命中的是**侧栏源列表 34 项**，不是文章行；文章行实测是 `div.card.card-lift.p-3.cursor-pointer.relative`，与接口正好 30↔30。把"选错元素"包装成"容差"是端到端最隐蔽的一种假绿：数字看着合理，判据已经废了。同类还有 /hot/ 热搜事件视图——同一 class 既有含标题的外层也有只剩源芯片的内层，用"每行都出自响应"判会 100% 误报。
- 规则：①选择器必须实测，且**用零容差判据去验证它**（行数与接口条数相等、每行标题都能在响应里找到）；②给每条列表剧本加一条"选择器没落进侧栏/导航"的自检（`closest('aside')` 必须为 0）；③行结构有内外层之分时，判据换方向：文章卡视图判"渲染 ⊆ 响应"，事件榜判"响应 → 页面覆盖率"，别硬套一种；④页面会折叠（日报「展开剩余 N 条」）时，**总行数根本不适合当判据**，逐列 pill 才是。
- 案例：`tools/eval-e2e.cjs` 的 `SEL` 表（每条都带实测计数注释）、`subsetConsistency`/`responseCovered` 两个方向、E1 的"选择器不得落到侧栏"断言。

### #48 慢接口让 DOM 与响应错配：这不是竞态，是我读得太早（2026-09-19 实测，但**别拿这条掩盖真竞态**）
- 症状：E5 同一条判据三轮里两轮红——DOM 显示「全部 25150」，接口给的是 `counts.all=7291`。第一反应是"前端有竞态"。
- 根因：`/api/reading?tab=all` 首屏实测 15~30s，点「文章」后 `type=article` 约 5~26s；我"点完就取最后一条响应读数"，取到的是**上一轮还没落地的**响应。真正的判据顺序是：**先等到"参数对得上的那条响应"到达，再读数**。
- 规则：①按**参数**筛响应（`waitForApiWhere(r => r.params.type.value === 'article')`），不许用"最后一条 /api/reading"凑；②等不到就显式红并写明"等 N 秒没到"，让慢接口本身成为可见的事实（这条同时是 B31/B73 的行为证据）；③**反过来**：如果做完②之后 DOM 仍与那次响应对不上，那才是产品的锅，要单独立项——本轮确实查出一个真缺陷（晚到的旧响应覆盖新筛选），已按 #49 处理，**不能用"我等久一点"把真竞态调成绿**。
- 案例：`tools/eval-e2e.cjs` E5 的 `waitForApiWhere` + `c.metrics.readingResponses`（把每轮 type/counts 都留档，红的时候能一眼看出是哪种）。

### #49 端到端引擎第一次抓到真缺陷：晚到的旧响应把筛选结果整屏覆盖（2026-09-19，B74）
- 症状：点「文章」后页面计数 pill 仍是「全部」的数字；点「视频」（云端收藏视频为 0）列表还挂着上一批 30 行。
- 根因：`MyReadingPage.fetchPage` 只有 `if (loadingRef.current) return` 一道闸。首屏 `type=all` 慢请求晚于用户筛选的快响应到达时，`setItems/setCounts` 无守卫地整体覆盖回去；而加载中点的筛选更会被直接吞掉。接口侧 counts 与列表是同一份表达式（B60 已修），所以这是**纯前端时序**缺陷——白盒与接口巡检都看不见，只有真点页面才看得见。
- 修法：每次请求领一个序号，成功与失败两条路径落地前都比对"我还是最新那次吗"；防重入只保留给翻页（`if (!reset && loadingRef.current) return`）。
- 规则：①凡是"筛选/搜索 + 分页"的列表，**响应落地必须有序号或 AbortController**，这是默认要求不是优化项；②这类缺陷只能由端到端层抓到，所以端到端必须真的点交互（只截图等于没测）；③锁要判行为数量：`seq !== seqRef.current` 必须**恰好两处**（成功+失败路径），只数一处就是恒真断言——G9 里配了"删掉任一守卫探针必须失败"的反向验证。
- 案例：`web/src/pages/MyReadingPage.jsx` 的 `seqRef`；回归锁 G9；端到端剧本 E5；`docs/ISSUES.md` B74。

### #50 永远不会红的判据就是假门禁：写死真、两侧恒等、读不到就返回空集（2026-09-19 独立对抗审查查出）
- 症状：三条看起来"有断言"的行，实际**任何产品状态下都绿**：①E7 `assert(c,'data','客户端路由…', nav1 === nav0)` ——`performance.getEntriesByType('navigation')` 在每个新 document 里长度都是 1，整页刷新与不刷新两侧恒等；②E10 `assert(c,'data','未知路径不返回 200 HTML 兜底', true, '只登记事实')` ——字面量 true；③`dictKeys()` 读不到 `i18n.jsx` 时 `catch { return [] }` ——空字典让 E8 的"无裸 key 上屏"永远满分。
- 根因：判据的**方向**写成了"记录一个事实"而不是"这件事可能不成立"。同一类还有把 `status 0`（fetch 根本没到服务端）当产品红报——那是环境不通，判错方向会逼人去找不存在的前端 bug。
- 规则：①每条判据写之前先问"什么情况下它会红？"答不出来就删掉或换成会翻转的观测量（E7 改成 **window 标记存活 + document 只拉一次**：硬刷新必然清掉标记）；②机器扫一遍剧本表源码禁止 `assert(c,'kind','…',true,…)` 与两侧同值式判据（自检里有这两条，且**只扫剧本表区间**——扫全文会让探针命中自己的源码，这本身就是一次踩坑）；③工具侧"读不到依据"必须是 `fail_env` 抛错，禁止用空集合兜底把检查变成常绿；④`status 0` 一律 `TOOL:` 抛错归 fail_env。
- 案例：`tools/eval-e2e.cjs` E7/E10/`dictKeys`/`api`；自检探针「剧本里不许有写死真的判据」「dictKeys: 解析真实 i18n 字典且不少于 50 键」「api: status 0 抛 TOOL」。
- **补②（2026-09-20，B94 测量阶段自打）**：**包含判断写成前缀判断 = 永不命中的假绿**——取证脚本里 `LIKE '&amp;%'`（前缀）与 `LIKE '%&amp;%'`（包含）只差一个 %，却让我在同一张表上先得到「源名 0 条」、再从另一支脚本得到「6 条」。规则：①任何"有多少行受影响"的统计，**换一种写法再算一遍**，两个数不一致就是判据坏了、不是数据变了（本轮正是靠这个对账发现的）；②统计口径要把**清单本身**写进脚本（哪几个实体、是否包含匹配），否则下次换个实体集又得到一个数。

### #51 跑过 ≠ 验收过：`--only E1 --fast` 全绿也会 exit 0（2026-09-19 独立对抗审查查出）
- 症状：端到端默认跑全剧本 ×3 轮 ×线上，但 `--only`/`--fast`/`--target 127.0.0.1` 三条路都能跑出一条"绿 + 退出码 0"的记录，而这句话一旦写进交付说明就等于用一条探针兑了"端到端已验收"。
- 根因：退出码只表达"跑到的那些有没有红"，没表达"**这一轮够不够格叫验收**"。我自己本轮已经犯过两次"把跑过当验收过"（漏冒烟、漏文档同步），所以这不是假想风险。
- 规则：①验收轮定义写死为「全剧本 × ≥3 轮 × 真实云端」，三条缺一即 `NOT_ACCEPTANCE` 且**永不返回 0**（返回 2），即便全绿；②`env_lock.json` 与 `report.json` 都落 `acceptance:{ok,reasons}`，交付说明引用端到端前先读这个字段；③`known_gap` 豁免必须校验编号真的登记在 `docs/ISSUES.md`，未登记的豁免等于删剧本，直接 fail_env 中止；④同一口径适用于其它层：白盒/过程/内容检查的自检项数以 `--self-test` 输出为准，文档不复制条数（防漂移）。
- 案例：`tools/eval-e2e.cjs` 的 `acceptanceOf/gapIssues`；`docs/EVAL_GUIDE.md` §3.7；AGENTS.md §3 第 10 条子项。

### #52 回归测试写生产数据，留下的最坏形态不是"脏值"而是**悬空引用**（2026-09-19 线上实测，B78/B79）
- 症状：`GET /api/mybrief` 线上回 `{"ok":true,"empty":"no-subscription"}` —— 用户的「我的早报」整页退化成引导态，但库里 `settings.subscription.ids` 看着完全正常（`[2104]`，非空、类型对、能 JSON 解析）。
- 根因：`regression-my-brief` 会**在真实 Turso 里建一个 TEST 源**、把 `subscription.ids` 指向它，测后 `after()` 删掉源行、再把 `subscription.ids` 还原成"进测试前快照到的值"。只要某次运行在快照之后、还原之前被打断（Ctrl-C、超时、进程被杀），**还原的就是那个 TEST id**，而源行已经删了 →  settings 里留下一个不存在的 id。读层 `resolveSubscriptionIds` 对"键缺失/空数组"有兜底，对"声明了但全部落空"**没有**，于是直接把整页打死。（**是哪一次中断留下的没有日志可查**，这里只声称机制成立，不声称还原了现场经过。）
- 规则：①测试**不许**在生产库里"建实体 + 让别的表引用它"——要么全程本地文件库（H 组的做法：`file:` + 子进程冷缓存），要么引用只用到测试自己可见的临时键；②`after()` 的还原必须是"删掉我建的东西 + 把引用也清掉"，而不是"写回一个我快照来的值"（快照本身可能已经被上一次中断污染）；③凡是"id 列表"型设置，读层必须对**引用完整性**负责：全部落空要与空集合同等对待，部分落空要忽略坏 id（别让一个坏 id 打死整页）；④这类损伤**接口巡检看不出来**（`/api/mybrief` 回 200 且 `ok:true`），只有按用户语义断言"应该拿到 report 或明确的空态"才看得见。
- 案例：`lib/source-axes.js#resolveSubscriptionIds`（兜底补齐）、`tests/regression-20260919h.test.js` H6/H7/H8、`docs/ISSUES.md` B78（代码已修）/B79（生产数据修复等你授权）。

### #53 判据自己骗自己（第二次和第三次）：清点把首屏响应清掉了 + 又用绝对长度当门槛（2026-09-19 验收轮抓到）
- 症状：`--only E4,E9` 之外的一切正常，但**验收轮 E4 报「AI 精选 页面自己的请求返回非空〔没拦到〕而 DOM 有 29 行」**、E9 报「详情面板出现且含被点标题〔面板字数=297〕」。
- 根因（两条都不是产品缺陷，两条都是判据写错，而且都是**我们自己写过又犯**的同类）：
  ①**E4 是假绿显形**：剧本在点 tab 前 `net.length = 0`，可 `/hot/` 首屏其实已经把该视图的数据取回来了（SPA 切回同一个 tab 不会再发请求），于是"页面自己的请求"永远拦不到；而**在 `by:'script'` 修好之前这一条一直是绿的**——`waitForApi` 拿到的是上一条**探针自己**的响应（脚本自己发、自己证明页面发过）。给 #46 补的那一刀，正好把这处旧假绿照了出来。
  ②**E9 是拍的门槛**：`pane.len > 300` 是绝对值，而今日流里 `content_html` 明文只有 112 字的薄正文条目真实存在（面板 297 字 = 标题 + 元信息 + 那 112 字），把正常渲染判成缺陷。同一个文件上面就写着"拿一个拍出来的长度当门槛会把正常渲染判成缺陷"——**写过不等于不会再犯**。
- 规则：①剧本内的 `net` 只在"进这一轮之前"清一次；**断言前清 store 等于把自己要用的证据扔掉**，需要"点击后新发的那条"时按**参数**筛整轮记录，而不是清空后等；②涉及"渲染是否接住了数据"的长度类判据一律用**相对量**（面板 ≥ 正文明文的一半），绝对门槛必须先给出"这一族条目实测长度分布"作依据；③失败信息里必须带上两侧读数（面板字数 / 正文明文 / 标题头），否则红一次就要重跑一轮才能定位——本轮就是这么绕的；④**新加的排除逻辑会让旧假绿显形**：给评测器补一刀之后，验收轮的红灯要当"第一次真测"读，不要当"突然坏了"读。
- 案例：`tools/eval-e2e.cjs` E4（`waitForApiWhere` 按 `params.tab` 筛整轮 + 不再清点）、E9（相对判据 + 读数字样进 detail）；探针轮实测 `--only E4,E9 --fast` 由红转绿，且因非验收轮退 2（见 #51）。
- **补⑤（同一条验收轮的第三次红）**：判据只写了"有内容"这一条腿，**没写产物页的第二合法态**。E8 用 `>500 字` 判"不白屏"，而 `/mybrief/` 在第 3 轮渲染的是 **218 字的合法空态**（B76 归一后页面正常显示「今天订阅源没有新的精选内容」），于是 pass/pass/fail_product → 整轮退 2。规则：**凡是"页面应当有内容"的判据，都要先问"这个产物有没有合法的空态文案"**，有就把两条腿一起写进判据（`>500 字` **或** 命中显式空态文案），并把命中的空态记进 `metrics.emptyPages` 让它是可见事实而不是被吸收掉的绿。


### #57 仓库是 CRLF：文本型锁与"注入型探针"里的 `{\n` 永不命中，于是假绿（2026-09-19 自查 I10 时撞上）
- 症状：给 I10 做负向自检（把重统计塞回首屏，期望锁变红）。三例注入里两例回报"pass"，第一反应是"锁没拦住、判据漏了"。真实情况是**注入根本没发生**：`s.replace('async function handleStatus(req) {\n', …)` 里的 `\n` 在 CRLF 文件里前面还夹着一个 `\r`，模式永不匹配，`replace` 静默返回原文 → 被测文件没变 → 测试当然绿。
- 根因：①仓库源码整体 CRLF（`api/[...slug].js` 实测 2924/2925 行带 `\r`），而判据/探针的模式串按 LF 心智写；②`String.replace` 不报错——**"没匹配上"和"匹配上但结果一样"在返回值上无法区分**，探针 therefore 必须自带"改动是否落进文件"的断言。同一族还有一例：I10 旧版拿"第一个行首 `}`"当函数右边界，被函数内的 `} catch {` 截断 → 把自己的合法 SQL 判成逃逸（假红）。
- 规则：①任何"改源码来验证锁会不会红"的探针，写回前必须断言 `next !== orig && next.includes(标记)`，不成立就报**「探针自身失效」**而不是报锁失效；②切函数/块边界禁止手写分隔符，统一用 `lib/src-spans.js`（按"下一个顶层声明"切、按 `/\r?\n/` 分行、剥整行注释），锁只依赖它的行为；③判据里凡出现 `\n` 字面量，先自问"这个文件是 CRLF 会怎样"；④负向自检必须**逐条**注入并逐条报结果，"三例两红"不叫证据。
- 案例：`tools/_probe-i10-selftest.cjs`（三例注入 + 复原，末行打印"探针成立：三例全红、复原后全绿"）；`lib/src-spans.js`；行为锁 `tests/regression-20260919i.test.js` I13（同一份样例分别按 CRLF/LF 切，两侧 body 必须逐字相等）。

### #58 "手工列清单"的判据必然漏：B20 门槛在三次审查里栽于同一类（2026-09-19）
- 症状：B20（入报质量门槛）先被记成"runner 已修"，独立对抗审查查出"三份生成器只接了一份"，本轮按 `GENS = [collect-turso, api/daily-generate, server/daily]` 逐个点名后仍判绿——而线上最新一期 `id=103` 实测带着 **29 分与 22 分**两条低质条目。真实写入点是 **5 个**：漏掉的两个是 `tools/collect-turso.js#runDaily`（裸报告，与已接的 `runDailyAi` 同文件）和 `api/[...slug].js#generateDailyInline`（读层内联兜底，压根不在清单里）。
- 根因：三层叠加——①清单靠人记，新增写入点不会通知清单；②判据是**文件级**（"该文件里出现过 `passesDailyQualityGate`"），同文件的第二个函数天然被算成已接；③`id=103` 的 `stats` 形状 `{candidates,articles,sections,totalItems}` 就是内联兜底那一份的指纹，但没人拿产物反查生成器。
- 规则：①判据从**事实本身反查**：扫 `INSERT INTO daily_reports` 的每一处出现 → 用 `lib/src-spans.ownerAt` 找宿主函数 → 要求该函数体内有门槛调用；②反查结果必须**同时断言数量**（`writers.length >= 5`），否则"扫不到东西"也算过；③判据文件必须**排除自身**（本文件里那个字面量会命中自己的扫描，见「自检探针须排除自身」）；④凡"多份实现同一语义"的不变量（采集/清洗/门槛/栏目表），一律用这种"扫真实调用点"的写法，不许维护文件清单。
- 案例：`tools/eval-whitebox.cjs` W14（从 `INSERT INTO daily_reports` 反查 5 个写入点并逐个核对门槛）、W13（栏目表副本反查，同族）；`docs/ISSUES.md` B20（本轮由 ✅ 回退为"待下一批日报验证"）。

### #59 "从事实反查"仍然可以只覆盖一半事实（2026-09-19 第二轮对抗审查，#58 的续集）
- 症状：按 #58 把 W14 改成"扫 `INSERT INTO daily_reports` 反查宿主函数"之后，我以为判据已经不靠人记了。第二轮审查证明**它仍然有三处能绿**：① 扫描范围写的是"四个目录"（`server/api/tools/lib`）——这本身就是一张手写清单，`scripts/ cloud/ portal/ web/` 全不在内；② 按"函数名"去重后，**同一函数里的第二条 INSERT 不再单独判定**；③ 判据只看"函数体里出现过 `passesDailyQualityGate` 这个字面量"——把返回值扔掉（`guards.applyDailyQualityGate(list, …);` 不作赋值）、或只在**行尾注释**里留一句"门槛已接"，都照样绿。同一轮还查出我把 `tools/_probe-i10-selftest.cjs` 写进坑的"案例"里，而那支脚本**根本没提交**（判据的证据在仓库里不存在）。
- 根因：**"反查"是一个方向，不是一种完成度。** 三处漏洞的共同点是判据在自己的实现里又留了新的隐含假设（目录范围、去重键、字面量存在＝行为发生），而这些假设不会报错，只会一直绿。与坑 #42/#46/#50/#53 同一个病：**判据的覆盖面必须由被检事实决定，不能由我实现的方便程度决定。**
- 规则：①反查类判据必须**全仓扫**，排除项只允许"非运行代码"（依赖/产物/归档）与"测试夹具"两类，且**每条排除都要写理由并在锁里反向断言**（`I15` 现在用临时目录造 5 种样本，逐项钉"该看见的要看见、不该参与的不许参与"）；②同一函数内**每一条**写入语句单独判定，不许按函数名去重；③判据要判的是**行为后果**而不是名字存在——门槛类统一收敛成一个"结果必须被赋值回去"的形态（`= x.applyDailyQualityGate(`），并把五份写入器共用的"读配置→过滤→出声"整段收进 `lib/brief-guards#applyDailyQualityGate` 唯一实现（配置坏值不再各自解释）；④**行尾注释也算注释**：剥注释用字符状态机而不是正则（`lib/src-spans#stripComments`），并用 `node tools/_probe-strip-selftest.cjs` 反向自证"剥完 212 个源文件没有一个语法被破坏"——剥注释的判据自己先要不吃代码；⑤探针与判据**必须共用同一套事实**：`tools/_probe-w14-selftest.cjs` 现在从 `lib/daily-writers` 取目标清单（上一版它硬编码了 5 个函数名，等于判据与取证两套事实）；⑥写进文档的案例文件必须真在仓库里——落档前先 `git status` 看它是不是 `??`。
- 残余面（明说，不假装全覆盖）：`archive/`、`tests/` 里新增的产品级写入点 W14 看不见；经 `handlers.get(k)()` 之类动态派发、或运行时拼出的表名，静态判据也抓不到——这三条要靠端到端/巡检的行为层兜，不靠 W14。
- 案例：`lib/daily-writers.js`（派生清单唯一实现）、`lib/src-spans.js#stripComments`、`tools/eval-whitebox.cjs` W14、`tools/_probe-w14-selftest.cjs`（5 写入点 × 3 种摘法 = 15 例全红且点名）、锁 `I15`（临时目录造样本，含"扔掉返回值不许算已接"）。

### #61 先改代码后补锁 → F2P 取不到"改前红"，看起来像锁是假的（2026-09-19 本轮实测）
- 症状：B93 的 bilibili 那半边补完后，给锁 I14 出 F2P 证据时按 `--auto-base`（反查锁的引入提交 = `c46fd89`，基线 `5eece48`）跑出**「改前也全绿 → F2P 不成立，按 §6 应删掉或重写断言」**。但断言不是假的——是**修复(5eece48)与锁(c46fd89)落在了两个提交里，锁的引入提交的上一条已经没有 bug 可抓**。
- 根因：`eval:f2p` 的基线定义是"锁引入提交的父提交"，它隐含假设**锁与修复同批落地**。把锁留到下一批，"改前红"这个事实就在 git 里消失了，工具只能报"锁抓不到 bug"。
- 规则：①**锁与它保护的修复必须同一个提交**（这是 §3.9 的隐含前提，之前没写下来）；②已经犯了的话，**不许**为了让 F2P 变红去弱化断言，也不许删锁——正确做法是**显式 `--base` 指到"修复之前"的那个提交**（本轮：`--base 6665087` → 改前红 1 条 / 改后全绿，证据 `docs/eval/f2p/2026-09-19173056.json`），并在交付说明里写清基线是手动回拨的、原因就是这个；③给"新抽出来的判据基建"（如 `lib/src-spans` / `lib/daily-writers` 的行为锁 I13/I15）出证时，F2P 不适用（基线上根本没有被 require 的文件，只会以"整文件加载失败"的形式红），这类锁的证据是**负向探针**（把真实产物改坏，判据必须逐例翻转），别硬套 F2P 然后把"改前不红"当成锁的问题。
- 案例：`tools/eval-f2p.cjs` 的 `--auto-base`；`docs/eval/f2p/2026-09-19173056.json`（I14 手动回拨基线）、`2026-09-19173201.json`（门槛配置锁同理）；`tools/_probe-w14-selftest.cjs` / `_probe-strip-selftest.cjs`（基建类锁的负向取证）。
