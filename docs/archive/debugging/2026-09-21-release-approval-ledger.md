# 放行清单（2026-09-19 夜汇总 → 09-20 收口 → 09-21 整表放行并出账）

> 类别：调试/交付记录 | 归档自：`docs/ISSUES.md`「⛔ 待你点头的放行清单」整节（15 行，原样照档，未改一字） | 归档轮次：2026-09-21 文档整理轮
> 关联代码：`lib/{retention,ai-throttle,brief-guards}.js`、`tools/{eval-f2p,eval-whitebox,eval-e2e,doc-lint,collect-turso}.cjs|js`、`server/services/datamgr.js`、`api/collect.js`
> 关联坑：`docs/pitfalls/testing.md` #61/#62/#63/#64/#65/#66/#67 | 关联 spec：`docs/specs/43-collect-retention-safety/spec.md`、`docs/specs/41-e2e-whitebox-eval/`、`docs/specs/{36,37,38,39,40}-*/` | 取代：无
> 状态：**已放行并出账**（用户 2026-09-21：「放行清单整块移出成新的文档归档」「如果要批准我直接全部批准」）。本件不是"修好的功能"而是**授权台账**，它的生命周期在整表点头那一刻结束；表中未闭事项一律以 B 编号行继续活在 `docs/ISSUES.md`，落点见 §三。

## 一、为什么移出（§2.3：归档 = 从现役位搬到归档位 + 原位留一行指针）

- 这张表的用途是**一次性收集授权**。用户 09-20 已回「我已都放行」、09-21 再确认整表放行，此后每行的 `等什么` 都只能读作"已放行"——一张不再提问的表留在《活跃问题 SSOT》里，会让下一个接手的人误以为还有十五件事在等人点头。
- 表里 15 行的实质内容（缺陷定性、取证、修法）在 `docs/ISSUES.md` 的 B 编号行与 `docs/specs/43` 里**各已有一份**，本表只是它们的授权视图；按 AGENTS §2.5 与 §4.1，同一易变事实两处写死必然分叉（本表 #14 行自己就记着"同一条待办变长"）。
- 三条衍生自**被否决的全量审计轮**、用户 09-21 指示冻结的动作，不随本件复活：见 §四。

## 二、原样照档（逐字未改）

## ⛔ 待你点头的放行清单（09-19 夜汇总，09-20 收口；按"再等下去谁最疼"排序）

> **现状（09-20 夜）：用户已对本表全部条目放行**（原话"我已都放行"）。**8 项已收口**（#0 换库 / #1 B104 / #2 B106+B114 / #3 B102 / #6 BL7 / #7 BL8 / #11 push / #14 ignore），**7 项排队待做**（#4 #5 #8 #10 #12 #13 在我这边，#9 的勾选动作在你那边）；`等什么` 一列自本行起一律读作"已放行"，不逐行改写（避免同一事实十五处漂移）。
> **放行 ≠ 已做完，也 ≠ 免掉产品选择**：#5 已定为"接回每日清理，7 天 + 豁免"；#13 仍要一句"preview 改不改 `GET`"；#8 按本表排序**必须排在 #4、#5 之后**。
> 这一节只是把散在 BL/B/⏸ 里的授权项拢成一处，**不含任何新判定**；每条的完整证据在对应行与小 spec 里。
> 你醒了只要回一句「按 1→2→3 顺序做」也行，我按表里"第一步"那列开工，不越界。

| # | 等什么 | 再等的代价（可核） | 我拿到许可后的第一步 |
|---|---|---|---|
| 0 | ~~P0 Turso 整库读封锁~~ **已收口**：换库 + 三处凭据同步 + runner/线上双复验（取证与代价全在 **B118** 与 `docs/HANDOVER.md` §2.1；操作口径在 `docs/RUNBOOK.md` §10.9） | — | 只剩你侧动作：轮换旧 `TURSO_AUTH_TOKEN` 与 `ghp_` PAT（见 B122/B125） |
| 1 | ~~**B104**：给坑 #62/#63/#64 各配 `tests/` 锁（同批，坑 #61）~~ **已收口（09-20 夜）** | 曾：`npm test` 必现红（G3 下游于 W9）→ 回归网失去"没弄坏东西"的判定能力 | ✅ 锁在 `tests/regression-eval-substrate.test.js`（七条，含负向样本）；当轮复跑 **whitebox 16/16** + **`npm test` 454/454**，两条真实退出码均 0。读数、取证口径与**两条残余**（W9 判据本身仍"注释即算绿"）见本文 B104 行 |
| 2 | ~~**B106 + B114**：修 `eval-f2p` 的 env/product 误判 + F2P 条数与文档脱钩~~ **✅ 09-20 夜已收口** | 曾：**任何"改前必红"证据不可信**，且工具会建议删掉 13 条好锁（方向是删安全网） | ✅ 判定链加"红项全是文件名形态且零目标命中 ⇒ env/退 2"（旧账 4 份按新判据被 `--ledger` 逐份标出）；`--ledger` 成常驻子命令；重跑时区族 13 条锁 → **用例名级改前红 13/13、改后 13 条全绿、退 0**（`docs/eval/f2p/2026-09-20135512.json`）；同批立**坑 #65**（号位原为 B106 预留）+ `tests/` 锁 #65-1（坑 #61 同批）。**我自己反向犯过一次**：把"基线缺本轮新建文件"单独判 env，被 `docs/EVAL_GUIDE.md` §6 的收敛型取证规则与一条旧自检探针当场挡住（见 #65 规则③）。剩一件：`lint:docs` 加"引用 F2P 条数必须带证据文件名"，并入放行表 #12 |
| 3 | ~~**spec 43 D1 / B102**：本地清理对齐云端豁免~~ **✅ 09-20 夜已收口** | 曾：本地服务一起**满 24h 就删 39,458/43,214 文章（91%）+ 1,161/1,161 视频播客（全部）**（09-20 只读复测数，比原记的更进一层） | ✅ 用户口径改为**本地不删内容**（本地是灾备副本）：谓词收进 `lib/retention.js` 一份，`local` 作用域 articles/videos = `skip`、只清 `pending_items`/`daily_reports`；三端（`server/services/datamgr.js`、`tools/collect-turso.js`、`api/collect.js`）改读同一份；**W17 已进门**（判据 `findRetentionViolations`，白盒与锁 R4 同源）；锁 `tests/regression-retention.test.js` R1~R4（R2 往临时文件库真插真删 + 摘豁免对照）。**取证口径**：本批属"基线无对应形态"的新增门禁，按坑 #64 规则②不套 F2P 改前红，证据 = R1/R2/R3 里的突变对照（把 videos 列入可删、摘掉 later 豁免，判据必须翻）。**残余**：`api/[...slug].js` 的 `ARTICLE_CLEAN_WHERE` 是第三份谓词，该文件有并行会话在改，本轮未收口——W17 对它按**整文件**记账豁免，所以那文件里新写的第二份谓词会被一起放过（已知空洞，随 #4/#8 收） |
| 4 | **D3 / B103**：先做**内容级**转储 | 云端 `/api/backup` 只存配置三表；本地快照停在 09-06 → **删错了无处可回** | 写 `articles`/`videos` 导出转储 + 本地新快照，之后才允许任何"执行删除"的验证 |
| 5 | **D4**：>7d 未读那 **10,733 条**到底删不删、保留几天 | 保留清理一直挂着不跑 = 文档说的"每天 04:13 清理"继续是假的；库也继续涨 | 按你的选择：接回触发口**或**明确写"当前不删"，两处（`collect.yml` + 三处调度文档）一起定稿 |
| 6 | ~~BL7 恢复报警出口~~ **已转绿**（换库副产物，非授权写入）：渠道有真实出口 + 最新报警 `05:07` 真的送达 | 残余：旧库 `settings.alerts` 仍是哨兵值，将来从旧库迁 settings 会把哨兵带回来 | 见 **BL7-更正** 行；关闭条件补一条"渠道 URL 非哨兵"回归锁（属白盒新增，等放行） |
| 7 | ~~BL8 `ai.minIntervalMs` 复位~~ **前提被推翻，已按实情收口**：该键不存在，产品一直取默认 4000ms；假的是 preflight 的 `Number(null)=0` | — | 已落 `lib/ai-throttle.js` 下限保护 + 判据共用同一份算术；preflight 9/9。详见 **BL8-更正** 行 |
| 8 | **BL10 / B15**：清 `'null'` 污染（2.5 万行级） | | **必须排在 #4、#5 之后**——清 `read_at` 会让这 2.5 万篇重新变成"可被删"，先有转储与决策再动 |
| 9 | **B79**：生产 `subscription.ids=[2104]`（实读仍悬空） | "我的早报"继续以"重点源报"的面目出现 | 你后台重新勾选（安全）或授权我按现存 8 条 `focus=1` 写回（写前备份） |
| 10 | **三端一致性 + 配置卫生的剩余小刺，一行一号**：**B107** 噪声口径手写 14 处且 `/api/reading` 没套 → **B108** `gen-dir-index` 不写自动段，与 AGENTS §2.8 不符 → **B109** 本地两个幽灵事件开关（假开关家族）→ **B111** 翻译 prompt 3~4 份事实源（prompt 那条是 B111，不是 B107/B112）→ **B112** `schemaVersion` 落库类型混杂 → **B113** 审计产物里一条明文飞书 webhook → **B115** 254 条锚点里 5 条已烂 + `lint:docs` 不查行号 → **B116** 父 spec 背景层按作废前提排期 | 三端一致性债继续滚（噪声 20 处、幽灵事件键、prompt 3~4 份、类型混杂、索引器与 §2.8 不符、文档不实）+ **B113 是凭据卫生**：真实飞书 webhook 明文在**未跟踪但没被 ignore** 的 `docs/eval/audit/schedule.json` 里，一次 `git add docs/` 就会提交出去（我按路径逐个 add，没碰它） | 一句「可以改代码」即可按 1→9 逐条排队，每条都带"坏样本必红"的负向自证。**B113 有一步不需要改代码**：给 `.gitignore` 加一行 `docs/eval/audit/`，你说"加"我就加 |
| 11 | ~~push 许可~~ **已推**（用户选「推」）：`961fec0 → c183413`，含 `210a07b`（cover 一行）与 `fc3ae51`（下限保护） | 再积累未推提交即重新成为待办 | 取法：`git rev-list --count origin/main..HEAD` |
| 12 | **文档门禁扩面四条**（B114 ② + B115 ① ② + B116 + B126）：①"引用 F2P 条数必须同句带证据文件名"；②`file:line` **行号越界**判据（硬前置：能区分"活锚点"与"作为反例被引用的死锚点"）；③"父 spec 背景段的实测断言必须带 B 编号或指路"；④**表格每行格数必须等于表头**（本轮实测：破格行渲染成多一列而门禁全绿，见 B126） | 本轮六轮人工复核抓到的 B104/B112/B113/B114/B115/B116/B117 **全是同一件事**：文档说的和代码/证据做的不一样，而现在没有任何自动门禁抓得住——全靠我一轮轮手工对，且**下一轮会话不会记得这些**。 | 一次改 `tools/doc-lint.cjs` 四条判据，每条配负向自证（塞入越界锚点/无出处条数/裸断言/多一格必须变红），与三条 `tests/` 锁同批提交（坑 #61） |
| 13 | **B117**：把 `regression-20260918` 那条**向生产发的 `DELETE`**（`/api/weekly/archive/999999` + 真 token）搬到隔离库，另加 20260913b 的 preview 调用同批搬 | 生产数据当前**没被删过**（已逐份证死），但这份测试的安全性只押在"999999 这个期号恰好不存在"上，**不是押在隔离上**——这是"下一次 BL7"的现成形状。且它换来的信息只有"路由可达"，性价比极低 | 改成隔离库正负两条断言（存在→删并回 `removed`；不存在→404）+ 补"子进程必须被指到本地文件库"断言；顺带给 #12 的第③条判据提供真样本。**另需你单独拍板**：`/api/data/cleanup/preview` 是否改成 `GET`（现在用 POST 表示"只数不删"） |
| 14 | **一次性取证脚手架的处置**：`tools/_f2p-ledger.cjs`、`tools/_cite-audit.cjs`、`tools/_test-write-exposure.cjs` 三支探针**未跟踪也没被 ignore**（`git check-ignore` 实测三个全 NOT IGNORED），里面不含凭据（只做正则与路径统计），但一次 `git add tools/` 就把三支一次性脚本带进历史 | 与 B113 同一形状（未 ignore 的落盘产物） | 三条出路任选：①按放行表 #2/#12 做成常驻命令（`--ledger` / `npm run lint:cites` / 测试写入面判据）后删掉探针 ②现在就给 `.gitignore` 加 `tools/_*.cjs` 一行（与 B113 的 `docs/eval/audit/` 同批，**零代码**）③保持现状（本文档已写明它们的存在与取法）。**09-20 增量（同一条待办变长，不是解决了）**：本轮又留了整目录脚手架 `.tmpchk/`（探针 + GH Actions 日志 + `/api/*` 响应），已补 `.gitignore` 一行（`git check-ignore` 实测命中 `.gitignore:61`）；上面三支 `tools/_*.cjs` 已按**选项②**落地（09-20 夜，用户点头）：`.gitignore` 加 `docs/eval/audit/` + `tools/_*.cjs` 两行，`git check-ignore` 实测命中 4 条（`_f2p-ledger` / `_cite-audit` / `_test-write-exposure` / `_diag-thin-body`）。**边界必须写清，别读成"历史清掉了"**：本仓已有 **22 份 `tools/_*.cjs` 是跟踪状态**（含 FEATURE_MATRIX 当跑法引用的 `_probe-*-selftest.cjs`），ignore 只挡今后新增，不撤销历史；今后要常驻的判据仍应按选项①做成 npm 子命令，而不是靠 `_` 前缀混进同一命名空间（否则"一次性"与"常驻"再分不开） |


## 三、放行后的落点（本件唯一新增的判断）

| 原表行 | 放行后去了哪 |
|---|---|
| #0 换库止血 | 已收口；剩你侧动作 = 轮换旧 `TURSO_AUTH_TOKEN` 与 `ghp_` PAT（挂在 B118/B122/B125） |
| #1 B104 | 已收口（`tests/regression-eval-substrate.test.js` 八条 + whitebox 16/16）。未闭残余：**W9 判据本身仍"注释即算绿"**，随 §三·门禁扩面批 |
| #2 B106 + B114 | 已收口（`eval:f2p` 判定链 + `--ledger` 常驻 + 坑 #65 + 时区族 13 条重新出证）。未闭残余：**"F2P 条数须带证据文件名"判据**，随门禁扩面批 |
| #3 B102 | 已收口（`lib/retention.js` 唯一实现 + 白盒 W17 + 锁 R1~R4，本地不再删内容）。未闭残余：**`api/[...slug].js:2010` 的第三份删除谓词**（W17 对它按整文件豁免） |
| #4 B103 内容级转储 | **待做**，任何"执行删除"验证的前置闸；不许走 `PUT /api/backup` 单键方案 |
| #5 B101 保留策略定稿 | **待做**，口径已由用户定为「接回每日清理，7 天 + 豁免」；缺的只是 `cleanup` 触发口与三处文档定稿 |
| #6 BL7 报警出口 | 已转绿（换库副产物，非授权写入）。残余 = 旧库 `settings.alerts` 仍是哨兵 + 渠道形状无回归锁 |
| #7 BL8 | 前提被推翻并按实情收口（`lib/ai-throttle.js` 下限 + preflight 同算术 + 锁四条），无后续动作 |
| #8 BL10 / B15 `'null'` 订正 | **待做**，硬排在 #4、#5 之后（清 `read_at` 会让 2.5 万篇重新变成"可被删"） |
| #9 B79 悬空订阅 id | **待做**，用户已选"自己在后台重新勾选"（安全可核对），我这边不写生产 |
| #10 三端小刺打包 | **待做**，B107 / B109 / B111 / B112 / B113 / B115 / B116 一行一号排队。**B108 已由并行会话按「撤义务」路径收口**（AGENTS 原 §2.8 删除 + `doc-lint` 第 7 条移除 + `gen-dir-index.cjs` 进隔离），故打包行少一条 |
| #11 push 许可 | 已推（`961fec0 → c183413`，其后再 4 次） |
| #12 门禁扩面四条 | ◐ **09-21 交付四条判据 + 新加一条**（`tools/doc-lint.cjs` 第 7~11 条：表格超格 B126 / 活锚点越界 B115① / F2P 条数须带证据 B114② / 父 spec 背景须带出处 B116③(提示级) / **同表编号唯一**——本轮实测抓到两个 B128 并存才加的)。判据自证 `npm run lint:docs:selftest` 31 例双向 + 锁 `tests/regression-doc-lint-rules.test.js`。**未做**（09-21 后续三轮已清大部分）：B115② 裸文件名要写全路径、`_cite-audit.cjs` 转常驻 `npm run lint:cites` —— 这两条仍待做。✅ 已交付：B127 两条（过程层读数落进 `report.json.process`；`--report` 先验输入 schema，喂错退 2 且说清认成了什么，`fail_env` 也改退 2）、W9 收紧（只认 `test()` 标题/断言消息里的 `#N`，注释不算）、B124 拟号标注与打包行拆分（`EVAL_GUIDE` §4.2 已标（拟）+ `W5`/`W8` 登记废弃）。另加两条本批自己撞出来的判据：第 7b 条「表格断裂」（表头与分隔行并成一行会让整张表脱离格数判据）与第 10 条「同表编号唯一」（实测两个 B128 并存） |
| #13 B117 测试绑生产 | **待做**：三份仍绑生产 Turso 的测试搬隔离库 + 新判据"测试里出现 `DELETE`/`POST` 未指 `file:` 即红"。其中「`preview` 改 `GET`」随 §四 冻结 |
| #14 一次性脚手架处置 | 已按选项②落地（`.gitignore` 加 `docs/eval/audit/` + `tools/_*.cjs` + `.tmpchk/`）。边界：只挡今后新增，已跟踪的 22 份 `tools/_*.cjs` 不动 |

## 四、随被否决审计轮冻结、不随本表复活的三条

- **`POST /api/data/cleanup/preview` 改 `GET`**（原表 #13 附问）：与被否决的全量审计轮同批冻结，不再追问。
- **扩 `doc-lint` 扫 `.env.example` 的"非占位 secrets 键" + 重写 git 历史**（原 B80/AU-05 衍生）：冻结。**凭据本身仍要轮换**——B122/B125 是活跃风险行，不随本条冻结。
- **补 `.github/workflows/ci.yml`（push-CI）**：用户 09-21 明确「不补」，`npm test`/`lint:docs` 继续只在本地跑（现状见 `FEATURE_MATRIX.md` §1.5 末行）。
