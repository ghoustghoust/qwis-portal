# `docs/ISSUES.md` 核销轮移出件（2026-09-21）

> 类别：调试/交付记录 | 归档自：`docs/ISSUES.md` 四段整节（B27~B70 六域登记、🟡 观察中、已关闭挂案、阻塞项已闭行）+ 六条已核销单行 + 本轮被改写的四处头部 | 归档轮次：2026-09-21 文档整理轮
> 关联代码：`web/src/`（阅读器/后台/早报页渲染层）、`api/[...slug].js`、`lib/{reading-filters,media,brief-guards,daily-columns,ai-throttle,source-axes}.js`、`tools/{audit-cloud,eval-whitebox,eval-content}.cjs|js`、`server/routes/{health,hot,videos}.js` | 关联坑：`docs/pitfalls/testing.md` #45/#50/#59/#60、`docs/pitfalls/frontend.md` #54 | 关联 spec：`docs/specs/{36,37,38,39,40}-*/` | 取代：无（本件是"当时逐字登记的原话"；后续以各域 spec 与 `docs/ISSUES.md` 当前行为准）
> 状态：**已核销出账**。共同点：不再改变下一步决策。§一 的登记原文其根因与改法已在各域 spec 里各有一份（§2.5 禁止的第二事实源）；§三~§六 是已修完/已排除/已闭合的行；§七 是观察期已过的观察项与操作留痕（其中长期有效的四条规则已抄进活文档）；§八 是本轮为收敛口径而**改写**的头部原文，逐字留存以便反查"原来怎么写的"。

## 为什么这样切（§4.1：洁净 = 精炼 + 去冗 + 归档，被删原文逐字可反查）

- `docs/ISSUES.md` 头部自陈「只列**当前活跃**的问题」，但它同时装着 09-19 那一批 20 条页面批注的**登记流水**、观察期早已过去的观察项、和三条已划掉的阻塞项。打开缺陷账本先读到五十行完工记录，是账本失职。
- 域表自己的表头就写着「每条只留一行症状，根因与改法在对应域 spec（**详情唯一去处，避免两处腐烂**）」——但 B 编号同时出现在这里与 `docs/specs/{36,37,38,39,40}` 里。实测 B27~B70 逐号反查：**只有 B29 / B69 / B70 三条至今没有任何 spec 落点**，这三条逐字留在活文档，其余整节移出。
- 切片由 `.tmpchk/0921-split.cjs` 完成：保留行一律从原文件切出、不重新打字，落盘前逐行反查原文每一行都要么仍在活文档、要么能在本件里逐字找到。

## 一、B27~B70 六域登记原文（整节照档；B29/B69/B70 三条留在活文档故不在此列）

## 🆕 本轮 20 条页面批注实测新增（2026-09-19，B27~B58）

> 全部为**只读实测 + 线上 SQL/端点取证**后的判定；每条只留一行症状，根因与改法在对应域 spec（详情唯一去处，避免两处腐烂）。
> 用户批注 20 条 → 拆出 32 个可执行缺陷（一条批注常常牵出多个根因）。

### 域 A · 阅读器与「我的阅读」口径 → `docs/specs/36-reading-semantics/spec.md`

| # | 症状（一句） |
|---|---|
| B27 | 「未知日期」：云端 `/api/reading` 快路径漏 `AS date` 别名（`api/[...slug].js:1134-1141`），前端把字符串 `'unknown'` 按降序排到最前 |
| B28 | 文章/播客/视频筛选无效：`tabCond` 未加括号被 OR 吞（`:1173`）；同一响应 counts 走另一条 SQL → 数字与列表互相打脸；本地实现正确＝三端漂移 |
| **B60** | B29 的同类判定全库共 **6 份手写副本**（本地列表/本地计数/云端列表/云端计数/runner 日报/runner 周刊）→ 实测 `type=podcast` 列表 30 行但 `counts.all=0`；`type=article` 计数 6413 vs 真实 7282（869 篇公众号不进计数） |
| **B61** | runner 与日报那份播客判定比读层**少一个 `.opus`** → opus 播客单集永远进不了日报/周刊「视频与播客」栏 |
| B30 | 云端无任何 `UPDATE videos SET watched_at` 路径 → 视频/播客观看足迹永久丢失（只有本地 `server/routes/videos.js:81` 有） |
| B31 | 阅读器卡顿：`sort=smart` 表达式无索引（实测 11.4/13.6/15.3s）+ 列表无虚拟化、`web/src` 零 `memo()` + 每点开一篇文章重拉 545KB sources 重渲侧栏 |
| B32 | 「已读」口径：`GET /api/articles/:id` 即置 `read_at`（点开＝读完），且详情缓存命中不发请求 → 足迹不可信 |
| **B62** | B60 的前端尾巴：阅读页类型徽章自己再造一份分类判定（`MyReadingPage.jsx:22` 按 `sourceType==='douyin'` 猜播客），与已收敛的筛选口径互相矛盾 → 播客 Tab 筛得出条目、每条却写「文章」 |

### 域 B · 后台信息架构与功能隔离 → `docs/specs/38-admin-ia-refactor/spec.md`

| # | 症状 |
|---|---|
| B33 | 源库组合视图无搜索框；四轴编辑入口割裂在 5 处（`reader_visible`/`muted` 只能批量改）；组卡「订阅」无计数、无退订分支、无「仅已订阅」筛选 |
| B34 | **spotlight 双写冲突（潜伏，不是正在丢数据）**：`DailySettingsTab.jsx:96` → `PUT /api/settings/daily` 全量替换 spotlight，而两端实现（云端 `api/[...slug].js:2263`、本地 `server/routes/daily.js:133`）写的都是 `UPDATE sources SET spotlight = CASE WHEN id IN (…) THEN 1 ELSE 0 END`，**没有 WHERE 作用域** → 界面只列候选集内的源，保存时却把**候选集之外**（wemp 公众号、podcast）的 `spotlight=1` 一律清零，用户无从取消也就无从挽回。另：候选类型表现在有 **5 份副本**，而 `lib/reading-filters.js` 的文章类含 `wemp` → 同一概念两种答案。**09-19 线上实测暴露面**：`spotlight=1` 共 59 个（rss 37 / x 18 / youtube 3 / bilibili 1），**候选集之外当前是 0 个**；触发条件是"先给候选集外的源标重点 → 再去日报设置点保存"。**修法（待批准，属 38-C C2 + 38-D D3）**：候选集收进 `lib/` 一份唯一实现，全量替换的 UPDATE 必须 `WHERE type IN (候选集)` = "替换作用域 == 界面呈现集合"，两端同步改并配隔离库行为锁（局外源的 spotlight 必须存活） |
| B35 | 后台 header 内层 `max-w-[1100px]` 与内容区 1160/960 两套容器 → 左缘永久错位 54~70px（用户"看着别扭"的定量根因） |
| B36 | 全后台无 sticky/浮动保存条；保存语义三种并存（即时写 / 底部统一 / 分散多按钮）；无 dirty 未保存提示 |
| B37 | 调频与 failover 用 `window.prompt`/`confirm`；Tab 态纯 `useState` 无 URL 深链，刷新回源库 |
| B38 | 请求瀑布：`/api/settings` 同页被调两次、`/api/settings/daily` 120KB/4.6s、热点页拉 `/api/sources?type=rss` **451KB/4.3s** 只为挑一个 AIHOT 源 |

### 域 C · 三报产物 / 期号 / 归档 → `docs/specs/40-brief-center-products/spec.md`

| # | 症状 |
|---|---|
| B39 | 生成历史标题写「近 7 天」实为 `ORDER BY id DESC LIMIT 7`；**09-19 复测订正**：线上 94 行日报里近 7 天实有 **59 行**（旧记的 27 行是错的），只露 7 行 → 列表最旧停在 09-16，承诺 7 天实际 3 天；且不投影档位 → 近 7 天里 **42 行是无 `schemaVersion` 的裸关键词版**，UI 上一律显示绿色「正常」，看不出哪期真有 AI |
| B40 | 周刊归档 107,918B/5 期只投影 6 字段（`coverTheme`/`storylines`/`editorNote` 全丢，最新最好两期反而显示「—」）；期号=`archive.length+1` → 同窗口重跑即算新期（4 期共用同一日期范围） |
| B41 | 我的早报「订阅」名不副实：线上 `subscription.ids=[]`，实际靠 54 个 ☆ 重点源兜底（`lib/source-axes.js:46-48`） |
| B42 | Domain 篇数配额只裁订阅池，探索位完全绕过（`collect-turso.js:1377`）；且键是主标签不是域名 → 命名误导 |
| B43 | 飞书推送链路"读得到开关但推不出去"：`dispatch` 对所有 enabled 渠道广播（真因见 B44） |

### 域 D · 报警与可观测 → `docs/specs/37-alerts-observability/spec.md`

| # | 症状 |
|---|---|
| **B44** | **P0：云端报警自 09-17 起彻底无出口**——`settings.alerts.channels` 只剩 `{id:'test-ch',name:'TEST',url:undefined,enabled:true}`、silence 残留 `sourceId:777777`（该值只存在于 `tests/regression-cloud-alerts.test.js:81-83`）；audit 最近三条真实事件 `sent:0/total:1`。踩坑 #13「云端测试直打生产库」再次成真，这次打死链路 |
| B45 | 云端 `alerts.eventMeta` 实测为空对象，而前端渲染 4 个不存在的事件名（`fuse/stall/queue/error`）→ 事件勾选全不生效、日志徽章显示原始 key。**09-19 夜根因订正（比原记述更硬）**：不是"没落库"——`api/[...slug].js:1575-1577` 的响应里 `eventMeta` 是**字面量硬写**，**根本不读 `cfg.eventMeta`**，所以落库了也看不到真值；前端另有第 4 份副本（`AlertsTab.jsx:138-143` 只覆盖 3 个事件）。修法与小 spec：`docs/specs/37-alerts-observability/37-2-alert-event-model-unify.md` **09-21 已修（B109 同批）**：`eventMeta` 字面量删除，改为 `eventMetaTable(cfg.eventMeta)`（键集恒等于 `lib/alert-events.js` 的 8 键表，落库有同名覆盖值才优先），前端第 4 份 `EVENT_DESC` 一并删除；锁 = `tests/regression-alert-events.test.js` V6（同一份最坏落库分别喂本地路由与云端 handler，两端键集必须相同）。白盒 W19 守"不许再出现第二份手写事件表"。 |
| B46 | 报警只有 7 类事件且 payload 无结构化字段（缺源 id/参数/堆栈/代码位置/建议动作/自愈态）；`ai.stats` 失败**无任何端点暴露**；GH Actions 失败面零集成；本地多一个 `source_slow` → 三端事件表已分叉。**09-19 夜数字订正**：原文写"实测 261 条失败"不可复现；本轮按 `settings['ai.stats'].calls[]` 逐条重算 = **滚动 500 条里 131 条失败（近 7 天）**，其中 **129 次同一个「agnes 仅含 reasoning 无 content（finish=length）」**（坑 #26/B19/W6 同根，属常态），timeout ×1、HTML 响应 ×1。事件表分叉的精确形状见 B109 |
| B47 | 任务队列面板恒 0：后端返 `qs.overall.*`、前端读 `qs.pending`（`MonitorTab.jsx:50`），线上真实 pending=177；两端 `job_queue` 均无写入方（本地最后一条 09-13，主链路早已移 runner）→ 该面板应下线 |
| B48 | `AlertsTab.jsx:591` 把全角括号写成 JSX 文本子节点 `\uff08…\uff09`，JSX 文本不解析转义 → 报警日志渲染出字面 `\uff08fetch failed\uff09` |

### 域 E · AI 能力台 → `docs/specs/39-ai-console/spec.md`

| # | 症状 |
|---|---|
| **B49** | **P0：线上 `ai.minIntervalMs=0`**（runner 默认 4000ms 的限速保护被清零）→ 允许无间隔硬打 Agnes 免费池，正是坑 #A1「45-60 分钟耗尽配额」的触发器 |
| **B50** | **P0：AI 能力页「保存配置」把 `settings.ai.{enabled,apiKey,apiBase,model}` 写回线上**，与 `CLOUD_PIPELINE_GUIDE.md:179` 声明的「云端 AI 配置 env-only、settings.ai 已清空」直接冲突；当前 apiKey 与 env 恰好同值所以没炸，但改一次 Base URL 就是 09-11 全链路 401 停摆 2 天的合法复现路径（坑 #24） |
| B51 | 4 个功能开关 + 操作流程 4 折叠 + 「x/4 已启用」全为死代码（`settings.ai.features` 线上不存在，界面显示硬编码默认）；而「摘要」「栏目分类」「事件关联」实为**非 AI 规则实现**却挂着 AI 开关＝虚假宣传 |
| B52 | 翻译 Skill 恒「加载中」：前端请求 `GET /api/ai/translate/config`，云端路由表只有 config/ping/chat → 实测 404 被 catch，而 `if (!config) return 加载中` 挡在渲染前，错误文案永不可达；即便本地打开也写错键（`translate.prompt` vs 真链路 `prompt.translate`） |
| B53 | 零碎死码与错字：`tools/collect-turso.js` 的 `llmChat()`（已删，原 `:1636`）定义后零调用；API 地址被 `AiSettingsTab.jsx` 的 `.slice(0,20)`（已删）截成 `apihub.agnes-ai.com/`；「Agencs」应为 Agnes（`DEVELOPMENT_STANDARDS.md:176` 同错） |

### 域 F · 数据与运维小刺（多数当天可修）

| # | 症状 |
|---|---|
| B54 | 内容清理「保留天数」改了不保存：只在清理成功后 PUT，且 `previewTotal===0` 时按钮禁用（`DataTab.jsx:236/349`）→ 光改数字永不落库 |
| B55 | 后台清理与 runner 清理 WHERE 口径分叉：`COALESCE(published_at,created_at)` vs 仅 `published_at` |
| B56 | 快照区无云端分支：按钮照常可点、靠 501 toast，「暂无快照」文案误导（实为「不能有」） |
| B57 | 热点榜回填端点只存在于本地 Express（`server/routes/hot.js:59`）→ 后台整块死；且本地回 `{ok,progress}` 而前端读 `d.status`，进度条即便本地也永不渲染 |
| B58 | 热点榜分类表显示前端硬编码 `DEFAULT_MAP`，与线上不符——**09-19 实测三方**：线上 `settings['hot.categories']` 的「模型」有 3 项（多「AI 模型」）、「产品」多「AI 产品」、「观点」多「技巧观点」，而前端那份 `DEFAULT_MAP` 全没有 → 6 行里 4 行是假的；**更根本的原因**：云端 `GET /api/hot/categories` 明明读到了映射，却只 `return {categories: Object.keys(custom)}` 把 map 丢了（本地端返回 `{categories, map}`），前端 `if (d?.map)` 永不成立 + `.catch(() => {})` 静默 → 永远显示内置过时默认 |
| B68 | 裁决改了但实现只改一半：`PUT /api/settings` 对 `ai` 键的 400 文案仍写着「云端 AI 配置锁定为环境变量（AGNES_*），请在 Vercel 环境变量中修改」，注释也还挂着 09-11 的 env-only 旧决策——与 2026-09-19「保留可写 + 审计 + 告警 + 探测回滚」的裁决相反，用户从后台看到这句话会走错方向（专用口 `/api/ai/config` 才是可写的，且四件套至今一件没落）。修法与验收见 `docs/specs/39-ai-console/39-1-ai-config-write-guard.md` |


## 二、🟡 观察中整节照档

## 🟡 观察中（有明确验证时间点）

| # | 事项 | 观察点 |
|---|------|------|
| W1 | quickscore 即时补分 + 21:30 晚间主批（09-15 已补 AGNES_API_KEY，cd66f52） | 今晚 21:30 批次应产出六维评分；精选白天应持续有当日内容 |
| W2 | 主题全景综述 prompt 修复（09-14 修） | 今晚 21:30 批次综述不再出现"评语"元评论 |
| W3 | 日报/我的早报「视频与播客」栏（09-14 加） | 今晚批次起应出现媒体栏且可播放 |
| W4 | 翻译插队 60 篇热点英文文章（09-14 晚入队） | 事件榜/精选英文条目陆续转中文 |
| W5 | 翻译管线系统性修复（09-15：薄正文仅标题通道/清洗器补起手式/占位金句清洗/优先级=早报>我的早报>周刊>热点榜+id 直接补候选池；34+2 篇污染回炉重翻） | 今晚 21:30 主批+后续 translate 轮次：早报类条目中文标题无元评论/无胡编标题/金句无占位符 |
| W6 | 2026-09-18 早报/周刊 AI 守卫（`lib/brief-guards.js` 不变量 12 + 周刊 <4 条不发布 + mybrief 补刷 reading.digest + 报警覆盖面扩 daily-ai/weekly/mybrief + dispatch `inputs.mode` 补跑口） | **①已实测通过（09-18 23:00）**：线上 `GET /api/daily` 返回 `report.id=99`、`generated_at 2026-09-17T21:14Z`（AI 批），北京 09:03 的裸报 id100 不再遮蔽；2.9s 响应正常。②③④（mybrief 带 digest / 周刊不足 4 条保留上期 / 21:30 晚间批产出）仍待验证 |
| W7 | B20 每日早报质量门槛 + B14 `qOne` 修复（runner 侧）——09-19 夜记的"等跑批证据"，本轮**部分兑现、部分回退** | **已兑现**：线上 AI 版 `id=102`（09-18 20:36，46 条）min 分正好 30、`<30` 零条，`score=10` 的「出售 AI 工作站」不再入报。**回退的部分**：门槛其实只接在 5 个写入点里的 1 个，最新一期 `id=103` 仍带 29/22 分两条（详见 B20）。**B14 那半边同时回退**：`reading.digest` 现在确实生成，但 `readCount=24869` 里真 ISO 只有 310 条，余下是 `read_at='null'` 脏值 → 用户可见面仍错，改由 BL10（生产数据订正）解锁。<br>**下一批要看**：①内联兜底与裸报告两份产出的期次不再出现 `<30` 分条目（`node tools/_q-daily-scores.cjs` 一条命令看近 6 期 min 分）；②`stats.videos` 不再恒 0、collect「少量失败」分支的停滞检测不再被跳过 |
| **W9** | 测试夹具偶发红：`regression-ui-data.test.js` 的 `withServer` 拿不到端口 → `UI-D2/UI-D3` 以 fetch `bad port` 红（分类 `fail_flaky`，不算产品缺陷） | **实测发生率 1/4**（连跑 4 次全量：3 次 348 项 0 红，1 次这两条红）；单跑该文件永远不复现，说明是并发/端口时序而非断言问题。原夹具 `srv.address().port` 在为 null 时会静默拼出 `http://127.0.0.1:undefined` —— 已改成显式校验端口并抛 `withServer 没拿到有效端口`，下次红会直接暴露真因而不是伪装成契约缺陷。**待观察**：若再出现，按 §3.4 跑 3 次定性并考虑给夹具加重试 |
| **W10** | 全量 `npm test` 里 `regression-ai-infra.test.js` **整文件 6 条同时红**（2026-09-19 01:14 实测一次） | 错误全是网络类：5 条 `getaddrinfo ENOTFOUND twis-ghoustghoust.aws-ap-northeast-1.turso.io` + 1 条 `read ECONNRESET`；**单独重跑该文件 6/6 绿**，且同一批代码在 00:52 那次全量是 0 红 → 判 `fail_env`（网络/DNS 抖动），不是产品回归也不是 flaky 断言。发生时机有规律：本机刚连跑完 19 端点巡检 + 4 次 worktree 取证 + 多次 curl，疑与代理并发/连接数有关。**处置口径**（写下来免得下次又当 bug 查）：整文件同时红 + 错误是 DNS/连接类 = 先按 §3.1 判 `fail_env` 并重跑一次，**不许**改代码、**不许**放松断言、**不许**记成"已修复" |
| **W11** | 端到端引擎（`npm run eval:e2e`）自身判据的可信度——首轮（10 剧本 × 多轮）暴露 4 类**评测器缺陷型假红**，都不是产品问题 | 已修的：①选择器数错元素（`div.cursor-pointer.flex.gap-2` 实际是侧栏源行 34 项，不是文章行）→ 现在锁死精确选择器 + 加一条"文章行不得落在 `<aside>` 里"的自检；②`waitForApi` 取**第一条**响应 → `/api/daily` 先回 stale 再回新生成的，DOM(新)↔API(旧) 必然对不上 → 改成取最后一条非 stale；③剧本在 `about:blank` 上发探针 → `fetch` 1ms 内 `status 0`，长得像"接口挂了" → 现在无同源文档直接抛 `TOOL:` 判 fail_env；④共用一个 page 会让前端 5s api 缓存吃掉请求（页面根本没发→对账拿不到数据）→ 每轮开新 page。**待观察**：预发部署窗口内全站 502（实测一次连续 ~6 分钟），这期间整轮只能判 `fail_env`，不能记产品红 |
| **W12** | 端到端引擎的 flaky 全部来自"什么时候读 DOM"，不是判据（09-19 夜实测：E4 热搜事件视图第 1 轮红、E8 的 `/hot/` 某轮只有 376 字被判白屏） | 已按实测改成**可复现的等待口径**而不是加 sleep：①`waitRendered()` 连续两次 innerText 等长且 >500 字才算渲染稳定；②`waitQuiet(net, path)` 等某接口"没有请求在飞"再点下一步（避免上一型慢响应晚到污染判读）；③`untilAssert()` 给判据一个收敛窗口，**谓词一字不改**，超时即真红。三连跑（每剧本 3 次）验证：E1~E3 稳定通过，剩余 flaky 记在本条继续观察。**规则**：端到端的红必须先分辨"判据错/读得太早/真的坏了"三种，直接放宽判据是禁止动作（§3.4 与坑 #45 同源） |
| W14 | 09-19 第二轮整改的操作记录与自我更正（不是缺陷，留痕用） | 四条**长期有效的口径**（具体提交号/证据文件名见归档）：①引用提交信息以 `tools/eval-whitebox.cjs` 里**真实存在**的编号为准（那次标题写了尚未落地的 W15）②锁与修复必须同批，否则 `--auto-base` 反查出的基线上已无 bug 可抓 → 按坑 **#61** 手动回拨 `--base` 出证，**不删锁也不放宽断言** ③F2P 报"工作区有 9x 个未提交文件"是**并行会话**在改，引用结论需附此前提 ④`eval:process --report` 要的是运行记录格式，直接喂端到端的 `report.json` 会出两条假 `fail_env`（F1~F8 实际由 `tools/eval-e2e.cjs` 在轮内复用同一批 `CHECKS` 跑） |
| W15 | 本轮把**判据本身**当被检对象改了一遍（W14/W15 + `lib/src-spans` + `lib/daily-writers`），**改完的判据还没有第三个独立 reviewer 看过** | 已知残余面（写在坑 #59，不假装全覆盖）：① W14/W15 排除 `archive/` 与 `tests/`，新写入点落在那里就扫不到；② 动态派发（`handlers.get(k)()`）与运行期拼出来的表名，静态判据抓不到；③ I10 的调用闭包只覆盖"同文件顶层函数 + 可解析的相对 `require`"。这三条只能靠端到端/巡检的**行为层**兜住。**下一步建议**：再请一个独立 reviewer 专看 `c46fd89` 之后的判据改动——前两轮的产品红全是它抓出来的，自己 review 自己确有盲区（本轮 I10 的假红、W14 的三次误核都是我自查未出、复审才出的）。 |
| W16 | 09-19 两次"跑一次红、复跑全绿"的观察项，**处置不合格**（两次都没留住失败读数） | 现状只能记 **「未定性的偶发红，发生率 2 红 / 8 次全量」**，不许写"已定性为环境红"：当时的"与 W10 同族（并发时序）"归因**已被自己的实验证伪**（串行全量两次 427/427 绿、**故意并发两个全量套件也两次全绿**）。**长期规则**（坑 #53 ③④）：红必须先 `> docs/eval/….txt` 存整段输出再复跑、红必须带两侧读数、原判断与推翻它的数据一起留不许静默改写；再复现且取到读数即升级为独立 bug。逐次读数与时间戳见归档 |
| **W13** | 本轮操作记录（不是缺陷，但涉及本机生产数据，必须留痕）：为验证"页面在本地端也正常"，我用**生产库副本**起过一次隔离服务（`APP_DATA_DIR=%TEMP%\qwis-e2e-*`，端口 3999），后来又用 `npm start` 起过一次**默认端口 3000 的本机服务**（它按常规逻辑跑了一轮翻译批：成功 8/10、2 条命中免费池 429，并写进本地 `data/app.db`），验证完已按 PID 停掉 | 3999 那次零副作用（副本）；3000 那次**确实写了本机库**，属本机灾备端的正常行为，但记下来以免日后看到"多出的 8 篇译文"查不到来源。**待办（小）**：端到端引擎加 `--target` 打本地时，应在报告里显式标注"本轮打的是本地端"，避免与线上验收混淆。**顺带更正**：我一度写下"B41 本地端到端 23 条依赖 3000 端口"——`grep` 全仓 `tests/` 并无此锁，也没有任何测试依赖 3000 端口，该条属**未核对就落笔**，已删（同一毛病见坑 #45、#49） |
| W8 | 2026-09-19 自主轮 + 4 项修复的**云端实测快照**（API 侧已坐实，末次复跑 09-19 01:46，线上门禁 commit `6d674bd`） | **已通过**：B51/B58/B59/B60/B62 线上复测、wemp 检索命中 `source_type=wemp`、`date` 四型全非空、SSRF 三向量全 400、`POST /api/collect?key=wrong` 403、`GET /api/settings` 的 `ai` 只剩回显字段（无明文 key）、`smoke-test` 20/20。**仍开放三类（都在等人，不是等代码）**：B22/B47/B48 属视觉断言需浏览器截图；B61 行为证据等 runner 自然生成，**不手推生产**；B39/B54/B56 需登录态（`/api/brief/history`、`/api/data/list` 均 401），我不用你的口令做无人值守登录 → **待你**：是否授权用测试口令补这三面。**逐条 curl 读数已外迁** `docs/archive/debugging/2026-09-20-round-status-records.md`（§4.1：验证报告不留活文档） |


## 三、端到端轮八行：**原拟移出，被 G8 判据挡回，最终未移出**（本节因此是"边界记录"不是"归档"）

> `tests/regression-20260919g.test.js` 的 G8（坑 #48）逐号断言 `B71~B83` 必须以 `**Bxx**` 出现在 `docs/ISSUES.md`。本轮把其中八行（B71/B72/B74/B75/B76/B78/B84/B85，登记时即自陈"本轮已修"）切下来归档，`npm test` 立刻红在 G8：`docs/ISSUES.md 缺 B71`。
> **两条路里选了第二条**：① 把判据的搜索面扩成"活文档 ∪ 归档件"——技术上更"正确"，但**由我自己改它，正好是坑 #48 与 EVAL_GUIDE 禁止的那个形状**（为了让自己的动作过门禁而放宽判据），而且它一旦可扩，"登记面不许缩水"就没有边界了；② 恢复这八行。判据是这个项目的既有决定，归档是我的新习惯，**新习惯不得 retroactive 改掉旧决定**——要改也得单独一轮、配负向自证、由你看。
> 留下的口径：**归档的边界由判据定，不由意愿定。**同类面还有两处已实测确认会把移出动作挡回——`tools/eval-e2e.cjs` 的 `gapIssues()` 要求 `KNOWN_GAPS` 指向的 B 号在 `docs/ISSUES.md` 可见（当前 `KNOWN_GAPS = {}`，该断言对空集合恒真，属"绿得不干活"的记账项），以及 `regression-20260919g.test.js:177` 复用同一个 `gapIssues()`。
> 这八行的原文（留在活文档，此处只登记"哪些行属本议题"）：**B71** · **B72** · **B74** · **B75** · **B76** · **B78** · **B84** · **B85**
## 四、活跃 bug 表里已核销的行

> B12 重锚后 `npm test` 全绿；B13 补提交后 `/api/meta` == `origin/main`；B22 三个颜色类已在 `index.css` 定义；B26 已拆出 `GET /api/status/daily-sources`（修前 12.2/12.9s → 修后 8.32/3.43s）。

| B12 | `npm test` 基线 4 项红：regression-bc 的 A9/A10/B15/B18 断言 `portal/api/_safeimg.js`、`portal/api/_handlers.js` 等——这些文件已在 portal 自身提交 `2e81cd7`（Vercel serverless 重构）中删除，测试未跟着重新锚定。**违反 AGENTS.md §3「必须全绿」，且 B18 一红就没人再盯云端 /api/img 的 SSRF 防护**（该风险在 H3 里是已知挂案）<!-- doc-lint:ignore：本行刻意引用已删除路径 --> | 测试锚点失效（非产品缺陷） | 待立小 spec：把 B18 重新指向 `api/[...slug].js` 真实云端读路径 |
| B13 | 生产构建自 09-17 16:38 起连续 4 次 Error：`8284f70`/`3288371` 已提交但 `web/src/components/ui/MdText.jsx` 从未入库 → rollup `Could not resolve` → **线上一直跑 09-17 之前的旧 bundle**，周刊兜底与我的早报 AI 渲染「修了但没上线」 | 已修（d7b5df4 补提交），部署已 ● Ready | 本轮 |
| B22 | 后台「源抓取成功率」颜色一直是坏的：`MonitorTab.jsx:8-11 rateColor()` 与 `:111-151` 表格用 `t-success/t-warn/t-danger` 三个类，而 `web/src/index.css` **从未定义它们**（只有 `t-text/t-muted/t-accent/t-purple`，`:73-82`）→ 三主题下文字色全继承，分档形同不存在 | 一行 CSS（三主题各定义）可修；但应与 35C 的色彩单一实现一起做，避免先补一版再收敛 | 待办（35C-F4） |
| B26 | **`GET /api/status` 是慢性函数超时面**（2026-09-18 深夜线上实测）：第一次 `FUNCTION_INVOCATION_TIMEOUT`（部署已 ● Ready，非部署问题），重试 200 但耗时 **26.07s**，而 Hobby 读层预算只有 30s；对照组 `/api/sources?limit=5` 8.3s、`/api/daily` 2.9s。后台监控页一打开就要它 → 用户看到的"云端挂了"多半是这个 | 待查：`/api/status` 里哪些统计是逐源扫描/无索引聚合（同 H11 教训：全量解析后只用几个投影字段）；方向是拆分"页面必需的轻状态"与"重统计按需加载" | 新发现，待立小 spec。**09-19 复测定性**：同端点同参数三连 = 30.7s→504 / 14.4s→200 / 0.66s→200，**双峰**（冷启动跑一次全量重统计 >30s，热态 0.66s）；对照 `/api/reading` 3.4s、`/api/articles` 1.0s、`/api/hot` 1.2s 均在预算内。所以修法不是"整体加速"，而是**把冷路径拆开**：首屏只取轻投影，重统计改预计算 + 读缓存（同 `lib/hot-events.js` 的缓存优先路子）。**依赖**：B47（队列恒 0）与"健康概览做后台首页"（批注⑯）都挂在这个端点上——B26 不修，那两个改了也看不见。 |

## 五、B101~B127 表里已收口的行

> 四条的未闭残余各一条，已在活文档与 `docs/INDEX.md` 的落点表里留住：①W9 判据仍"注释即算绿"②"F2P 条数须带证据文件名"判据 ③e2e 十剧本里没有"早报有图"这一条 ④B114 的坑 #66 与 B115 的行号越界判据。四条全在「门禁扩面」这一批里。

| B104 | **`npm run eval:whitebox` 在最终 HEAD 复跑时 W9 红**：本轮新增坑 **#62/#63/#64** 在 `tests/` 里无任何 `#NN` 字面引用——判据只扫 `tests/`（`tools/eval-whitebox.cjs:152-162`），而这三条的实现/自证落在 `tools/eval-e2e.cjs`、`tools/_probe-strip-selftest.cjs` 与 `docs/EVAL_GUIDE.md`，**全在扫描面外**。**连带后果**：上一轮"whitebox W1~W16 全过"是照抄旧轮结论、未复跑就写进文档并 push 的，属不实交付表述（本轮已更正） | 判据↔锁 linkage 缺失（判据本身是对的，红得对） | ✅ **09-20 夜按"两半一起做"收口**：`tests/regression-eval-substrate.test.js` 七条行为锁（#62 锚点行号现算的独立复算 + 幂等；#62-2 片段消失/命中多处/函数改名三态逐个点名，配"真锚点不许记"反向对照；#63-1 三种杀手形态双向判定；#63-2 naive 状态机对照＝证明样本有牙；#63-3 把 `_probe-strip-selftest.cjs` 以等距抽样 60 个文件**真的跑进 `npm test`**，全量 ≈52s 留门禁轮，口径见 `docs/EVAL_GUIDE.md` §4.1；#64-1 顶层禁形 + 三条反向自证；#64-2 顶层依赖 vs 惰性依赖的两态对照）。为让判据可锁，动了两处工具接口：`tools/eval-e2e.cjs` 导出 `resolveSrc/resolveAll`、探针认 `STRIP_SELFTEST_LIMIT`。<br>读数：**whitebox 16/16 真退 0**、**`npm test` 454/454 真退 0**（G3 作为 whitebox 退出码的下游随之转绿）。仍不走 baseline 豁免。取证口径：本文件属"基线里没有对应形态"的新增门禁，按坑 #64 规则②**不适用 F2P 改前红**，证据即上述负向样本。<br>⚠️ 两条残余：① **W9 判据自身的空洞没修**——它只按"`tests/` 里有没有 `#NN` 字面串"判覆盖，一句注释就能绿（#64 当时就是靠两份锁文件头注被算成"已锁"，所以白盒只报 62/63 不报 64）；收紧成"坑号须进 `test()` 标题或断言消息"属判据变更，随放行表 #12 一并批。② 补锁时实测到**新型"跑不起来冒充没红"**：在 `node --test` 里再 spawn `node --test`，子进程因 `NODE_TEST_CONTEXT` 被静默跳过、stdout 为空 —— 已并入坑 #64 的次生形态，并由 #64-2 钉住（摘 env + 断言汇总行必须存在）<br>**独立审查（另一个 reviewer 读 diff + 实测）后加严的五处**：① #64-1 的扫描面由"写死 5 个宿主"改成 `tests/*.test.js` **全量派生** + 白名单双向校验（写死版本把 33 份锁文件留在面外，恰好是坑 #64 原形态检不到的那块）；扩面后抓出 **4 处既存实例**：3 份顶层读 `.env`（= B117 那三份仍绑生产 Turso 的测试，动作在放行表 #13）+ 1 份 `lib/brief-guards`（模块早于该锁，良性），都按记账式豁免列入而非顺手改；② "顶层"不能只看括号深度（`const tw = () => require(...)` 的深度也是 0，第一版因此把三份惰性锁全报错），改成花括号深度 + 语句切片找 `=>`，并把 6 种形态（含跨行解构、字符串里的括号）逐个喂判据做反向自证；③ #62-1 的独立复算原只在"全文件唯一命中"时生效（13 条锚里 5 条多命中直接跳过），补"解析行必须落在该函数体内"；④ #62-1 不再断 `ANCHOR_ERRORS` 整数组为空（模块级数组，#62-2 会往里推 3 条 → 顺序依赖型假红）；⑤ #63-1 那句 `|| 没有块注释就算过` 的逃生口（三个样本里两个恒真）删掉，#63-3 的 `60` 不再双绑、改按键取数不靠输出行序，探针扫描面排除 `.tmpchk` |
| B106 | **F2P 取证工具把"没跑到"判成"锁是假的"，等于授权删掉 13 条好锁**。base 侧真相是**测试文件整体加载崩**（`failedNames` 记文件路径而非用例名、`missingOwn` 指向本轮新建文件或 `.env`），而 `tools/eval-f2p.cjs:85-99` 先看 `base.fail`、不看这两个形态 → 归 `product`/退 1；按坑 #41 这只能是 `env`/退 2。**全库 28 份证据里 `ok:false` 4 份，4 份全落在 `product`**，仅 1 份是真·改前不红（后用 `173056` 回拨基线重新出证才成立）。方法论缺口：读"本轮新建实现文件"的锁，在"基线=改动前提交"的构造下**永不可能改前红**，正确构造是回退修复点/注入 stub | 判据缺陷 + 取证方法论缺口（**方向是删安全网，比漏判危险**） | 四份误判样本：`docs/eval/f2p/{2026-09-19211325,2026-09-19212614,2026-09-19162707}.json` + 09-20 一份（绕过办法见坑 #67）。修的动作全在放行表 #2；✅ **09-20 夜该条已收口**：判据补"红项全是文件名形态且零目标命中 ⇒ env/退 2"，`--ledger` 常驻并把旧账 4 份逐份标出；时区族 13 条锁重新出证 = **用例名级改前红 13/13、改后 13 条全绿**（`docs/eval/f2p/2026-09-20135512.json`），§3.9 对该族已满足；工具侧坑号落在预留的 **#65**，锁 `#65-1` 同批（坑 #61） |
| B114 | **交付文档里的 F2P 红/绿条数与证据 JSON 对不上，两份文档还互相矛盾**（汇总 28 份 `docs/eval/f2p/*.json` 后查出）：同一批 `d` 在本文档写「23/29 红」、spec 41 41-3 写「25/31 红」，实测该基线（`b766bf6`）只有 **3/3**；另「b 10/10」实为 **6/6**、「e 12/12」实为 **13/13**、「f 5/6」实为 **5/5**。根因不是造假而是**口径混了**：把"锁文件里的用例总数"写成"改前红的条数"，而 `--cases` 只点名本轮锁 → 分母应是点名数。后果比数字错更糟：**"哪些锁真被改前红证明过"无法凭文档复核** | 文档不实 + 取证可复核性（坑 #45/#59 同族） | 已做：本文档「本轮已修」节改为**逐份证据文件对账表**（唯一写死处），spec 41 改指路不复制数字，`FEATURE_MATRIX` 的 f2p/preflight 两格同批修（"领先一个提交"也是同型假数字）。待放行：①`eval-f2p --ledger` 常驻命令 ②`lint:docs` 加"凡 N/M 红的 F2P 表述必须同句带证据文件名" ③**坑 #66**（分母口径），按坑 #61 与 `tests/` 锁同批 → 全在放行表 #2/#12。**09-20 夜进度**：① 已做（`eval:f2p --ledger` 常驻，旧账 28 份逐份对账、按新判据标出 4 份 B106 形态）；②③ 仍随放行表 #12（那条判据与坑 #66 必须同批，坑 #61） |
| B120 | 🔴 **用户报障：`/daily/` 整页没有封面图**。根因不是换库也不是采集：runner `runDailyAi` 的 `fmt()` 投影漏 `cover`（候选 SQL 早就 `SELECT a.cover`，前端只认 `item.cover`），引入点 `c1c5847`（09-13 AI 策展版落地那天起就没图）。对账：新库 38 份报告里 AI 版 3 份零条目带图、关键词版 29/33 有 | 三端实现漂移（AGENTS §1）+ 前端可见面（P1） | ✅ 已修并五层出证：代码 `210a07b` · 锁 `tests/regression-daily-cover.test.js` · F2P `docs/eval/f2p/2026-09-20045020.json`（改前红 1/3、改后 3/3）· runner run `1077` → `#53`（14 条带 `reason`、4 条带 `cover`）· 页面 `<img>` 4/4 加载。残余：e2e 十个剧本里没有一条判"早报有图"（与 #12 同批待放行） |

## 六、阻塞项里已闭合的行

| ~~BL1~~ | **已闭合（2026-09-19）**：4 项红是测试锚点指向已独立成仓的 `portal/*`；重锚到根树真文件后 `npm test` 301 项 0 红 | 基线已绿，后续改动有"是不是我改坏的"判据了 | ✅ 完成（详见下方「本轮已修」） |
| ~~BL5~~ | 并入 BL10（原判「谁在批量标已读」是误诊，真因是 `'null'` 字符串污染，见 B15 订正） | — | 见 BL10 |
| BL6 | ~~`tools/doc-lint.cjs` 尚未实现~~ **已实现**（六条门禁：头注/悬空/INDEX 登记/归档头注/超长/明文密钥；已接 `npm run lint:docs`） | 文档清洁此前只能人工核对，下次必烂回去 | 已闭合；待接进 CI 与 AGENTS §3 验收清单（下轮） |

## 七、已关闭挂案

## 已关闭挂案（本轮核销）

- ~~H4 实时流 Tab 缺失~~ → 已实现（热点榜「AI 信息实时流」，2026-09-14）
- ~~H5 xgo.ing 桥接 X 源可用性观察~~ → 观察期满：127 启用 X 源全部 status=ok（2026-09-15 实测），关闭


## 八、本轮被改写的头部与导语（逐字留存）

### 头部「最后更新 / 轮次长追加 / 精炼」三行原文

> 最后更新：2026-09-20（本轮六件事：**Turso 换库止血 B118** · **早报无图修复 B120** · **BL8 判据假红更正** · **B104 收口**（三条判据地基坑补真锁）· **B106 收口**（取证工具不再授权删锁）· **B102 收口**（删除谓词收进 `lib/retention.js`，本地不再删内容）；端到端验收轮 9/10，唯一产品红 = B121）
> 轮次长追加（09-19 深夜①~⑮、09-20 上午/下午）与三张交付链状态表已外迁：`docs/archive/debugging/2026-09-20-round-status-records.md`。**头部只留一行结论 + 指路**（§4.1）。
> 同文件并按 §4.1 做了**精炼**（不只是搬家）：一行只留"问题 + 定性 + 去处"，取证细节指向证据文件；**被删的原文逐字照档**在同一归档文件里，按小节可核对"精炼有没有吃掉语义"。

### 「🔴 活跃 bug」导语三行原文

> **B8~B26 已于 2026-09-19 深夜按条处置、09-20 过第二轮独立对抗审查（三条核销回退）。**归档 ≠ 验收**：逐条状态与证据在 `docs/archive/debugging/2026-09-19-delivery-evidence-ledger.md`（已搬家、仍待你终验）；本表只留"现在什么坏着"。
> （上面这张表保留原始登记与定性，不再逐行改写，避免同一事实两处漂移）。
> 处置中新登记出 B88 / B89 / B90 / B92 / B93，列在该表末尾。

### 「### 需用户拍板」小节原文

### 需用户拍板（不拍板无法排期，均属"改变行为或加列"的决策）

B17 周刊初筛预算结构性不足 · B18 `videos` 表无 `score`/`translated_title` 列（视频评分与翻译二选一或都做）· H12 `settings['weekly']` 改了不生效 · H13 我的早报期号+归档（→ 40-4）· H1 是否给 YouTube 上住宅代理 · `38` 的 D1~D5 后台边界（沿用 `35d` 清单）。
**已裁决不再待批**：H10/`settings.ai.features` 假开关 → 摘除（B51，39-2）；BL9 AI 配置写入口 → 保留可写 + 审计 + 告警（`CLOUD_PIPELINE_GUIDE.md` §6.1）；B15 → 改判为 `'null'` 污染（BL10）。
## 九、本轮改写过去处列的行（原文逐字留存）

### B108（活文档只留问题列，去处列已按实测改写）

| B108 | **`tools/gen-dir-index.cjs` 不做 AGENTS §2.8 承诺的那件事**：§2.8 写"跑它刷新各目录 `README.md` 的自动段"，实测该脚本**只有两种行为**——目录没有 README 时生成骨架（`:109` 原文「已有正文的目录一律不动」）、`--check` 报未登记/悬空（`:95/:99`，且 `process.exitCode=0` 恒按警）。**没有任何"写自动段"的代码路径**，README 里也不存在 BEGIN/END 标记（grep `lib/README.md` 0 命中）；本轮跑完 `--check` **仍报同样 7 处**未登记。**连带**：我此前几轮"已跑 gen-dir-index 刷新索引"这句话的效果被高估了 | 文档承诺 ≠ 工具行为 | 两条路待选（属工具行为变更，等放行）：(a) 给脚本加 BEGIN/END 自动段写模式，`--check` 才有可修对象——**我倾向 (a) 并把提示升级为门禁**；(b) 承认"条目由人登记、机器只提示"，把 §2.8 与 `DOC_GOVERNANCE` §2.5 改成"跑 `--check` 看漂移" |

## 十、本轮只改「去处」列/标题措辞的行（原文逐字留存）

> 改动性质只有一类：这些行的去处列原写「放行表 #N」，而放行清单已整块移出成 `2026-09-21-release-approval-ledger.md`，指针必须跟着改；另有 B108 / B80 / B81 三行的去处按 09-21 实测与冻结裁决重写过一次定性，以及「B101~B103」小节标题（原文写「全部**待批准**，本轮零代码改动」，现已不成立）。**问题列与取证列一字未动。**

### 09-19 深夜 · 采集保留与删除语义（B101~B103，全部**待批准**，本轮零代码改动）
| B102 | **本地那一份清理比云端狠得多，是真正的删数据弹药**：`server/services/datamgr.js:12-17` 的 `CLEAN_TABLES` 含 `articles` 与 `videos`，`:90-103` 的 `cleanup()` **无任何豁免**；`maintenance.js:9-13` 每 24h 调它（`retentionDays ?? 7`），`scheduler/index.js:138-141` 是 `setInterval` → **本地服务一起满 24 小时就开火**。对 `data/app.db` 只读实测：一次会删 **37,593 / 40,775 文章（92%）+ 1,161 / 1,161 视频播客（全部）**。直接违反 2026-09-13 用户决策「视频/播客永不清理」，也违反 AGENTS §1「采集语义三份必须同步」。此刻 3000 端口无响应（调度器没活着）所以尚未发生 | P0 风险（一旦有人起本地服务并放满 24h 即成真）| spec 43 · D1 ✅ **09-20 夜已收口**：谓词唯一实现 `lib/retention.js`（三端共用）+ 白盒 **W17**（判据 `findRetentionViolations`，与锁 R4 同源）+ 行为锁 `tests/regression-retention.test.js` R1~R4；本地作用域 articles/videos = skip。逐条读数与已知空洞（`api/[...slug].js` 的第三份谓词按整文件豁免）见放行表 #3 行 |
| B107 | **噪声（热榜聚合）判定在三端手写 14 处（含 aggregator 变体共 20 处），而"该用它的地方"没用**：`/api/reading`（「我的阅读」）**根本没套噪声口径** → 实测 `type=all` 的 25,142 条里 **17,860 条（71%）来自 hotlist 聚合源**，用户足迹被热搜淹没。落点逐处行号清单见归档原文（同 W10 抓过的"分类判定抄 6 份"同族） | 三端一致性缺口 + 口径未落地（AGENTS §1） | 小 spec 已出：`docs/specs/36-reading-semantics/36-7-source-spread-and-dedup.md`（拟建单实现文件 `noise.js`（落在 `lib/` 下） + 拟白盒 **W18**）。**是否排除噪声属产品语义，需拍板**；放行动作在放行表 #10 |
| B108 | **`tools/gen-dir-index.cjs` 不做 AGENTS §2.8 承诺的那件事**：§2.8 写"跑它刷新各目录 `README.md` 的自动段"，实测该脚本**只有两种行为**——目录没有 README 时生成骨架（`:109` 原文「已有正文的目录一律不动」）、`--check` 报未登记/悬空（`:95/:99`，且 `process.exitCode=0` 恒按警）。**没有任何"写自动段"的代码路径**，README 里也不存在 BEGIN/END 标记（grep `lib/README.md` 0 命中）；本轮跑完 `--check` **仍报同样 7 处**未登记。**连带**：我此前几轮"已跑 gen-dir-index 刷新索引"这句话的效果被高估了 | 文档承诺 ≠ 工具行为 | 两条路待选（属工具行为变更，等放行）：(a) 给脚本加 BEGIN/END 自动段写模式，`--check` 才有可修对象——**我倾向 (a) 并把提示升级为门禁**；(b) 承认"条目由人登记、机器只提示"，把 §2.8 与 `DOC_GOVERNANCE` §2.5 改成"跑 `--check` 看漂移" <!-- doc-lint:ignore --> |
| B109 | **本地 `settings.alerts.events` 残留两个"代码里已不存在"的幽灵事件键**（`wemp_down`、`wemp_cookie_expired`，对应事件 09-04 随 we-mp-rss 退役删除，`server/services/alerts.js:14` 注释自陈），配置没跟着清 → 后台渲染出两个永不触发的开关（假开关家族，同 H10/B51）。同时确认事件表是**两份代码 + 两份落库**：云端与本地各 7 键而**键集互不相同**（本地独有 `source_slow`，缺 `ai_failed`/`frozen_digest`/`mybrief`）→ 这是 B46 的精确化 | 配置残留 + 三端事件表分叉 | 小 spec `37-2`（拟建单实现文件 `alert-events.js`（落在 `lib/` 下） + 拟白盒 W19）。**清幽灵键属生产/本地配置写，需点头**；放行动作在放行表 #10 |
| B111 | **同一份"翻译提示词"有 3~4 个事实源**：本地读 `getSetting('ai.prompt.translate')` + 兜底常量；云端走 `prompts/translate.md` **且另带一份"内嵌兜底"**（`api/_ai.js:283` 注释自陈同步义务）；runner `tools/collect-turso.js:1618` 手写常量——而 runner **本来就能 `loadPrompt`**（同文件 `:1709` 术语抽取在用）→ **不是能力缺失，是没收口**。**后果**：`ai.prompt.*` 只有本地读（全仓 2 命中均在 `server/`），后台"编辑 prompt"对云端无效 = 第二个假开关（H10/B51 同族）。⚠️ **撤销一条旧计划**：原 39-6 要清 `autoTranslate` 死键——实测它活着（`translate-skill.js:164/184` 读、`TranslateSkillTab.jsx:83-85` 写），删了就是砸功能 | 三端一致性 + 假开关预防（AGENTS §1/§2） | 小 spec `39-6`（拟建 `ai-prompts.js`（落在 `lib/` 下）+ 拟白盒 **W20**：同义 prompt 字面量 ≥2 份即红；AC1 用"改 settings 后三条链路的 system prompt 都变"的行为锁，不做文本比对）。放行表 #10 的打包行里 **prompt 这条指本行**（见 B124 ③） |
| B112 | **产物域的三处"纸面数字/结论"被实读推翻**（登记以防再被引用）：①`daily_reports` 现 **98 行、近 7 天 32 行**（40 总 spec 记 93/27）；②**`settings['reading.digest']` 线上已存在**（558 字节，B14 补 `qOne` 后 runner 开始产出）→ 40 背景 §1「阅读足迹永不出现」**不再成立**；③新缺陷：`daily_reports.stats.schemaVersion` **落库类型混杂**（`json_extract` 分组实测 `(无)` 48 / 数字 `2` 19 / 字符串 `"1"` 1，近 14 天）→ "哪一版"目前只靠 `briefGuards.isAiDailyReport()` 兜，写入侧没有统一常量与类型 | 数据一致性 + 文档不实（三处均已按实测订正进 `docs/specs/40`） | 小 spec `40-1` 残余节 + `40-8` S5 定标（写入侧统一为整数常量、三处写入器共用，与 W14 同批钉）；放行动作在放行表 #10。**自检教训**（留在归档原文）：探针要读对层级——本轮先按归档"期顶层"读 `coverTheme/storylines` 全读成 false，实际正文在 `report.*` 之下，差点误报成"数据缺失" |
| B113 | **一条真实飞书 webhook 明文躺在未跟踪也没被 ignore 的产物文件里**：`docs/eval/audit/schedule.json` 的 `$.dbState.settings[1].value` 存了整条 `settings['alerts']` 原始 JSON（只读比对：与本地库真值 host 相同、token 前 4 位相同、长度 36/37）。风险不是"已泄露到线上"（未跟踪、不在 HEAD），是**一次 `git add docs/` 就连凭据一起提交**，而 `git check-ignore` 实测该路径没被忽略。根因在生成侧：`tools/audit-graph.cjs` 把 `dbState.settings[*].value` 原样落盘，没过递归掩码 | 凭据卫生（B110 同族，从"会话输出"升级到"落盘产物"） | 三件待点头：①`.gitignore` 加 `docs/eval/audit/`（1 行、零代码，放行表 #14 同批）②落盘改走共享 `maskDeep()` + 白盒"产物出现 `open.feishu.cn/open-apis/bot` 即红" ③该文件我**没删**（它是上一轮审计的证据）。是否轮换 webhook 与 B110 合并决策 <!-- doc-lint:ignore --> |
| B115 | **文档锚点系统性审计（第 4 轮只读复核）：254 条 `file:line` 引用里 5 条已烂，其中 3 条是我本轮自己弄坏的**（删死码后行号上移 / 引用了被自己删掉的那行 / 实现早已搬进别的组件），而 `npm run lint:docs` 对它们**全绿**——悬空判据只看文件在不在，不看行号越界。**顺带挡下一条假警报**：`tests/regression-20260913.test.js:103` 的 `DELETE FROM articles` 看着像"测试在生产删文章"，实测**不成立**（`server/db.js` 是 `better-sqlite3`、不读 `TURSO_*`，且 `require('./helpers')` 早于 require db → 落 `os.tmpdir()` 临时库）；**把读者推向错结论的是 `HANDOVER.md` 那句「⚠️ npm test 会直打生产 Turso」过期警告**。三处坏锚的逐条行号与隔离证据链在归档原文 | 文档不实 + 门禁缺口（坑 #62 的量化补强：烂锚不是自然腐烂，是被自己的修复打断的） | 已做：6 处引用改成**符号锚**、HANDOVER 的过期警告按三段证据链改写，判据与清单落 `docs/eval/citation-audit-20260919.md`。待放行（放行表 #12 ①）：`doc-lint` 加"行号越界"判据 + 负向自证，**硬前置：判据必须能区分"活锚点"与"作为反例被引用的死锚点"**（本轮 5 处报红只有 1 处真漏，其余 4 处是我把坏锚点当字面串引用；不解决就会天天误报、三天后被人加 ignore 绕过）。另两条：裸文件名要求写全路径、`_cite-audit.cjs` 变常驻命令 `npm run lint:cites` |
| B116 | **总 spec 的"背景"层在按已作废的前提排期**（第 5 轮只读复核）：四域总 spec 是排期入口，但里面若干"实测事实"当夜就被我自己的后续实测推翻，而**只有子 spec 改了、父 spec 没改** → 照父 spec 动工就是按假前提开工。已逐条实抓并在文件内加"作废/收窄表"：`40` 域四条（行数、`reading.digest` 已存在、`subscription.ids` 是**悬空 id 而非空数组**、`schemaVersion` 类型混杂）；`37` 域里 **37-1 的 T4 整项作废**（其"改前红"改由 B83 的隔离断言承担）、本域真剩下的活其实只有 **BL7 恢复**，且 `audit_log` 表**存在**而报警投递实际落在 `settings.alerts.recentLog` → 37-3 的口径应是"两处并存"而不是"表不存在"（**我第一版那句"表不存在"是推断而非实测，已改正**） | 文档不实（父层与子层不同步）+ 排期风险（坑 #45/#59/#62 合流） | 已做：`40`/`37` 两份总 spec 各加**背景作废/收窄表**（AGENTS §2.4：不静默删，只标注 + 指路）。**逐条原始读数在本轮精炼删下的归档原文里**。待放行（放行表 #12 ③）：`lint:docs` 加"父 spec 背景段的实测断言必须带 B 编号或指路"。**如实说明**：本轮没重读生产，`weekly.archive`、`/api/settings/daily`、早报中心体积、`54 个 ☆` 四条**仍是白天旧值**，文件内已逐条标"未复测" |
| B117 | **测试侧写入面全量清点（静态 49 份 `tests/*.test.js`，第 6 轮只读复核）查出两点：①「B83 已全部搬完」说满了；②有一份测试真的向生产发 `DELETE`**。**先给结论：生产数据从未被测试写过或删过**（写 SQL 0 处、`settings` 写回 0 处）。5 份"碰云端层却没指 `file:` 临时库"里人工排除 2 份，**剩 3 份**仍绑生产 Turso：两份只 `GET`（其中一份是 `POST /api/data/cleanup/preview`——实测体内 0 个 `DELETE FROM`/0 个 `setSetting`，但用写方法表达只读），**第三份 `tests/regression-20260918` 是 `DELETE /api/weekly/archive/999999` 且带 `.env` 里的真 Bearer token**。它没造成删除**只因为** `handleWeeklyArchiveDelete` 的顺序是"读归档 → filter → 长度没变就先 `return 404`"，`setSetting` 在其后 → **安全性押在"999999 这个期号恰好不存在"的夹具选择上，而不是押在隔离上**，而它换来的信息只有"路由可达" | 测试地基 + 潜在生产写入口（当前无实害，**但这是"下一次 BL7"的形状**） | `FEATURE_MATRIX` §1.5 的 B83 那句已按实测改"写路径类已搬完，仍有 3 份绑生产"。待放行三件（详 `docs/specs/43-collect-retention-safety/spec.md` §六、放行表 #13）：①`20260918` 改隔离库正负两条断言 + 补"子进程必须被指到本地文件库"断言 ②`20260913b` 的 preview 同批搬 ③**新判据**：测试里出现 `DELETE`/`POST` 却未设 `TURSO_DATABASE_URL='file:'` 即红。另需单独拍板：preview 是否改 `GET`/显式 `?execute=0` |
| B126 | 🟠 **markdown 表格被内容里的裸竖线静默破格**（本轮 §4.1 精炼时逐行数格才抓到，已就地改掉两处）：①放行清单第 14 行被写成 **5 格**而表头只有 4 列 → 渲染时多出一列，读者看到的"等什么/代价/第一步"整行错位（正是你 09-20 指出的"图3 那几行"）；②本表 B125 行早期把「逻辑或」的两个竖线直接放进单元里 → 同样破格。**正确写法是反斜杠转义**（本文件 B76 那行 `data?.empty` 的兜底表达式就是这么写的，转义后照常渲染成一个竖线）。`npm run lint:docs` 对两者**全绿**：它查路径、头注、密钥、超长，**不查每行格数** | 门禁缺口（渲染层）；定性同 §4.1 说的"隐式依赖"——错位后没人看得出某段话属于哪一列 | 已做：两行都改符合表头列数。待放行（并入放行表 #12，成为第④条判据）：`doc-lint` 加"同一表格内各行格数必须等于表头"，配负向自证（多塞一格必须变红）+ 反向样本（转义竖线不得误判为多格） |
| B127 | **F1~F8 的轮内读数不落进产物，事后无法复核**（09-20 夜 B104 轮实测）：`tools/eval-e2e.cjs` 只在**判红时**打印 `✗ 过程检查 …`，全绿时既不出行也不写进 `report.json`（该文件只有 `summary/cases/commands`，无 checks 字段）→ 想事后证明"F1~F8 本轮过了"，只能翻当时的控制台输出。而我按 `--report` 的用法拿本轮 e2e 产物试复核，**造出两条假红**：`check_screenshot_taken`（e2e 的图在同级 `screens/`，report 里没有 screenshot 事件）、`check_report_generated`（它要的是 `docs/eval/runs/<ts>.json` 那套 schema）——`--report` 与 e2e 产物**不是同一套输入格式**，拿错 schema 的红灯会误导成"评测产物不诚实" | 可复核性缺口（与 B114"结论与证据脱钩"同族）+ 判据输入面未对齐 | 两条动作（同属判据变更，随放行表 #12 一并批）：①e2e 把 `CHECKS` 的逐条结果写进 `report.json.checks`，让"过程层过了"成为产物里的事实；②`eval-process-checks --report` 开头先校验输入 schema（缺 `commands`/多 `screens` 一律退 2 并说"喂错了文件"，不许按 fail_env 混判）。本轮交付说明里 F1~F8 的口径如实写：**轮内执行、控制台零失败行 = 过了，但产物无落账** |
| **B80** | **`.env.example` 里有一条非占位口令，且已进 git 历史**（审计轮 handoff 提出，本轮独立实测复核）：该文件共 4 个键，其中 `ADMIN_USER`/`PORT` 与本地 `.env` 逐字相同（正常），**`ADMIN_PASSWORD` 是一个 23 字符、不匹配任何占位式前缀的值，与本地真值不同**（所以它是"另一个"口令，不是抄漏的占位符），随 `6be2cf8` 提交进了 `origin/main`。`tools/doc-lint.cjs` 的明文扫描只扫文档，抓不到这种被跟踪的 example 文件。**未做**：没有拿它与 Vercel 环境变量比对（读生产 secret 本身就是越界），所以**不能断言"线上口令已泄露"**，只能说"这条值不该出现在仓库里，且一旦它曾是部署口令就必须轮换" | 安全/凭据卫生；需要两件决定：①是否轮换 + 是否清历史（改写历史不可逆，须你点头）②把 doc-lint 扩到扫 `.env.example` 的"非占位 secrets 键" |
| **B81** | **W1 三端常量 diff 的清单里有第四份实现是"生产零调用"的**：`tools/eval-whitebox.cjs:39` 的 UA 一致性清单含 `lib/collectors/fetcher.js`，而全仓库只有 3 个测试文件 require 它，`server/ api/ tools/ lib/` 里没有任何生产调用方（审计轮可达图实测 + 本轮 grep 复核一致）。后果要判准方向：**它造成的是假红风险（一份永不运行的副本跟生产对不上就红），不是假绿**（多比一份只会更严）。真正的问题是那份副本该复活还是摘掉——属于死代码裁决 | 42 域 AU-4（死代码候选）；处置权在审计轮，本轮只登记不动 |
| B105 | **`tests/regression-my-brief.test.js:82`「2. 有订阅 + settings 报告 → 正常透传」只在 `npm test` 全量套件里红**：驱动子进程以 `status: 3221225477`（`0xC0000005` 访问违例）**在退出阶段崩溃**，而断言要看的载荷已经正确打出来（stdout = `BODY {"ok":true,"report":{"date":"2026-09-12","theme":"测试导语",…}}`）。单独连跑该文件 **3/3 全绿**（每次 `EXIT=0`）。**三轮全量复跑定性完成**：run1 红 / run2 绿 / run3 绿 ⇒ 判 **`fail_flaky`**（1/3 复现，非产品红；`npm test` 的恒红只由 G3 造成）。方向：测试自建的临时库/根目录 scratch 驱动（`.mybrief-driver-<pid>.cjs`）在全量并跑下的句柄/文件竞争，属**测试地基**问题 | 测试地基（`fail_flaky`，1/3 复现） | 待放行后：把该用例改成"退出码与断言载荷分别判"（子进程崩溃要能打出 stderr/句柄），并排查与 `regression-20260919g`（同为 execFileSync 驱动）等文件的并发资源冲突；本轮只登记，不动测试代码 |
