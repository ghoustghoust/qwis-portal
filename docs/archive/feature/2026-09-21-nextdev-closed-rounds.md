# NEXT-DEV-REQS 核销轮移出件（2026-09-21）

> 类别：功能 | 归档自：`docs/NEXT-DEV-REQS.md`（2026-09-21 核销轮，DOC_GOVERNANCE §3 Step4，lint 超长触发）
> 关联代码：各条目行内 commit 号即出处 | 关联坑：无 | 关联 spec：`docs/specs/22~26`、`docs/specs/41-e2e-whitebox-eval/spec.md` | 取代：无
> 状态：已核销（T2/T4 各轮当时已经用户验收；T6 第 0/1/1.5 步为已执行完的流水）

> 本件每一节都是**逐字原文**；行内"今晚 21:30 首跑"等措辞指当时日期，引用前先看节标题。


## §一 T2 系列存档（已全部核销）

## T2 系列存档（2026-09-12 定稿 → 已全部核销）
| 项 | 状态 |
|---|---|
| T2-1 订阅模型 | ⏸ 未实施——**v1 过渡**：runMyBrief 暂读 `sources.focus=1`（2026-09-13 已标 8 源：The Verge/HN/量子位/36氪/财联社/第一财经/新华社/半导体行业观察）；T3-1 R5 吸收为行为驱动订阅 |
| T2-2 推荐算法 v1 | ⏸ 未实施——破茧/探索位语义并入 T3-1 R2；兴趣画像并入 T3-1 R5 |
| T2-3 管理后台 UX | ⏸ 并入 T3-2 |
| T2-4 taskQueue 存量失败修复 | ✅ 已完成 2026-09-12（实现退化双 bug 修复，249 全绿，P2-10 核销） |

## §二 T4-1 快改清单（全部完成）

## T4-1 快改清单（✅ 全部完成 2026-09-13 晚，commit d8b0347；实测证据见 docs/ISSUES.md 修复记录）
| # | 项 | 说明 |
|---|---|---|
| Q1 | 移除前台 EN/中文切换按钮 | i18n 机制保留代码，仅隐藏 IconRail 的语言按钮（用户明确不需要） |
| Q2 | 源库 OPML 导出 | 管理台源库加「导出」按钮 → `GET /api/opml/export`（标准 OPML 2.0，含 type/group，其他阅读器可直接导入；OPML 导入已有，补导出方向） |
| Q3 | 存储统计"库体积" | 本地=文件大小已有；Turso 云库无文件概念 → 接 Turso Platform API 或显示行数+索引数替代（定性：非 bug，是云库口径差异） |
| Q4 | 内容清理策略确认 | 现状：runner cleanup 每日只清热榜 7 天前旧数据；管理台「内容清理」Tab 可按保留天数清理（文章/视频/待处理/日报候选，预览后执行，写 Turso）。用户期望默认 7 天：把 runner cleanup 扩为按 settings retention（默认 7 天）清理普通文章，**豁免**：read_at/later 标记的、入过早报的、视频/播客全部 |
| Q5 | 源库「无字按钮」排查 | 源库工具条有纯图标按钮无文字/无 title（用户截图反馈），逐个补 title 或文字；顺带审管理台所有 icon-only 控件 |
| Q6 | 云端源库补「新建文件夹 / 删除源 / 编辑源」入口 | 云端 API 已支持（14-sources-write：sources POST/DELETE/PUT + groups 写），**缺的是 SourceLibraryTab 的 UI 控件**——补按钮+弹窗 |
| Q7 | 熔断源自动恢复 | 熔断源冻结 48h 后 runner 批次尾部自动 unfreezeSource（保留 fail_count 清零逻辑，复用 6h 错峰），连续 3 次自动恢复仍失败则延长到 7 天（防死循环）；用户可配开关 |

## §三 T4-2 源治理与反过载（全部完成）

## T4-2 源治理与反过载（依赖 specs/23 七层设计）
> 进度（2026-09-14 凌晨）：R1/R2/R3 ✅（9c4f21e）+ **R4 七层防御入报 ✅**（9a287ae：L3 配额/L4 负例/L5 权威加权+保护位/L6 MMR+滑杆——specs/23 L2-L6 全部落地，今晚 21:30 首跑见真实效果）。T4-2 完成。剩余全局项：**已全部完成**（周刊 v2 ✅ cbe6b5b / reading 骨架屏 ✅ / 行为画像+Domain 配额 ✅ 2728e44——T3-1 R5 尾收口）。后续为新需求立项，不在本清单
- **R1 源合并**：同 URL 已去重；追加"同内容源"检测——同域名+同名或同 feed 指纹的源合并（保留一个，其余 enabled=0 标 `extra.mergedInto`）。
- **R2 源并联 failover**：允许把 N 个源标为同一 failover 组（`extra.failoverGroup`），主源熔断时 runner 自动用组内备用源，全部挂才报熔断。
- **R3 频率自适应**：按源实测出文频率自动调 intervalMin（腹泻源降频聚合、低频源放宽），替代静态分层（specs/23 L1）。
- **R4 早报管线接入七层防御**：事件聚类默认化（L2）+ 报量配额（L3）+ 广告降权（L4）+ 权威加权与低曝光保护位（L5）+ MMR 探索位与多样性滑杆（L6）。

## §四 T5 需求清单已完成/作废行

| ~~T5-1~~ | 热点榜重设计 ✅ 完成（88042ce 数据层 + 2176cae 精选=AI评分流+Top5 卡+f3b1964 reason；评分回写链修复为核心） | 完成 | — |
| ~~T5-2~~ | 前后台信息架构重构 ✅ 已完成 2026-09-15（specs 26→27 今日视图+27b 四轴+29 源库三视图+30 后台 5 Tab 收敛+对照卡；commit `f338ac1`+`83db503`，278 测试全绿+云端冒烟全过+对抗性审查修复） | 完成 | — |
| ~~T5-4~~ | 视频/播客上榜可播放 | ✅ 已完成 2026-09-14（e72e22d 等：云端视频端点补齐 + 播客并入视频板块 + 早报媒体栏 + 弹窗可播放） | specs/31 |
| ~~T5-7~~ | 周刊 coverTheme 可读化（B7） | ✅ 已完成（ca2d91a） | — |
| ~~T5-8~~ | IconRail 图标配文字标签 ✅ 已完成 2026-09-15（随 spec30，rail-btn 图标+常驻文字） | 完成 | — |
| ~~T5-9~~ | 我的阅读真实性核查 | ✅ 已核查（读行为真实；24855 空串为历史批量产物） | 结论落档 |
| ~~T5-10~~ | 抖音板块下架 ✅ 已完成 2026-09-15（随 spec30，后台 Tab 移除；源仍在源库检索视图可管） | 完成 | — |
| ~~T5-12~~ | 已退役源引导（B9）/ 报警日志（B5）/ OpenAI 条目识别（B6） | ✅ 已完成（ca2d91a） | 小 spec 34 首批 |
| ~~T5-14~~ | 热点榜二阶段 ✅ 已完成（52dca08~e7e8d05 更名/扩源/分类/UI + 7e41641 事件卡对齐样图） | 完成 | — |
| ~~T5-16~~ | 热点榜三阶段+补丁 ✅ 已完成（82ae70d~a4aa375：实时流 AI 化/精选口径修复/事件信源胶囊+趋势折线+热度严格降序/译文标题+中文摘要/分类收拢/摘要兜底/详情封面/翻译优先级/事件 runner 预聚合治 504） | 完成 | specs/25 三阶段+补丁表 |
| ~~T5-15~~ | **已作废（2026-09-18）**：原「每轮修复后更新 `docs/DELIVERY-2026-09-14.md`」机制。作废理由：按日期命名的交付文档必然与 ISSUES/FEATURE_MATRIX 三份并行、且必然在下一轮开始时就过时（本次文档清洁实测该文件 14KB 全是已被取代的流水），它就是文档污染的生产器。**替代决策**：交付即写进 SSOT（问题→`ISSUES.md`，功能→`FEATURE_MATRIX.md`，坑→`pitfalls/`），已完结内容按 `docs/DOC_GOVERNANCE.md` §3 归档；旧文件已下沉 `docs/archive/debugging/2026-09-14-delivery.md` <!-- doc-lint:ignore：本行刻意引用作废前的旧路径 --> | 流程 | 作废 |

## §五 T6 第 0 步 P0 止血（已全部出账）

### 第 0 步：P0 止血 —— **用户 2026-09-19 决定：先不写生产，等评测就位**
| P0 | 内容 | 现状态 |
|---|---|---|
| BL7 | 报警链路恢复（`tools/sync-alerts-config.js --force` 从本地 `settings.alerts.channels[].config.url` 取真值） | **延后**，与 41-2 端到端剧本一起出"改前红/改后绿"证据后执行 |
| BL8 | `ai.minIntervalMs` 0 → 4000 + 下限保护 | **延后**（写生产）；但**不写生产的防再犯项可先行**：测试改快照+还原+断言、`settings.alerts` 写路径加测试指纹守卫 |
| BL9 | AI 配置写入口裁决 | **已裁决：保留可写 + 审计 + 变更告警 + 写后探测失败即回滚**，落档 `CLOUD_PIPELINE_GUIDE.md` §6.1，实施在 39-1/39-3 |
| BL10 | `'null'` 污染一行修根因 + 一次性订正 | **延后**，与 40-8 脏数据订正合并做 |
> ⚠️ 代价必须写明：在 BL7 恢复之前，**系统出任何故障都不会通知你**（熔断、停滞、AI 失败、早报降级都静默）。这是本轮选择下的已知风险，不是遗漏。

## §六 T6 第 1 步小刺打包执行结果

### 第 1 步：当天可修小刺打包（不立 spec，一批提交）
B27（`AS date`）· B28（`tabCond` 加括号）· B39（生成历史真 7 天窗 + 投影档位）· B47（任务队列面板下线）· B48（JSX `\uff08` 字面串）· B53（`slice(0,20)`、Agencs→Agnes、`llmChat()` 死函数）· B51（假开关与假文案摘除）· B54（保留天数独立保存）· B56（快照区云端分支与文案）· B57（回填块标"仅本地" + 修 `d.progress`）· B58（分类表对齐 `hot.categories`）· B22（成功率颜色变量）· B35（左缘对齐容器统一）· B38（重复 `/api/settings` 去重）· 35A-F6（系统性故障不折算单源失败，一条判据）
**第 1 步执行结果（2026-09-19 自主轮）**
- ✅ 已完成 15 项：B22 · B27 · B28 · B39 · B47 · B48 · B51 · B53 · B54 · B56 · B58 · 35A-F6，
  外加实测过程中新发现并一并修掉的 B60 / B61 / B62（同一分类判定全库 6+1 份副本收敛成一份）。
  每项都有回归锁且「改前红 / 改后绿」，共 43 条锁（`tests/regression-20260919b.test.js`、
  `tests/regression-20260919c.test.js`、`tests/regression-20260919d.test.js`）；
  四门禁 + 云端实测口径见 `docs/EVAL_GUIDE.md`。
- ⛔ 未做 3 项，各有明确原因，不是遗漏：
  - **B35 / B38** — 要改 `web/src/pages/AdminPage.jsx`，该文件有你在途未提交改动（B11 骨架屏）。
    不覆盖别人正在改的文件；等你提交后我接着做。
  - **B57** — 修法要在云端**新增一个会写库的回填端点**（`POST /api/hot/backfill` 读层版）。
    按「先不写生产，等评测就位」的在效决定暂停，已完成取证：本地 `server/routes/hot.js:59` 有、
    云端读层完全没有，前端 `backfillReady` 因此长期停在降级态。等你点头即可做（也可选"只标仅本地、不补云端"）。
- 🆕 过程中新增阻塞 **BL12**：Vercel 构建长时间卡 `Initializing`，"推了就等于上线"不成立 →
  已把「线上 commit == origin/main」做成 `eval:preflight` 的硬判据，之后所有云端实测先过这条。
- 📌 待你拍板的三件生产写：BL7 恢复报警渠道、BL8 恢复 AI 限速、BL10 订正 2.5 万行 `read_at='null'`。

## §七 T6 第 1.5 步取证工具化

**第 1.5 步：把"取证"变成一条命令，并因此摸出 4 个新缺陷（2026-09-19 深夜自主轮）**
- ✅ **41-3 交付并工具化**：`npm run eval:f2p -- --auto-base --tests <锁文件>`。本轮新增/扩充锁文件 b·c·d·e·f 五份，
  **条数不在这里抄录**（以 `npm test` 汇总为准），每份都由工具出"改前红/改后绿"双证据，落 `docs/eval/f2p/*.json`；
  取证器自带的自检项数同样以 `--self-test` 输出为准（含"基线选错必须退 2 而不是判假锁"）。
- ✅ **顺藤摸出并修掉 B64~B67**（都在"负责发现问题的工具"上）：
  云端基址 5 份手写副本且 `tools/audit-cloud.js` 打的是 2026-09-11 已下架域名（8 天零有效巡检）；
  巡检 `pass: !!verdict` 把失败理由算成通过（永远 19/19 满分）；三条判据断的是不存在的契约；
  **`eval:preflight` 把哨兵报警渠道算成「1 个可用渠道 ✓」，即 P0 的 BL7 在门禁里长期绿灯**。
  修后实测：巡检 `通过 18 / 失败 0 / 未验收 1`；preflight 如实报红，并量化 BL7 = **近 7 天 33 条事件全部未送达**。
- ✅ **spec 补齐**：38 域 8 个板块小 spec（A 壳 / B 首页 / C 源库 / D 早报中心 / E AI 台 / F 数据 / G 监控报警 / H 热榜策展）
  + `docs/specs/37-alerts-observability/37-1-p0-alert-path-restore.md`（BL7 恢复的可执行方案：快照→同步→**回读送达**→守卫→哨兵测试）。
- 📌 **BL7 现在只差你一句话**：判据、回归锁、F2P 工具、门禁显示四样都齐了，本地真值渠道（飞书）完好，
  执行 `node tools/sync-alerts-config.js --force` 即可恢复；恢复后会真的开始往飞书推消息，故未擅自执行（详见 37-1 的 T5 噪声控制选项）。
- ⚠️ 门禁现状：`npm test` 363 条 0 红、`lint:docs` 0 错、whitebox 全过、`eval:process` 7/7、`build:vercel` 通过；
  **`eval:preflight` 6/9 是刻意的红**——三条红分别是 BL7（两条：无出口 + 未送达）与 BL8（`ai.minIntervalMs=0`），

## §八 被改写的头部原文

# 后续开发需求（T3/T4 + T5-1/2/4/8/10/14/16 已核销 · T5 剩余 2026-09-15 待确认）
> 最后更新：2026-09-18（T5-15 交付文档机制作废；specs 四件套实情订正）
